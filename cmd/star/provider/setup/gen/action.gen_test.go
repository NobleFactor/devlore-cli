// SPDX-License-Identifier: Apache-2.0
// Copyright Noble Factor. All rights reserved.

// Code generated by go.render; DO NOT EDIT.

package setup_test

import (
	"strings"
	"testing"

	provider "github.com/NobleFactor/devlore-cli/cmd/star/provider/setup"
	_ "github.com/NobleFactor/devlore-cli/cmd/star/provider/setup/gen"
	"github.com/NobleFactor/devlore-cli/pkg/op"
)

func TestActionNames(t *testing.T) {

	names := []op.ActionName{
		provider.CheckHook,
		provider.InitConfig,
		provider.InstallHook,
		provider.PrecommitCheck,
		provider.PrecommitInstall,
		provider.Tools,
		provider.UninstallHook,
	}
	for _, name := range names {
		a := getAction(t, name)
		if got := a.Name(); got != name {
			t.Errorf("Name() = %q, want %q", got, name)
		}
	}
}

func TestRegister(t *testing.T) {

	expected := []op.ActionName{
		provider.CheckHook,
		provider.InitConfig,
		provider.InstallHook,
		provider.PrecommitCheck,
		provider.PrecommitInstall,
		provider.Tools,
		provider.UninstallHook,
	}
	for _, name := range expected {
		_ = getAction(t, name)
	}
}

func TestCheckHookAction_DryRun(t *testing.T) {

	action := getAction(t, provider.CheckHook)
	ctx, buf := dryRunCtx(t)
	activationRecord := op.NewActivationRecord(nil, "", ctx)

	result, undo, err := action.Do(activationRecord)
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	wantSubstring := "[dry-run] " + string(provider.CheckHook)
	if !strings.Contains(buf.String(), wantSubstring) {
		t.Errorf("dry-run output = %q, want substring %q", buf.String(), wantSubstring)
	}
}

func TestInitConfigAction_DryRun(t *testing.T) {

	action := getAction(t, provider.InitConfig)
	ctx, buf := dryRunCtx(t)
	activationRecord := op.NewActivationRecord(nil, "", ctx)

	result, undo, err := action.Do(activationRecord)
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	wantSubstring := "[dry-run] " + string(provider.InitConfig)
	if !strings.Contains(buf.String(), wantSubstring) {
		t.Errorf("dry-run output = %q, want substring %q", buf.String(), wantSubstring)
	}
}

func TestInstallHookAction_DryRun(t *testing.T) {

	action := getAction(t, provider.InstallHook)
	ctx, buf := dryRunCtx(t)
	activationRecord := op.NewActivationRecord(nil, "", ctx)

	result, undo, err := action.Do(activationRecord)
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	wantSubstring := "[dry-run] " + string(provider.InstallHook)
	if !strings.Contains(buf.String(), wantSubstring) {
		t.Errorf("dry-run output = %q, want substring %q", buf.String(), wantSubstring)
	}
}

func TestPrecommitCheckAction_DryRun(t *testing.T) {

	action := getAction(t, provider.PrecommitCheck)
	ctx, buf := dryRunCtx(t)
	activationRecord := op.NewActivationRecord(nil, "", ctx)

	result, undo, err := action.Do(activationRecord)
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	wantSubstring := "[dry-run] " + string(provider.PrecommitCheck)
	if !strings.Contains(buf.String(), wantSubstring) {
		t.Errorf("dry-run output = %q, want substring %q", buf.String(), wantSubstring)
	}
}

func TestPrecommitInstallAction_DryRun(t *testing.T) {

	action := getAction(t, provider.PrecommitInstall)
	ctx, buf := dryRunCtx(t)
	activationRecord := op.NewActivationRecord(nil, "", ctx)

	result, undo, err := action.Do(activationRecord)
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	wantSubstring := "[dry-run] " + string(provider.PrecommitInstall)
	if !strings.Contains(buf.String(), wantSubstring) {
		t.Errorf("dry-run output = %q, want substring %q", buf.String(), wantSubstring)
	}
}

func TestToolsAction_DryRun(t *testing.T) {

	action := getAction(t, provider.Tools)
	ctx, buf := dryRunCtx(t)
	activationRecord := op.NewActivationRecord(nil, "", ctx)

	result, undo, err := action.Do(activationRecord)
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	wantSubstring := "[dry-run] " + string(provider.Tools)
	if !strings.Contains(buf.String(), wantSubstring) {
		t.Errorf("dry-run output = %q, want substring %q", buf.String(), wantSubstring)
	}
}

func TestUninstallHookAction_DryRun(t *testing.T) {

	action := getAction(t, provider.UninstallHook)
	ctx, buf := dryRunCtx(t)
	activationRecord := op.NewActivationRecord(nil, "", ctx)

	result, undo, err := action.Do(activationRecord)
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	wantSubstring := "[dry-run] " + string(provider.UninstallHook)
	if !strings.Contains(buf.String(), wantSubstring) {
		t.Errorf("dry-run output = %q, want substring %q", buf.String(), wantSubstring)
	}
}

func TestCompensableActions_UndoNil(t *testing.T) {

	ctx := newCtx(t)
	activationRecord := op.NewActivationRecord(nil, "", ctx)

	names := []op.ActionName{}

	for _, name := range names {
		t.Run(string(name), func(t *testing.T) {
			ca := getCompensable(t, name)
			if err := ca.Undo(activationRecord, nil); err != nil {
				t.Errorf("Undo(nil) = %v, want nil", err)
			}
		})
	}
}
