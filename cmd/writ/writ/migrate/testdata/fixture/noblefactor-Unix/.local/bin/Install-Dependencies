#!/bin/bash
# Install common development dependencies
set -euo pipefail

if command -v brew &>/dev/null; then
    brew install jq yq gh ripgrep fd
elif command -v apt-get &>/dev/null; then
    apt-get install -y jq ripgrep fd-find
fi
