# Status: JSON Provider

**Architecture document:** [3.5.5-json-provider.md](3.5.5-json-provider.md)
**State:** implemented; first design doc written 2026-07-22 (phase-8 step 39). Matrix greped, not asserted.

## Completion

| Component | Status |
|---|---|
| `Decode` / `Encode` / `EncodeIndent` value transforms | Landed |
| `Parse` → content-addressed `json.Resource` (canonical-form SHA-256, content-keyed interning, producer stamp) | Landed |
| Catalog rehydration (`UnmarshalJSON`/`Text`/`YAML` via runtime environment) | Landed |

## Document Discrepancies

None known — written 2026-07-22 against the tree.

## Test matrix (verified 2026-07-22)

| Method | Go | Starlark | Open gaps |
|---|---|---|---|
| `Decode` | `TestDecode_ParsesValues`, `TestDecode_Error` | `test_json.star` | — |
| `Encode` / `EncodeIndent` | `TestEncode_MarshalsValues`, `TestEncode_Error`, `TestEncodeIndent_Indents` | `test_json.star` | — |
| `Parse` | `TestParse_ProducerStamp` | `test_imm_json.star` | — |
| `Resource` (identity + canonicalization) | 22 resource tests: canonical hashing (`TestNewResource_{BytesHashesCanonical, KeyOrderingDoesNotAffectURI, WhitespaceDoesNotAffectURI, NestedKeyOrderingDoesNotAffectURI, ArrayOrderingDoesAffectURI, DataIsCanonical}`), rejection (`RejectsInvalidJSON`, `RejectsUnsupportedType`), producer stamp, reader/URI agreement, `TestDiscoverResource_*` ×2, addressing/digest/equality ×6, rehydration `TestUnmarshal{JSON,Text,YAML}_*` ×4, `TestValidate_AcceptsConformingDocument` | via `test_imm_json.star` | — |

## Outstanding

None.
