# Status: Flow Provider

**Architecture document:** [3.5.2-flow-provider.md](3.5.2-flow-provider.md)
**State:** Design + partial implementation (2026-07-01). The shared per-subgraph-executor + stamped-recovery-stack core
landed with phase-8 step 31; all four combinators are implemented — Subgraph, Gather, Choose (decision tree,
2026-07-02), and WaitUntil (body-subgraph polling, 2026-07-02). Plans:
[`31-subgraph-executor-ownership.md`](../plans/extract-starlark-from-op/phase-8/steps/31-subgraph-executor-ownership.md),
[`31.2-gather-resume.md`](../plans/extract-starlark-from-op/phase-8/steps/31.2-gather-resume.md),
[`10-plan-choose.md`](../plans/extract-starlark-from-op/phase-8/steps/10-plan-choose.md).

## Completion

| Component | Status | Notes |
|-----------|--------|-------|
| Shared core: per-subgraph executor owns its recovery stack | Landed (step 31) | `newChildExecutor`; the combinator takes `activation.Stack`, never mints. |
| Shared core: stamped `RecoveryStack` (`Stamp` / `UnitID` / `Result` / `Err` / `NestedStackByUnitID` / `NewChildRecoveryStack`) | Landed (step 31.2) | Identity/outcome on the stack, not an embedded receipt; serialized + retyped on resume. |
| Shared core: env-deferred unwind (companions take `activation`) | Landed (step 37) | `Unwind(runtimeEnvironment)`; `Push`/`PushNested` drop the env. |
| `Subgraph` — base case | Landed | Owns `activation.Stack`, walks children once, returns the stack. |
| `Gather` — N per item + resume | Landed (step 31.2) | Stamped per-iteration substacks (`"<gatherID>#<i>"`), concurrent up to `limit`, resume replays. |
| `Choose` — conditional-edge decision tree | Landed (2026-07-02) | `ChoosePlanner` builds `when`/`then`/`default` subgraphs + guarded edges (the decision tree); `flow.Choose` just executes the graph (`walkDecisionTree` behind `hasConditionalEdges`). Resume re-derives each branch from the node's result (step 42 slice 2c; was: guard-stamped receipts replaying the recorded path), `validateGuardedEdges` + ordering-edge cycle validation at both boundaries, lambda bodies via the new `function.call` leaf. Goal proof `TestChoose_UnchosenInvocationBranchDoesNotRun` green. |
| `WaitUntil` — poll-until-truthy container | Landed (2026-07-02) | Body subgraph (list / singleton / lambda via `function.call`) re-evaluated each poll on a scratch stack; falsy polls drop, the truthy run stamps the unit's ID and nests; required `timeout` fails with a plain error; resume re-enters fresh (step 12). |

## Document Discrepancies (design vs. current code)

- **Delegation.** `Gather`/`Choose`/`WaitUntil` share Subgraph's `walkSubgraphChildren` directly rather than calling
  the `Subgraph` method (see the design doc's §10.1) — an intentional interim until a child-activation affordance
  exists.

## Outstanding / open questions

Tracked in the design doc's §10:

1. A framework affordance to derive a child activation, so combinators call the `Subgraph` method and `Subgraph` itself
   unifies onto the stamped-stack shape.
2. A public-API pause-mid-combinator resume test (needs mid-run pause injection; lands with the eventing API, step 36).
3. ~~WaitUntil's remaining test-matrix rows~~ — closed at executor level with step 12 (2026-07-16):
   `TestWaitUntil_MatchAfterNPolls` (mid-run mutable probe) and `TestWaitUntil_BodyErrorFailsImmediately` in the
   plan package, plus the context-cancel row (2026-07-08). Only `.star` fixture variants remain absent
   ([step 52](../plans/extract-starlark-from-op/phase-8/steps/52-test-backfill-round-2.md) intake).

## Test matrix (verified 2026-07-03; terminals + WaitUntil rows re-verified 2026-07-22)

Per-method coverage, split by layer. "Go" = named unit/API tests in `pkg/op/provider/flow` (plus the plan package's
lifecycle suite where noted); "Starlark" = devlore-test fixtures.

| Method | Go | Starlark | Open gaps |
|---|---|---|---|
| `Subgraph` | `TestSubgraph_ReturnsActivationStack`, `TestSubgraph_CompensateSubgraph_RoundTrip`, `TestCompensateSubgraph_NilStack_NoOp`, `TestSubgraphBoundAction_FlowsLeafResult` (+`_Starlark`), `TestBareNodeUnderRoot_FlowsLeafResult`; plan lifecycle suite roots through it | implicit root of every fixture; explicit in `test_writ_adopt_subgraph.star` and every gather/choose body | `items=` iteration unimplemented; no dedicated `plan.subgraph` fixture |
| `Gather` | `TestGather_StampsIterationSubstacks`, `TestGather_Resume_ReplaysCompletedIterations`, `TestGather_Resume_ReentersPausedIteration`, `TestCompensateGather_{Nil,Empty}Stack_NoOp`; plan: `TestGatherFailureUnwind_ViaPublicAPI` | `test_gather_{basic,concurrency,advanced}.star` (~16 scenarios) | — |
| `Choose` | `TestChoose_ReturnsActivationStack`, `TestChoose_CompensateChoose_RoundTrip`, `TestCompensateChoose_{Nil,Empty}Stack_NoOp`; walk: `TestRoot_FindsEntryNode`, `TestBranch_*` ×5, `TestHasConditionalEdges`, `TestNewCase_*` ×2; op: guard marshaling, `validateGuardedEdges` rejections | `test_choose_{literals,lambdas,predicates,exists,not_exists}.star`, `test_is_dir.star`, `test_is_file.star`, goal proof `test_choose_unchosen_branch.star` | end-to-end save/reload replay of a choose (branch-from-result replay unit-tested only — step 42 slice 2c) |
| `WaitUntil` | `TestWaitUntil_TimeoutOnFalsyBody`, `TestWaitUntil_TimeoutRequired`, `TestCompensateWaitUntil_NilStack_NoOp`; plan: `TestWaitUntil_MatchAfterNPolls`, `TestWaitUntil_BodyErrorFailsImmediately` (the step-12 edge rows, closed 2026-07-16) | `test_wait_until.star`, `test_wait_until_timeout.star` | `.star` fixture variants of the edge rows (proven at executor level only) |
| `Complete` | `TestComplete_ReturnsOutput` (terminals_test.go, landed at step 24's close 2026-07-18) | `test_flow_complete.star` | — |
| `Degraded` | `TestDegraded_RendersAndReturnsMessage` (pinned the Go-template render contract + that `Degraded` USES its activation via `Transition`) | `test_flow_degraded.star`, `test_flow_degraded_template.star` | — |
| `Failed` | `TestFailed_RendersAndReturnsMessage` | `test_flow_fatal.star`, `test_flow_fatal_recovery.star`, `test_flow_fatal_template.star` | — |
| `isTruthy` (shared) | 7 unit tests (nil / bool / int widths / floats / string / containers / zero-structs + typed nils) | exercised by every choose and wait_until fixture | — |
