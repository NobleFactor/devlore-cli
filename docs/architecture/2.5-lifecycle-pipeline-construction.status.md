# Status: Lifecycle Pipeline Construction

**Architecture document:** [2.5-lifecycle-pipeline-construction.md](2.5-lifecycle-pipeline-construction.md)

## Completion

| Component | Status | Completed | PR |
|-----------|--------|-----------|-----|
| Framework prereq (construction-time annotations, key-agnostic receipt, `Assemble` slots/`Tool`, `plan.origin`) | Complete | 2026-05-30 | — |
| `lore deploy` construction (phase-script harvest, native-PM invocations, `NewGraph`) | Not started | — | — |
| Save / load / run portability proven (docker, Ubuntu, built on macOS) | Not started | — | — |
| Provider ambient annotations (script-unit `annotations["package"]`) | Deferred | — | — |

## Document Discrepancies

None — the document describes the target model. The framework prereq is in place; the command-side construction
is the in-progress work tracked by
[`../plans/extract-starlark-from-op/phase-8/lore-command-rewrites.md`](../plans/extract-starlark-from-op/phase-8/lore-command-rewrites.md).

## Outstanding Work

1. `lore deploy` ground-up rewrite against the model (the first proving command).
2. Rewrite the docker `Linux.Debian` phase scripts from `Install-Docker` against the current `plan.*`.
3. Prove build → save → load → run on Ubuntu (graph built on macOS).
4. Provider ambient-annotations API — closes script-produced units' `annotations["package"]`.
