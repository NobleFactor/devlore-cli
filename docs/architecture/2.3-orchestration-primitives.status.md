# Status: Orchestration Primitives

**Architecture document:** [2.3-orchestration-primitives.md](2.3-orchestration-primitives.md)

**State:** rewritten 2026-07-21 (phase-8 step 34, slice C). The 2026-06-20 dated section (subgraph-executor
recovery-stack ownership) is promoted to the document's spine, updated where its "current deviation" paragraphs have
since landed: step 31 (per-subgraph executors own the stacks; combinators no longer mint them), step 35 (the retry
tri-state + jitter it flagged as unrealized), step 42 slice 3a (stamped nested stacks pushed directly — the
"slice of stacks" Gather shape and the ride-on-audit-receipt nesting are superseded). The phase-based body
(`Phase as Universal Boundary`, `ExecutePhaseInner`, `SlotProxy`/`GatherRef`, `Fatal`, the `Elevate` flow-action
stub, the `internal/execution/flow/*` file map) is replaced; `Fatal` → `flow.failed` (policy-adjudicated), and
elevation is recorded as a per-unit policy ([2.6](2.6-execution-policies.md)/[6.1](6.1-privilege-elevation.md)), not
a flow action. The pre-rewrite tracking-issue pointer (#92) was dropped with the body it tracked. Every code claim
verified against the tree on 2026-07-21 (`pkg/op/provider/flow/provider.go` signatures + `action_names.gen.go`,
`hooks.go`, `recovery_stack.go`).

## Completion

| Component | Status | Completed | PR |
|-----------|--------|-----------|-----|
| The four combinators on per-subgraph executors | Complete | 2026-07 (steps 9–12, 31) | phase-8 branch |
| Choose as plan-time branch construction (no runtime selection) | Complete | 2026-07-08 (step 10) | phase-8 branch |
| Stamped nested-stack recovery tree | Complete | 2026-07-12 (step 42 3a) | phase-8 branch |
| Terminals: `flow.complete` / `flow.degraded` / `flow.failed` | Complete | 2026-07-11 (step 41) | phase-8 branch |
| Field projection (`plan.item`) | Complete | 2026-07-15 (step 45) | phase-8 branch |
| Lifecycle hooks (seam present; production consumers pending) | Partial | — | step 50 |
| Document rewrite onto the landed model | Complete | 2026-07-21 (step 34 slice C) | phase-8 branch |

## Document Discrepancies

None known — the 2026-07-21 rewrite grounds every claim in the current tree.

## Outstanding Work

1. **Lifecycle hooks integration** — the hook interface drift (`OnSubgraph*` vs the spec's phase callbacks) and the
   hook-fed event stream are
   [step 50](../plans/extract-starlark-from-op/phase-8/steps/50-eventstream-narration-integration.md).
2. The scattered-reference sweep across sibling docs is step 34 slice D
   ([step 34](../plans/extract-starlark-from-op/phase-8/steps/34-architecture-docs-rewrite.md)).
