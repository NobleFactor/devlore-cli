# Status: Function Provider

**Architecture document:** [3.5.14-function-provider.md](3.5.14-function-provider.md)
**State:** implemented; first design doc written 2026-07-22 (phase-8 step 39). Matrix greped, not asserted.

## Completion

| Component | Status |
|---|---|
| Closure extraction + literal synthesis (`formatLiteral` over the full value range) | Landed |
| The function pack (source + bytecode sections, magic header, CAS path, recovery-site archival) | Landed |
| `Resource.Init` (mmap fast path; compiler-version-skew recompile fallback) | Landed |
| `function.call` (the lambda-desugar leaf, step 10) | Landed |

## Document Discrepancies

None known — written 2026-07-22 against the tree.

## Test matrix (verified 2026-07-22)

| Method | Go | Starlark | Open gaps |
|---|---|---|---|
| `Call` | — | `test_choose_lambdas.star` (lambda bodies desugar to `function.call`) and every lambda-bodied combinator fixture | dedicated units ([step 52](../plans/extract-starlark-from-op/phase-8/steps/52-test-backfill-round-2.md) row 18) |
| literal synthesis | `TestFormatLiteral_*` ×17 (none / bool / int / float / string+escaping / list / dict / tuple / nested / struct / unsupported) | via lambda fixtures | — |
| the function pack | `TestFunctionPack_{RoundTrip, SourceSectionReads, CompiledSectionReads, EmptyCompiled}`, `TestReadFunctionPackHeader_{BadMagic, Truncated}` | — | — |
| `Resource` | 8 tests: interface, producer stamp, recovery-site archival, `DiscoverResource` spec-type guard, `Init` fast path / mmap fallback / version-skew fallback / non-pointer guard | — | — |

## Outstanding

The step-52 row above.
