# Status: Platform and Package Management

**Architecture document:** [3.4-platform-package-managers.md](3.4-platform-package-managers.md)

The design is settled (2026-06-04) and was **reversed to an op-free `pkg/platform`**: `platform.Platform` stays in
`pkg/platform` (importing nothing from `pkg/op`), `pkg/op` imports it, and there is **no `op.Platform` /
`platformData`** — identity serialization into the graph is deferred to
[#282](https://github.com/NobleFactor/devlore-cli/issues/282). The `pkg/platform` reshape (Composite router, purl
routing with construction-time normalization, the native driver catalog, per-leaf state-query verification, named
`*Spec` factories) is **implemented and style-compliant**. The `pkg/platform` test rewrites and the `pkg.Provider`
veneer migration (#6 — `provider/pkg` + `provider/service`) are **done**; the consumer migration completed 2026-07-04
(`cmd/internal/lorepackage` was the last old-surface consumer — `cmd/lore` and `cmd/devlore-test` were already on
`platform.Detect`/`platform.New`; `cmd/writ` stays broken on unrelated sealed-graph drift, phase-8 step 33) and the
`pkg/op` duplicate surface (`op.Platform`, `op.PURL`, the string-based managers) is **deleted** (phase-8 step 19).
It supersedes the stale "Darwin Package Manager Idempotence" treatment formerly in
[§3 Action Namespaces](3-operation-namespaces.md), which was OS-specific and built on the removed `Host` interface.

## Completion

| Component | Status | Notes |
|-----------|--------|-------|
| `platform.Platform` interface (op-free) + `Spec` / `New` reshape | **Done** | `pkg/platform`; serialization deferred to #282 |
| `platform.PackageManager` Composite router + native leaf reshape | **Done** | router + `driver` + 4 leaf families, style-compliant |
| Per-leaf state-query verification (`bracket`) | **Done** | pre/post re-query; one `Receipt` per package |
| Package-index `Update` — manual force-refresh + automatic staleness gate (#9) | **Done** | `Update()` + router fan-out + per-leaf `refresh` (apt/dnf/pacman/brew/port); `ensureFresh` gate on Install/Upgrade/Search/Available (apt/pacman mtime; brew/dnf/port defer); hang-proof shell-out (`sudo -n` / timeout / `yes`-stdin). Tested |
| `pkg/platform` test rewrites | **Done** | rewritten to the new API; green, with `Update`/`refresh` coverage |
| Shared language drivers (`npm`/`pip`/`gem`/`cargo`/`go`) | Not started | additive, after the veneer |
| `pkg.Provider` veneer + `op.Receipt` adaptation | **Done** | #6 (two commits): `provider/pkg` + `provider/service` on the Composite router; inventory mitigation reverted; `pkg/op` green |
| Op-side deletes (`op.Platform` / `op.PURL` / `op` managers) | Not started | after the veneer migrates `lorepackage` |
| Run terminals + compensation-failure contract | Not started | phase-8 step 21 |

## Source plans

- [phase-8/platform-unification.md](../plans/extract-starlark-from-op/phase-8/platform-unification.md) — the
  contract flip and Composite router (step 19).
- [phase-8/pkg-install-reconciler.md](../plans/extract-starlark-from-op/phase-8/pkg-install-reconciler.md) — the
  `pkg.Provider` veneer (step 20).
- [phase-8/compensation-failure-contract.md](../plans/extract-starlark-from-op/phase-8/compensation-failure-contract.md)
  — run terminals, error actions, journal, restart (step 21).

## Outstanding Work

1. Land step 19 (the coupled contract flip — one commit; see the plan's phasing note) ahead of the Scenario-1
   `lore deploy docker` milestone.
2. Add the shared language drivers and per-driver verification incrementally.
3. Land step 21 (run terminals + compensation-failure contract); step 20 (the `pkg.Provider` veneer, #6) is **done**.
4. Cross-link from [§4.1 Resource Identity](4.1-resource-identity.md) and [§4 §6.6](4-resource-management.md) once
   `pkg.Resource` identity and the platform-data-provider sections are reconciled with this document.
