# Status: Service Provider

**Architecture document:** [3.5.11-service-provider.md](3.5.11-service-provider.md)
**State:** implemented; first design doc written 2026-07-22 (phase-8 step 39). Matrix greped, not asserted.

## Completion

| Component | Status |
|---|---|
| Five compensable pairs over `Platform.ServiceManager` delegation | Landed |
| Prior-state receipts (`was_running` / `was_enabled`) + semantic compensation | Landed |
| `CompensateRestart` no-op ruling | Landed |
| Predicates (`Enabled` / `Exists` / `Running`) | Landed |
| `Receipt.RestoreEncoded` via the catalog's URI→id namespace | Landed (step 44) |

## Document Discrepancies

None known — written 2026-07-22 against the tree.

## Test matrix (verified 2026-07-22)

| Method | Go | Starlark | Open gaps |
|---|---|---|---|
| `Start` / `CompensateStart` | `TestStart_{Success, AlreadyRunning, Error}`, `TestCompensateStart_Success` | `test_service.star` | — |
| `Stop` / `CompensateStop` | `TestStop_Success`, `TestCompensateStop_Success` | `test_service.star` | — |
| `Enable` / `CompensateEnable` | `TestEnable_Success`, `TestCompensateEnable_Success` | `test_service.star` | — |
| `Disable` / `CompensateDisable` | `TestDisable_Success`, `TestCompensateDisable_Success` | `test_service.star` | — |
| `Restart` / `CompensateRestart` | `TestRestart_{Success, Error}` | `test_service.star` | — |
| Predicates | `TestPredicates_ReportState` | `test_service.star` | — |
| `Receipt` | `TestReceipt_RestoreEncoded_JSONandYAML` | via the resume suites | — |
| `Resource` | 15 tests: construction (name / tag URI / rejection / producer stamp), addressing, digest/etag, equality ×3, rehydration ×4 | — | — |

## Outstanding

None.
