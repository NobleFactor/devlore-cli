# Status: Resource Registration

**Architecture document:** [4.3-resource-registration.md](4.3-resource-registration.md)

**State:** rewritten 2026-07-22 (phase-8 step 51, slice 3). The lazy-descriptor body — `ResourceDescriptor.Init`,
`RegisterConstructor`, `loadConstructor`, `ResourceFromValue`, the `starvalue_marshal.go` / `action_reflect.go`
call-site table, and the callable-extraction section — is replaced; none of those symbols exist in the tree. The
landed model: eager generated announcements into the one receiver registry
(`AnnounceResource(type, construct, methodParameters, sourceTypes...)` with environment-aware constructors), the
three construction paths (target-driven conversion, source-driven via `ConstructorForSource`, rehydration via
`ResourceConstructorByTypeID`), and callables as `function.Resource`. The source-type declaration section is
preserved (verified current against `pkg/op/planner.go` and the registry).

## Completion

| Component | Status |
|-----------|--------|
| Generated per-resource announcements (`gen/<resource>.gen.go`) | Landed |
| Environment-aware `ResourceConstructor` | Landed |
| Source-type declaration + `ConstructorForSource` (planner path) | Landed (step 25 arc) |
| Rehydration by canonical type id | Landed |
| Callables as `function.Resource` (extraction model retired) | Landed ([3.5.14](3.5.14-function-provider.md)) |
| Document rewrite onto the landed model | Complete 2026-07-22 (step 51 slice 3) |

## Document Discrepancies

None known — written 2026-07-22 against the tree.

## Outstanding Work

None for this document. Remaining step-51 slices are tracked in
[step 51](../plans/extract-starlark-from-op/phase-8/steps/51-documentation-debt.md).
