# Status: Plan Provider

**Architecture document:** [3.5.3-plan-provider.md](3.5.3-plan-provider.md)
**State:** implemented; first design doc written 2026-07-21 (phase-8 step 39). Every announced method covered; the
test matrix below is verified against the tree (test names greped, not asserted).

## Completion

| Component | Status | Notes |
|-----------|--------|-------|
| Three-tier `ResolveAttr` (adapters / promoted / own) | Landed | phase-8 D12 + I4; collision panics at construction |
| Detached-invocation model + `Plan` (the Go door) | Landed | phase-8 D5; session `InvocationRegistry` |
| `AssembleDefinition` (project → catalog transfer → seal → orphan scan → validate) | Landed | steps 13/14/16 |
| `Spec` / `Run` lifecycle | Landed | thin over `op.NewGraphExecutor` |
| `SaveDefinition` / `LoadDefinition` round trip | Landed | step 13; JSON + YAML by extension |
| `Case` / `Variable` / `Item` / `Origin` / `Clear` | Landed | `Item` = step 45 field projection |

## Document Discrepancies

None known — written 2026-07-21 against the tree.

## Test matrix (verified 2026-07-21)

Per-method coverage. "Go" = named tests in `pkg/op/provider/plan` (or the named package); "Starlark" = devlore-test
fixtures.

| Method | Go | Starlark | Open gaps |
|---|---|---|---|
| `ResolveAttr` (+ adapter) | `TestProvider_ResolveAttr_{Tier1_SubNamespaceAdapter, Tier2_PromotedBuiltin, Tier3_OwnMethod, RootProviderExcludedFromTier1, UnknownReturnsNil, TierOrder}`, `TestProvider_BuildPromotedBuiltins_PanicsOnCollision_{PromotedVsOwn, PromotedVsSubNamespace}`, `TestProvider_PromoteRootMethods_PanicsOnDuplicateRootMethod`, `TestAdapter_Attr_RoutesToMethod` | every fixture resolves through it | — |
| `Plan` | exercised via `TestLifecycle_ViaGoAPI` and the writ/lore command suites (`cmd/lore/lore/builder.go`, upgrade / adopt / decommission); dedicated error-path units `TestPlan_{InvalidActionName_NoDot, UnknownActionProvider, UnknownMethod, MalformedKwargs_WrongType}` | n/a (Go door) | closed — `TestPlan_{InvalidActionName_NoDot, UnknownActionProvider, UnknownMethod, MalformedKwargs_WrongType}` (`provider_units_test.go`) |
| `AssembleDefinition` | `TestAssembleDefinition_{MaterializesGraphFromInvocations, TransfersCatalogOwnership, OrphanInvocation_Errors}`, `TestNewProvider_BuildsNoGraph`, `TestProjectToBinding_Dispatch` | 60 fixtures call `plan.assemble_definition` directly | — |
| `Spec` | `TestProvider_Spec_DefaultsFromPlanningEnvironment` | fixtures via `plan.spec()` | — |
| `Run` | `TestProvider_Run_NilArguments_Error` + the lifecycle suite (`TestGraphSaveLoadExecuteTrace_ViaPublicAPI`, `TestGraphPauseResume{,Nested}_ViaPublicAPI`, `TestGraphSaveLoadResume_ViaPublicAPI`, `TestGraphResumeThenFail_RollsBack_ViaPublicAPI`, `TestGraphResumePromiseFidelity_ViaPublicAPI`, `TestGraphStop_UnwindsToStopped_ViaPublicAPI`, `TestGatherFailureUnwind_ViaPublicAPI`, `TestGitCloneResumeThenFail_RollsBack_ViaPublicAPI`) | 60 fixtures call `plan.run` | — |
| `SaveDefinition` / `LoadDefinition` | `TestGraphSaveLoadExecuteTrace_ViaPublicAPI`, `TestGraphSaveLoadResume_ViaPublicAPI`, `TestGraphSaveLoad_ContentTransport`, `TestGraphSaveLoadExecute_ViaStarlark` | via the Starlark lifecycle test | — |
| `Case` | `TestNewCase_*` (flow package) | `test_choose_{literals,lambdas,predicates,exists,not_exists}.star`, `test_choose_in_gather.star` | — |
| `Variable` | `TestPlannedDeferredDefault_ResolvesAtDispatch` (default resolution) | every gather body (`plan.variable("item")`) | no dedicated unit test of `field=` projection via `Variable` (covered via `Item`) |
| `Item` | op-side field-projection tests (step 45: 5 op tests) | `test_gather_projection.star`, `test_gather_projection_missing_field.star`, `test_choose_in_gather.star` | — |
| `Origin` | exercised via writ/lore graph construction; `TestOrigin_StampsScope` | n/a (Go-side concern) | closed — `TestOrigin_StampsScope` (`provider_units_test.go`) |
| `Clear` | `TestClear_ResetsLedger` | — | closed — `TestClear_ResetsLedger` (`provider_units_test.go`) |
| `InvocationRegistry` | exercised by every planner dispatch | n/a (accessor) | — |

## Outstanding / open questions

Tracked in the design doc's §9: signing at assembly (step 46 follow-on); `Case`'s cross-provider construction type.
The former matrix gaps (dedicated `Plan` error-path, `Origin`, `Clear` tests) landed with
[step 52](../plans/extract-starlark-from-op/phase-8/steps/52-test-backfill-round-2.md) — see its Disposition.
