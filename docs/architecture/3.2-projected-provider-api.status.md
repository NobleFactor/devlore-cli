# Status: Projected Provider API

**Architecture document:** [3.2-projected-provider-api.md](3.2-projected-provider-api.md)

**State:** rewritten 2026-07-22 (phase-8 step 51, slice 5b). Replaced: the announce-and-callback registration
(`op.Announce` / `InitAll` / `Provider`/`PlannedProvider`/`ImmediateProvider` descriptors — zero tree hits; the
pre-rewrite status companion called that section "accurate and current," which had itself rotted), the generated
planned/immediate receiver files (receivers are projected at runtime by the bridge), the `pkg/projection` package
(gone), the `//devlore:plannable` directive (superseded), and the lore/writ consumer flows (both
rewrote onto the shared plan provider — steps 33/47). Preserved and re-verified: **Pluggable brokers** and
**Member Projection** (incl. the 2026-06-09 one-registry consolidation). The promise model restated onto detached
invocations and the sealed binding set; consumers restated (star's LKG codegen, lore's dual door, writ's deploy
family, devlore-test). `3.3` is **archived** (user decision 2026-07-22) with a banner naming the landed
alternative; its one shipped idea (`+devlore:property`) credited.

## Completion

| Component | Status |
|-----------|--------|
| Announce-with-metadata registration + one process-global receiver registry | Landed (consolidated 2026-06-09) |
| Runtime projection (`goReceiver`, the Converter, the three-tier plan surface) | Landed |
| Per-method `Modifiers` (`+devlore:property`) | Landed |
| Member projection rules (fields/methods/properties, interface unwrap, env-free resolution) | Landed |
| Broker pattern (provider-owned routers; config tree = broker tree) | Design current (realization: 6.1 / step 38) |
| Document rewrite onto the landed model | Complete 2026-07-22 (step 51 slice 5b) |

## Document Discrepancies

None known — the 2026-07-22 rewrite grounds every claim in the current tree.

## Outstanding Work

None for this document. Remaining step-51 slices are tracked in
[step 51](../plans/extract-starlark-from-op/phase-8/steps/51-documentation-debt.md).
