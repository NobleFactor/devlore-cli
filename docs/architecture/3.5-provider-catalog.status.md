# Status: Provider Catalog

**Architecture document:** [3.5-provider-catalog.md](3.5-provider-catalog.md)
**State:** Living index (created 2026-06-28). Maintained alongside `pkg/op/provider`; updated whenever a provider is
added, removed, or gains a dedicated design document.

## Completion

| Component | Status | Notes |
|-----------|--------|-------|
| Catalog of action + resource providers | Complete (2026-06-28) | 18 action providers + 2 resource providers enumerated from `pkg/op/provider`. `stream/` is an empty directory and is omitted until it carries a provider. |
| Per-provider design docs | In progress | `3.5.1` Archive Provider written; remaining providers link to existing domain docs (`3.4`, `4.2`, `6.1`) or are marked `—`. |

## Document Discrepancies (catalog vs. current code)

- Several rows reference existing domain docs (`pkg` / `platform` → `3.4`, `mem` → `4.2`, `elevator` → `6.1`) rather
  than dedicated `3.5.x` documents.

## Outstanding

- Decide which remaining providers warrant their own `3.5.x` document versus a catalog row only.
- Keep the role-zone column synchronized with each provider's `+devlore:access` directive.