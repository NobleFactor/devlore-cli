# File Provider

> **Status:** implemented; documented 2026-07-22 (phase-8 step 39 — the provider had no design doc despite being the
> largest action surface); **mutator consumption revised 2026-08-22** (#585 — mutation targets are consumed, claimed
> resources gated by `on_missing`; [4-resource-management.md](4-resource-management.md) §3). Owner of the **unified
> file-mutation core**: one receipt type, one compensating action, the sealed filesystem taxonomy, and the
> conflict-policy write seam. Companion: [`3.5.4-file-provider.status.md`](3.5.4-file-provider.status.md).

See also:

- [3.5-provider-catalog.md](3.5-provider-catalog.md) — the provider catalog this document is a member of
- [2.2-phase-execution.md](2.2-phase-execution.md) — receipts, compensation, the saga machine
- [4-resource-management.md](4-resource-management.md) — the resource catalog the taxonomy interns into
- [5.3-recovery-site.md](5.3-recovery-site.md) — the archival service compensation restores from
- [2.6-execution-policies.md](2.6-execution-policies.md) — the runtime config the conflict policy reads

## Thesis

The file provider (`pkg/op/provider/file`, announced `op.RoleModule|op.RoleAction`, `+devlore:access=both`) is the
framework's filesystem authority: 24 registered actions spanning mutation, observation, and path algebra, all
confined to the runtime environment's `fsroot.Dir`. Three ideas carry the design:

1. **One receipt, one compensating action.** Every mutation — create, update, delete, file or directory, from any
   method — produces the same `*file.Receipt`, and every receipt names the same undo:
   `file.compensate_file_mutation`. `CompensateFileMutation` inverts *any* file mutation by dispatching on the
   receipt's `MutationKind` — so a receipt built by `WriteText`, `Move`, or archive extraction compensates
   identically, regardless of which method or dispatcher created it.
2. **A sealed, intent-declared taxonomy** (phase-8 step 23). `Entry` is the closed interface over exactly three
   variants — `*Regular`, `*Directory`, `*SymbolicLink` — sealed by an unexported marker method. Kind is
   **declared by the plan, never stat-assigned**: a plan that asserts one kind while the disk shows another is a
   kind-mismatch error (`errKindMismatch`), and the same URI claimed under two kinds is a plan conflict surfaced at
   the earliest claim.
3. **The write seam** (phase-8 step 49). Every write-path method routes occupied targets through the run's conflict
   policy — `stop` refuses, `skip` leaves the occupant untouched, `replace` (the floor) archives then overwrites —
   read live from the announced `runtime` config section.

## 1. The taxonomy

| Type | Kind | Notes |
|---|---|---|
| `Entry` | the sealed interface | `op.Resource` + `Path() fsroot.Path` + the unexported seal |
| `*Regular` | regular file | the read/write target type (`ReadBytes`/`ReadText` take it) |
| `*Directory` | directory | `Mkdir`/`RemoveAll` territory |
| `*SymbolicLink` | symlink | `Link`/`Unlink` territory; `Exists` is lstat — a link's existence is the link, never its target (2026-08-22) |
| `Resource` | the catch-all base | deliberately **outside** the seal — a hand-built base value cannot enter a taxonomy signature |

Candidates are built (`buildCandidateAs`) carrying the variant's canonical type id — the key the framework's
rehydration constructors and the pre-flight staging gate dispatch on — then interned into the resource catalog.
`candidateOfMode` kinds an already-observed entry; FIFO / socket / device kinds are errors.

## 2. The unified mutation core

`*file.Receipt` embeds `op.ReceiptBase` (the affected resource + the `TransactionID` the `op.RecoverySite` uses as
its recovery key) and adds:

| Field | Role |
|---|---|
| `MutationKind` | which mutation happened — the dispatch key for the undo |
| boundary `Resource` | the edge between pre-existing state and what the action created; compensation walks toward it, exclusive (`Mkdir` sets the nearest pre-existing ancestor so undo stops its upward removal there) |
| source `Resource` | the original location, for move-like operations |
| recoveryDigest | digest of the archived bytes at archive time — compensation re-hashes the recovery archive and compares, detecting tampering of the recovery store |

The five kinds and their inversions:

| `MutationKind` | Undo |
|---|---|
| `create_file` | remove the file |
| `update_file` | restore the archived prior content |
| `delete_file` | restore the archived file |
| `create_dir` | remove the directory (walking up to the boundary) |
| `delete_dir` | recreate the directory |

`CompensateWalkTree(activation, stack)` is the one batch companion: `WalkTree`'s per-entry receipts nest on a
`*op.RecoveryStack`, and compensation unwinds it — the same composite shape the flow combinators use
([2.2](2.2-phase-execution.md)).

## 3. The write seam and the conflict policy

At every write-path occupied-target check (`provider.go`'s seam), the run's conflict policy governs:

| Policy | Behavior |
|---|---|
| `stop` | error: "target is occupied and the conflict policy is stop" |
| `skip` | the occupant is left untouched (`errConflictSkip` internally; the action reports the skip) |
| `replace` | **the floor** — archive the occupant to recovery, then overwrite; compensation restores it |

The floor is `replace` deliberately (step 49 amended it from `stop`): in-place updates by the tools' own re-renders
are not conflicts, and a seam-floor `stop` broke every in-place updater. `writ deploy`'s pre-flight applies the
stricter occupancy classification above the seam. Known gap: the encryption provider's write path bypasses the seam
(pre-flight still gates it) — recorded in step 49.

## 4. The action surface

**Compensable mutators** (each returns its result plus a `*Receipt`; undo = `CompensateFileMutation` unless noted).
**Mutation targets are consumed, claimed resources** (ruled 2026-08-20; delivered 2026-08-22, #585): `remove`,
`unlink`, `remove_all`, and `move`'s source are resource-typed, so each mints a pending claim at plan time and is
verified by its consuming scope; a missing target follows `on_missing` (`stop`, the default, fails the scope;
`ignore` makes the call and the receipt records the no-op), and destruction transitions the catalog entry Gone with
the destroyer stamp ([4-resource-management.md](4-resource-management.md) §3).

**Claims are kind-honest** (ruled 2026-08-22, #586): each taxonomy kind's `Exists` is lstat plus a kind test, so a
claim activates only when the disk holds the CLAIMED kind — a `*Regular` claim over a directory or a symbolic link
fails verification at the starting line rather than activating kind-blind. Kinds are lstat-strict throughout (a
symlink to a regular file is kind symbolic-link), which is why the follow is its own action, `file.resolve`.

| Action | Announced kwargs | Notes |
|---|---|---|
| `file.backup` | `source_path, backup_suffix` | timestamped backup via `Move` |
| `file.copy` | `source, destination_path, chmod?, chown?` | contents copy with mode + Dockerfile-style ownership |
| `file.link` | `source_path, target_path, verbatim?=false` | symlink; archives any displaced entry; `verbatim` stores the literal `source_path` instead of absolutizing it |
| `file.mkdir` | `path, chmod?, chown?` | creates parents; boundary = nearest pre-existing ancestor |
| `file.move` | `source, destination_path, on_missing?=stop` | the source is a consumed claim (`*Regular`) — success marks it Gone (destroyer stamp); archives any existing destination |
| `file.move_directory` | `source, destination_path, on_missing?=stop` | `move`'s kind-honest sibling: a `*Directory` claim, so verification judges the claimed kind at the starting line (2026-08-22) |
| `file.remove` | `target, on_missing?=stop, prune?, boundary?` | file or empty directory; consumed claim; archived |
| `file.remove_all` | `target, on_missing?=stop, prune?, boundary?` | subtree removal; consumed claim; archived |
| `file.unlink` | `target, on_missing?=stop, prune?, boundary?` | symlink removal; consumed claim; archived |
| `file.walk_tree` | `root, fn, include_gitignored?` | depth-first fold of `fn` over entries; batch receipt = nested stack (`CompensateWalkTree`) |
| `file.write_bytes` / `file.write_text` | `destination_path, content, chmod?, chown?` | inline content writes |
| `file.write_file` | `target_path, src, mode` | streams `src` to disk — the deploy path's transport write |

**Observers and path algebra** (fallible / pure; no receipts):

| Action | Contract |
|---|---|
| `file.discover` | `path, kind?=entry, after?` — interns the entry at a RUN-COMPUTED path as a discovery: lstat, the entry itself, no follow. Stop-only; `kind` is opt-in strictness; `after` is the pure ordering edge ([4-resource-management.md](4-resource-management.md) §5.7) |
| `file.resolve` | `path, kind?=entry, after?` — interns what the chain DESIGNATES (stat, never a link), confinement-judged: the kernel resolves names implicitly at open; this model resolves designation explicitly at a unit |
| `file.exists` / `file.is_dir` / `file.is_file` | existence and kind predicates |
| `file.find` / `file.glob` | pattern discovery (`include_gitignored?=false` honors `.gitignore` by default) |
| `file.observe` | a point-in-time `*Observation` of a resource — exists, size, mode, mtime, inode, device ([4-resource-management.md](4-resource-management.md) §6.1: a fact, never cataloged) |
| `file.read_bytes` / `file.read_text` | content reads off a `*Regular` |
| `file.join` / `file.name` / `file.parent` / `file.root` | path algebra within the confined root |

Write defaults are umask-relative (`chmod?={{ umask 0o666 }}`, directories `0o777`, copies `0o755`); ownership is a
Dockerfile-style `chown` string with an empty-string no-op.

## 5. Usage — both doors

```python
staged = plan.file.write_text(destination_path="motd", content="hello", chmod=0o644)
graph  = plan.assemble_definition([staged])
plan.run(graph, plan.spec())
```

```go
resolved, err := runtimeEnvironment.ProviderByType(reflect.TypeFor[file.Provider]())
fileProvider := resolved.(*file.Provider)
entry, receipt, err := fileProvider.WriteText(activationRecord, destinationPath, content, mode, "")
```

## 6. Status and roadmap

| Surface | State | Notes |
|---|---|---|
| Sealed taxonomy (step 23) | implemented | intent-declared kinds; cross-kind claims error at first claim |
| Unified mutation receipt + `CompensateFileMutation` | implemented | five kinds; boundary walks; recovery-digest tamper check |
| Write seam + conflict policy (step 49) | implemented | floor `replace`; encryption bypass is the recorded gap |
| `WalkTree` batch compensation | implemented | nested-stack composite |
| Directory content digest | open | directories are digest-less pending the Merkle deliverable (step 48 ruling) |

## 7. Open questions

1. **The encryption write-path seam bypass** (step 49's recorded gap) — route it through the seam or keep the
   pre-flight-only gate.
2. **Directory digests** — the Merkle-tree directory digest (step 48 ruling) would extend the recovery-digest tamper
   check to directory archives.
