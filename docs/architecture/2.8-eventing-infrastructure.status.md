# Eventing Infrastructure — Status

**Architecture document:** [2.8-eventing-infrastructure.md](2.8-eventing-infrastructure.md)

**State:** design — **proposed 2026-07-21**, under active refinement. Not yet approved for implementation. Implemented
by [phase-8 step 50](../plans/extract-starlark-from-op/phase-8/steps/50-eventstream-narration-integration.md).

## Completion

| Component | Status |
|---|---|
| Eventing infrastructure (bus + registered sinks) | Not implemented — proposed |
| Hook interface (producer seam) | Partial — `pkg/op/hooks.go` exists (unused in production, drifted from the Step-7 spec) |
| Narration | Exists — `status.Narrator` (status / stderr) + `result.Pipeline` (results / stdout); OTel tee not built |
| Operational events → OTel Traces / Spans | Not implemented |
| Diagnostics API (DEBUG / TRACE, span-correlated) | Not implemented — a new API |
| OpenTelemetry integration | Not implemented |

## Document Discrepancies

None.

## Outstanding / open questions

1. `status.Narrator` / `result.Pipeline` vs. `slog` — handlers, above, or parallel.
2. Canonical stream taxonomy + attribute names — (operational logs, narrations, results) vs. (narrations, operational
   events, diagnostics).
3. Eventing *is* OpenTelemetry vs. *wraps* it.
4. Metrics derivation — in-app OTel Metrics API vs. Collector connector.
5. Hook-interface reconciliation (`OnSubgraph*` vs. spec `OnPhase*`; the missing run-status-transition callback) —
   tracked in [step 50](../plans/extract-starlark-from-op/phase-8/steps/50-eventstream-narration-integration.md).
