# Status: PowerShell Provider

**Architecture document:** [3.5.9-powershell-provider.md](3.5.9-powershell-provider.md)
**State:** implemented; first design doc written 2026-07-22 (phase-8 step 39). Matrix greped, not asserted.

## Completion

| Component | Status |
|---|---|
| `powershell.exec` via `pwsh -NoLogo -NoProfile -Command`, context-bound, `*Result` emission shape | Landed |
| `pwsh` hard requirement (no `powershell.exe` fallback — step 28 capability ruling) | Landed |

## Document Discrepancies

None known — written 2026-07-22 against the tree.

## Test matrix (verified 2026-07-22)

| Method | Go | Starlark | Open gaps |
|---|---|---|---|
| `Exec` | `TestExec_{Success, EmptyCommand, Failure}` (`provider_test.go`, `pwsh`-gated via `requirePwsh`) | — (fixture re-dispositioned; the `pwsh`-gated Go units cover it) | closed — `TestExec_{Success, EmptyCommand, Failure}` (`provider_test.go`, `pwsh`-gated via `requirePwsh`); fixture re-dispositioned (no fixture-level `pwsh` skip in the `.star` harness — the gated Go units cover it) — see [step 52](../plans/extract-starlark-from-op/phase-8/steps/52-test-backfill-round-2.md) Disposition |

## Outstanding

Step 52 closed the `Exec` gap (3 `pwsh`-gated units; fixture re-dispositioned) — see its
[Disposition](../plans/extract-starlark-from-op/phase-8/steps/52-test-backfill-round-2.md). This had been the only
provider in the catalog with zero coverage of any kind.
