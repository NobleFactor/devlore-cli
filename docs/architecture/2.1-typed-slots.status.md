# Status: Typed Slots

**Architecture document:** [2.1-typed-slots.md](2.1-typed-slots.md)

**State:** rewritten 2026-07-22 (phase-8 step 51, slice 1) onto the landed `pkg/op` model. The pre-`op` body —
`SlotValue` (with the Proxy variant), the `Context.Data` property bag and engine slot-filling, the
`Action`/`FallibleAction`/`CompensableAction` interfaces with `Undo`, `RegisterReflectedActions` /
`ActionRegistry` / `coerceSlotValue`, and the framework content model — is replaced by the sealed `Binding` set,
the variable surface, announce-time classification with reflect-once adapters, and the conversion cascade.
Preserved from the phase-8 design work: **Plan-Time Conversion**, **The Converter**, and **Calling into Starlark**
— whose Invoker text was re-verified against the tree 2026-07-22 (`function.Resource` builds its own invoker at
construction; per-resource instances, never a process singleton). Every code claim verified at time of writing.

## Completion

| Component | Status | Completed |
|-----------|--------|-----------|
| Sealed `Binding` set (Immediate / Promise / Variable + field projection) | Complete | phase-8 (steps 8, 45) |
| Plan-time promise type-check | Complete | phase-8 (step 16) |
| Variable surface (parameters, source resolution, frames) | Complete | phase-8 |
| Conversion cascade (`op.Convert`, addressing contract, the Converter, the Invoker) | Complete | phase-8 (steps 15, 25) |
| Announce-time classification + reflect-once adapters | Complete | phase-8 (steps 27, 43) |
| Document rewrite onto the landed model | Complete | 2026-07-22 (step 51 slice 1) |

## Document Discrepancies

None known — the 2026-07-22 rewrite grounds every claim in the current tree.

## Outstanding Work

None for this document. Remaining step-51 slices are tracked in
[step 51](../plans/extract-starlark-from-op/phase-8/steps/51-documentation-debt.md).
