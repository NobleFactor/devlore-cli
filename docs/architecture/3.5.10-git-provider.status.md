# Status: Git Provider

**Architecture document:** [3.5.10-git-provider.md](3.5.10-git-provider.md)
**State:** implemented; first design doc written 2026-07-22 (phase-8 step 39). Matrix greped, not asserted.

## Completion

| Component | Status |
|---|---|
| `Clone` (full option surface, producer stamp) + `CompensateClone` | Landed |
| `Checkout` / `Pull` (fallible movements on an owned repo) | Landed |
| `Observe` (HEAD / ref / bare / dirty / remotes fact) | Landed |
| Resource content identity (`Digest` = clean-HEAD SHA-256, dirty-aware; `Etag` screen) | Landed (feeds step 48) |
| `Receipt.RestoreEncoded` + `exec.CommandContext` cancellation | Landed (step 44) |

## Document Discrepancies

None known — written 2026-07-22 against the tree.

## Test matrix (verified 2026-07-22)

| Method | Go | Starlark | Open gaps |
|---|---|---|---|
| `Clone` | `TestClone_{HookReceivesArgv, HookPropagatesError, DirectoryDerivedFromRepository, OptionsReachHook, ProducerStamp}` + helpers (`TestGuessDirName_*`, `TestBuildCloneArgs_*`) | `test_git.star` | — |
| `CompensateClone` | `TestCompensateClone_{RemovesDirectory, NoResource}`; plan: `TestGitCloneResumeThenFail_RollsBack_ViaPublicAPI` (both formats) | via the resume suite | — |
| `Checkout` | `TestCheckout_{BuildsArgv, PropagatesError, MovesRef}` | `test_git.star` | closed — `TestCheckout_{BuildsArgv, PropagatesError, MovesRef}` (`checkout_pull_observe_test.go`; argv via the dry-run narration seam) |
| `Pull` | `TestPull_{BuildsArgv, PropagatesError}` | `test_git.star` | closed — `TestPull_{BuildsArgv, PropagatesError}` (`checkout_pull_observe_test.go`; argv via the dry-run narration seam) |
| `Observe` | `TestObserve_{NonRepoReportsAbsent, WorktreeReportsState, ReportsHEAD, DirtyWorktree, BareRepo}` | — | closed — `TestObserve_{NonRepoReportsAbsent, WorktreeReportsState, ReportsHEAD, DirtyWorktree, BareRepo}` (`checkout_pull_observe_test.go`) |
| `Receipt` | `TestReceipt_RestoreEncoded_JSONandYAML` | via the resume suite | — |
| `Resource` identity | 14 tests: `TestResource_Digest_*` ×7 (clean/dirty/stability/format), `TestResource_Etag_*` ×5, addressing ×2 | — | — |

## Outstanding

Step 52 landed the `Checkout` / `Pull` / `Observe` units — see its
[Disposition](../plans/extract-starlark-from-op/phase-8/steps/52-test-backfill-round-2.md).
