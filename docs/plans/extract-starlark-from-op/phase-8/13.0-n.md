---
title: "13.0(n): Variable binding infrastructure"
parent: "docs/plans/extract-starlark-from-op/phase-8.md"
issue: https://github.com/NobleFactor/devlore-cli/issues/283
status: in-progress
created: 2026-05-14
updated: 2026-05-15
---

# Plan: 13.0(n) — Variable binding infrastructure

## Summary

Rebuild the slot value model around a PowerShell-style variable binding system.
Graphs, subgraphs, and flow-control containers expose a bubble-up parameter
surface; the caller supplies values at execute time via a single immutable
variable map; a new [op.VariableResolver] assembles that map from layered
sources (override / flag / env / config / default) with explicit precedence and
provenance tracking via [op.VariableSource]. Retire the ambient
`RuntimeEnvironment.Data` / `Properties` machinery (done). Test-first: the
starlark integration tests that model `writ adopt` are authored before the
infrastructure that satisfies them. `writ adopt` running through the new
binding model is the exit discriminator.

The binding-layer types live in **`pkg/op`** alongside the rest of the
operations layer; there is no separate `pkg/binding`. Tool-side state (flag /
config / override maps + program name) lives in a new **`pkg/application`**
package that every tool (lore, star, writ, devlore-test) projects into.

`plan.run` invocation materialization is **in scope** for 13.0(n): the starlark
bridge collects [starlarkbridge.Invocation] entries while a `.star` plan
executes; `plan.run` walks the reachable invocation set and materializes
[op.Node] children onto the bound graph's root. Without this step the
integration-test contract authored under Phase 1 cannot be satisfied.

## Exit criterion

13.0(n) is complete when both of the following hold:

1. **All tests pass**, including the seven starlark integration tests under
   `cmd/devlore-test/devloretest/data/test_writ_adopt*.star` that model the
   `writ adopt` workflow (mkdir → move → link) with bubbled parameter binding,
   subgraph composition, precedence and origin assertions, and failure-mode
   coverage (missing-required, type-mismatch).
2. **`writ adopt` runs through the new infrastructure** and produces the same
   behavior the user observes today — the three nil-activation call sites in
   `cmd/writ/writ/commands.go` (`:1406` Mkdir, `:1416` Move, `:1428` Link) are
   gone, replaced by graph construction + [op.VariableResolver] +
   [op.GraphExecutor.Run].

When both criteria hold, `make build` is clean, `make vet` is clean, and
`make test` is green across the full suite.

The 5 remaining nil-activation call sites in `cmd/writ/writ/migrate_cmd.go`
and `cmd/writ/writ/migrate/execute.go` and the file provider's defensive
nil-activation paths are **out of scope** for 13.0(n) — they land in a
follow-on PR at the smaller-PR cadence.

## Scope

### In scope

- **Slot model — sealed three-variant [op.SlotValue].** Variants:
  [op.ImmediateValue], [op.PromiseValue], [op.VariableValue]. `EnvironmentValue`
  and the `Properties` interface are gone (deleted; no migration window).
  `SlotValue.Resolve` signature is `(variables map[string]Variable, results map[string]any) any`.
- **Session ownership model.** [op.RuntimeEnvironment] is the single owner of
  the session's [op.Root] and is the only type that holds Close responsibility
  ([op.RuntimeEnvironment.Close], guarded by `sync.Once`). Co-users
  ([starlarkbridge.Runtime], [op.GraphExecutor], [op.Graph]) borrow the env by
  pointer.
- **[op.Plan] free-function planner.** Drives a planning session bounded by a
  spec and a caller-supplied closure: builds a planning env, constructs a
  fresh graph, binds via [op.Graph.Rebind], invokes the closure, then defers
  [op.Graph.Unbind] and `env.Close`. Returned graph is unbound until the next
  session-owner (the executor) rebinds it.
- **[op.GraphExecutor].** Constructor takes `(ctx, spec)`; the executor owns
  its own env and exposes `Close` + `Environment`. `Run(graph, variables)`
  rebinds the graph to the executor's env, dispatches, and defers
  `graph.Unbind` so the graph is portable to subsequent sessions.
- **Binding-layer types in [pkg/op](../../../../pkg/op).** [op.Variable]
  (Name / Value / Source), [op.VariableSource] (Kind / Name),
  [op.VariableSourceKind] (Unknown < Default < Config < Env < Flag < Override),
  [op.VariableResolver] (constructor takes `*application.Application`),
  [op.Parameter] (already present; `Default any`, `Optional bool`).
- **[pkg/application](../../../../pkg/application).** Tool-side handle with
  `Name`, `Flags`, `Config`, `Overrides` maps plus `DryRun()`.
  [application.NewApplication] projects the user-supplied flags from a cobra
  command's merged pflag set (only flags the user actually passed land in
  `Flags`).
- **[op.RuntimeEnvironment.RegisterParameter] cascade.** Provider-declared
  parameters resolve immediately against the Application's source maps
  (override → flag → env → config → default), populate `env.variables`, and
  return errors on type-mismatch or re-registration with a conflicting type.
  Reads via [op.RuntimeEnvironment.VariableByName].
- **`plan.variable(name, default_value=…)` builtin.** Authored on the
  [plan.Provider]; returns an [op.Variable] reference that the bridge
  translates to [op.VariableValue]{Name: name} at slot-fill time.
- **`plan.run` invocation materialization.** Walks the reachable invocation
  set collected by [starlarkbridge.Invocation_registry], reifies each
  [starlarkbridge.Invocation] as an [op.Node] on the bound graph's root,
  emits edges per the PromiseValue NodeRef and Resource producerID metadata
  the bridge stashed during dispatch.
- **[op.ExecutableUnit.Parameters] bubble-up.** Node returns the parameters
  whose slots carry [op.VariableValue]; subgraph returns the deduplicated
  union of its topological roots' parameter surfaces.
- **[op.VariableResolver.Resolve] real implementation.** Precedence walk;
  type assertion at resolve time; env-string parsing to the parameter's
  declared Go type; aggregated errors on missing-required / type-mismatch /
  default-type-mismatch.
- **Preflight integration.** `bindVariables` preflight pass on
  [op.GraphExecutor.Run] computes `graph.Parameters()`, calls
  `resolver.Resolve`, populates `env.variables`, joins errors into the D5
  envelope, and fails fast.
- **Starlark integration tests (the contract).** Seven `.star` plans under
  `cmd/devlore-test/devloretest/data/test_writ_adopt*.star` already authored;
  they parse today, and they become green as Phase 2 / 3 / 4 / 5 land.
- **Test-harness extensions.** Runner option surface, [t.set_*] /
  [t.expect_variable*] builtins on the test-context provider.
- **`writ adopt` migration.** The three nil-activation sites in `runAdopt`'s
  helper (`commands.go:1406/1416/1428`) rewire to graph construction +
  `VariableResolver.Resolve` + `executor.Run`.

### Out of scope (deferred to follow-on PR after 13.0(n) lands)

- **Writ migrate.** The 5 nil-activation call sites in `migrate_cmd.go` and
  `migrate/execute.go`. Same migration pattern, smaller PR.
- **File provider defensive-path cleanup.** Deletion of the defensive
  nil-activation paths in `pkg/op/provider/file/provider.go`. Must wait until
  writ migrate's sites land, so the defensive paths aren't load-bearing.

### Out of scope (other phase-8 / phase-9+ work)

- `plan.choose` / `plan.gather` / `plan.wait_until` successor design.
- `plan.load` / `plan.save`.
- Orphan detection at plan-end.
- `CanConvert` and plan-time type-check pass.
- `.star` caller migration to new flow-control surfaces.
- `file.Resource` taxonomic factoring.
- Reconciliation as a side effect on Active catalog entries (#156).
- Platform verification at preflight (#282).

## Background

### Today's slot model (pre-13.0(n))

Sealed three-variant `op.SlotValue` (the original shape, pre-rewrite):

| Variant | Resolved at | Source |
|---|---|---|
| `ImmediateValue{Value any}` | plan time | call-site literal or converted Go value |
| `PromiseValue{NodeRef, Slot}` | execute time | `results[NodeRef]` (sibling output) |
| `EnvironmentValue{Key string}` | execute time | `Properties.Property(Key)` reading `RuntimeEnvironment.Data` |

The `Properties` interface and `RuntimeEnvironment.Data map[string]any`
provided an ambient property bag populated at process startup from CLI flags,
env vars, and user config. `plan.env("target_root")` created an
`EnvironmentValue` at plan time; resolution deferred to execute time via
`Properties.Property(key)`.

### Why the model needed to change

Two structural problems blocked the bubble-up composition model that
flow-control combinators require:

1. **No declared parameter surface.** Today, "overrides" passed to
   `Execute(graph, overrides)` was an opaque `map[string]any`. The executor
   had no declared schema to check against. A missing required parameter
   surfaced as a nil dereference or a type-assertion panic deep inside a
   provider method, mid-execution, after side effects had partially
   committed.
2. **Ambient state was not composable.** `RuntimeEnvironment.Data` was
   process-wide; it could not be re-scoped per subgraph. A subgraph composed
   inside a parent graph could not supply a different value for its child's
   `EnvironmentValue` lookup — the lookup hit the shared `Data` bag.

Bubble-up requires both: a typed parameter surface declared at plan time, and
a variable-resolution layer that scopes per-call rather than per-process and
tracks where each value came from.

### The unified model (what landed)

Replace `EnvironmentValue` with `VariableValue`. The slot's lookup target moves
from "process-wide property bag" to "this session's variable map." The tool
main becomes the assembler of the variable map — it parses CLI flags, env,
and config and stamps them into an [application.Application]; the
[op.VariableResolver] reads from `app.Flags` / `app.Config` / `app.Overrides`
plus the process environment (prefix derived from `app.Name`) and produces a
typed `map[string]op.Variable` with provenance. The executor sees only the
resolved map. The `Properties` abstraction is gone from the slot model
entirely. Subgraph composition becomes recursive bubble-up: a subgraph's
[op.VariableValue] slots are bound by its parent graph's slot fill (which may
be `ImmediateValue`, `PromiseValue`, or another `VariableValue`); the bubble
continues all the way up to the root graph's parameter surface.

## Design

### D1 — Sealed three-variant `SlotValue` (landed)

```go
type SlotValue interface {
    isSlotValue()
    Resolve(variables map[string]Variable, results map[string]any) any
}

type ImmediateValue struct{ Value any }
type PromiseValue   struct{ NodeRef, Slot string }
type VariableValue  struct{ Name string }
```

`ImmediateValue` and `PromiseValue` ignore the variables map;
`VariableValue.Resolve` returns `variables[v.Name].Value` (nil-safe on map
absence).

`VariableValue.Resolve` is reachable only after preflight has confirmed every
required `VariableValue` has a matching entry in the variable map. If
`variables[Name]` is missing at Resolve time, today's behavior returns nil —
Phase 4's preflight ensures the case is unreachable.

### D2 — Parameter (landed)

`op.Parameter` (in [`pkg/op/action.go`](../../../../pkg/op/action.go)):

```go
type Parameter struct {
    Name     string
    Type     reflect.Type
    Optional bool
    Variadic bool
    Kwargs   bool
    Default  any
}
```

`Optional` discriminates required-vs-optional (not a sentinel on `Default`).
`Default` is the parameter's declared default value, already at
`Parameter.Type` (parsed once at announce time by `parseDefaultExpression`).
Variadic and Kwargs cannot carry a default — the grammar rejects
`*parts?` / `**kwargs?=foo`.

Validation:

- **At resolve time.** [op.assignToType] asserts every non-env source value
  against the declared `Type` via `reflect.TypeOf(raw).AssignableTo(declared)`.
  Mismatches return an error before the variable is stored. Nilability is
  handled per `reflect.Kind` (pointer / interface / slice / map / chan / func
  kinds accept nil; everything else does not).
- **At slot-fill time.** Defense-in-depth — preflight catches issues first,
  but a slot-fill assertion mismatch is a `VariableResolver` bug and fails
  immediately rather than panicking inside a provider method.

### D3 — `ExecutableUnit.Parameters()` bubble-up

```go
type ExecutableUnit interface {
    // ... existing methods ...
    Parameters() []op.Parameter
}
```

- **Node.** `Parameters()` returns the method's parameters whose slot's
  `SlotValue` is `VariableValue`. The `Parameter.Type` is the method's
  declared parameter type; the `Parameter.Default` is propagated from the
  declared default.
- **Subgraph.** `Parameters()` walks the topological-root children (those
  with no incoming edges within the subgraph), unions their `Parameters()`
  outputs, and deduplicates by name. Multi-root collision (two roots declare
  the same parameter name with incompatible types) is a plan-time error.

Phase 1 lands a stub on `*Subgraph` (parameters left nil); Phase 3 lands the
topological-root union.

### D4 — `Variable`, `VariableSource`, `VariableSourceKind` (landed)

In [`pkg/op/variable.go`](../../../../pkg/op/variable.go):

```go
// Ascending precedence; higher beats lower.
type VariableSourceKind int

const (
    VariableSourceKindUnknown VariableSourceKind = iota
    VariableSourceKindDefault
    VariableSourceKindConfig
    VariableSourceKindEnv
    VariableSourceKindFlag
    VariableSourceKindOverride
)

type VariableSource struct {
    Kind VariableSourceKind
    Name string  // lookup key that matched
}

type Variable struct {
    Name   string
    Value  any
    Source VariableSource
}
```

All three carry `String()` for diagnostics. `VariableSource.String()` formats
as `<kind>:<name>` (or `"unknown"` for the zero value). `Variable.String()`
formats as `<name> = <value> [<source>]`.

`Name` convention per `Kind`:

| Kind | `Name` = | Example `String()` |
|---|---|---|
| `Unknown` | (empty) | `unknown` |
| `Default` | parameter name | `default:target_root` |
| `Config` | config-map key | `config:target_root` |
| `Env` | literal env var with program prefix | `env:WRIT_TARGET_ROOT` |
| `Flag` | normalized snake-case flag name | `flag:target_root` |
| `Override` | override-map key | `override:layer` |

### D5 — `VariableResolver` (skeleton landed; cascade in Phase 2)

In [`pkg/op/variable_resolver.go`](../../../../pkg/op/variable_resolver.go):

```go
type VariableResolver struct {
    app      *application.Application
    resolved map[string]Variable
}

func NewVariableResolver(app *application.Application) *VariableResolver

func (r *VariableResolver) EnvPrefix() string
func (r *VariableResolver) Get(name string) (Variable, bool)
func (r *VariableResolver) Variables() map[string]Variable
func (r *VariableResolver) Resolve(parameters []Parameter) []error
```

The resolver captures a pointer to the [application.Application] at
construction. At resolve time it reads `app.Flags`, `app.Config`,
`app.Overrides` directly (not snapshots — late binding lets per-command flag
adjustments take effect). The env-var prefix is derived from
`strings.ToUpper(app.Name) + "_"`.

**Resolution order per parameter** (descending precedence; first hit wins):

1. `app.Overrides[name]` → `VariableSourceKindOverride`.
2. `app.Flags[name]` → `VariableSourceKindFlag`. `app.Flags` keys are
   snake-case (normalized at [application.NewApplication] time —
   `dry-run` → `dry_run`, `target-root` → `target_root`); the resolver
   matches the parameter name verbatim. Framework code that reads specific
   flags (e.g., [application.Application.DryRun] for `dry-run`) likewise
   reads the snake-case key (`Flags["dry_run"]`).
3. `os.Getenv(EnvPrefix() + camelToSnake(name))` → `VariableSourceKindEnv`.
   `camelToSnake` uses Go's idiomatic greedy-acronym rule: consecutive
   uppercase letters form a single acronym; an acronym ends when a
   lowercase letter follows. Examples: `targetRoot` → `TARGET_ROOT`,
   `sourceURL` → `SOURCE_URL`, `URLPath` → `URL_PATH`, `UserID` →
   `USER_ID`. Mixed acronyms like `IPv4Address` produce slightly imperfect
   output (`I_PV4_ADDRESS`) and are tolerated; the rule serves the 99%
   case at zero maintenance cost.
4. `app.Config[name]` → `VariableSourceKindConfig`.
5. `Parameter.Default` (when `Optional == true` and `Default != nil`) →
   `VariableSourceKindDefault`.

After picking the value, the resolver:

- For env-sourced values, wraps the raw string in a private `envValue` type
  that implements [op.SourceConverter]. The [op.Convert] cascade picks the
  wrapper up at step 5 (source-side opt-in); `envValue.ConvertTo` handles
  primitives (bool / int* / uint* / float* / string via the existing
  `Kind` switch shared with `parseDefaultExpression`), stdlib special-cases
  (`time.Duration` via `time.ParseDuration`, `os.FileMode` via
  `strconv.ParseUint(base 0)`), then probes the target for
  [encoding.TextUnmarshaler], then falls through to `json.Unmarshal` for
  arbitrary Go types. `envValue.CanConvertTo` returns `false` for targets
  implementing [op.Resource] so [op.Convert] step 7 (registered Resource
  construction) wins the routing — env strings for Resource-typed
  parameters feed the same constructor path that tag URIs use today.
- For non-env sources, type-asserts the picked value against
  `parameter.Type` via [op.assignToType]. Mismatch → error.
- Records `resolved[name] = Variable{Name, Value, Source}`.

[op.Convert] itself is not modified. Adding a string-source step there would
silently parse stray starlark string literals reaching primitive-typed
slots — the codebase already documented this layering decision at
[`pkg/op/parameter.go:236`](../../../../pkg/op/parameter.go). `envValue` is
a distinct type from `string`; starlark slot fills produce plain `string`
and continue to error correctly when a primitive target is required.

For each required parameter (`Optional == false`) that found nothing →
aggregated error naming the parameter, target type, and the literal lookup
keys the resolver tried.

`Get` and `Variables` panic if called before `Resolve` — that's a programmer
error. After `Resolve` the resolver is fully read-only.

Phase 1 skeleton: `Resolve` initializes the resolved map to empty and
returns no errors. Phase 2 lands the cascade walk.

### D6 — `RegisterParameter` cascade on the runtime environment (landed)

Providers that need a binding-layer parameter immediately (at construct time,
not at preflight) call `env.RegisterParameter(p)`. The method:

1. Records the parameter in `env.declaredParameters`. Re-registration with
   the same name + same `Type` is idempotent; same name + different `Type`
   returns a type-mismatch error.
2. Walks the cascade: `Overrides → Flags → Env (deferred) → Config → Default`
   on `env.Application` directly (the resolver is shared, but
   `RegisterParameter` is a synchronous helper that doesn't require a full
   `Resolve` pass to inspect a single name).
3. Type-asserts the picked value via [op.assignToType]. Mismatch → error
   without overwriting state.
4. Stores the result in `env.variables[name]` so subsequent
   [op.RuntimeEnvironment.VariableByName] calls return it.

This shape lets providers participate in variable binding **before** the
graph's preflight pass — the four star providers (`config`, `setup`, `goast`,
`commands`) currently rely on this path for tool-side parameters.

The env source step in `RegisterParameter` is a deliberate skip until the
resolver's full env-string-parsing logic lands in Phase 2.
[op.VariableResolver.Resolve] is the canonical path for graph-time parameter
resolution; `RegisterParameter` is the construct-time fast path.

### D7 — Session ownership, planning, execution (landed)

```go
func NewRuntimeEnvironment(ctx context.Context, spec *RuntimeEnvironmentSpec) *RuntimeEnvironment
func (re *RuntimeEnvironment) Close() error           // sync.Once-guarded

func NewGraph() *Graph                                // unbound
func (g *Graph) Rebind(env *RuntimeEnvironment)
func (g *Graph) Unbind()
func (g *Graph) RuntimeEnvironment() *RuntimeEnvironment

func Plan(ctx context.Context, spec *RuntimeEnvironmentSpec,
          fn func(*Graph) error) (*Graph, error)

func NewGraphExecutor(ctx context.Context, spec *RuntimeEnvironmentSpec) *GraphExecutor
func (e *GraphExecutor) Close() error
func (e *GraphExecutor) Environment() *RuntimeEnvironment
func (e *GraphExecutor) Run(graph *Graph, variables map[string]Variable) (any, error)
```

`Plan` and `GraphExecutor` are the two session-owners. Each builds an env
from a spec, binds the graph during the active session, and unbinds at the
end so the graph is portable to the next session. The executor's `Run` accepts
the resolved variable map (Phase 4 will populate it from the executor's own
resolver pass before dispatch; today's callers pass `nil`).

### D8 — `plan.run` invocation materialization (Phase 5)

The bridge already collects [starlarkbridge.Invocation] entries during a
`.star` plan execution (`pkg/op/starlarkbridge/invocation_registry.go`). Today,
the invocation set is the bridge's private state and never produces
[op.Node] children on the bound graph's root.

`plan.run` is the bridge-to-graph reification step:

1. **Reachability.** Walk the invocation set rooted at the explicit
   "run target" (the invocation passed as `plan.run`'s positional argument,
   or implicitly the last invocation if no argument is supplied) following
   PromiseValue NodeRef edges and Resource producerID edges.
2. **Node materialization.** For each reachable invocation, build an
   [op.Node] using the cached method + slot map from the [Invocation]
   struct. Insert the node onto the bound graph's root via
   `graph.Root.AddChild`. The node's slots reference variables as
   [op.VariableValue] (already wired by the bridge at slot-fill time when
   the user authored `plan.variable("…")`).
3. **Edge materialization.** For each `PromiseValue` slot value the
   bridge stashed during dispatch, emit a graph edge (producer-node-id →
   this-node) and store the slot's `NodeRef` so the executor's promise
   resolution finds it at run time. Same for Resource producerID edges
   (extracted via `op.ExtractResource`).
4. **Invocation registry freeze.** After `plan.run` returns, the registry
   is frozen — further bridge calls in the same session that would emit
   invocations are an error (Phase 8 invariant I3 already declared).

`plan.run` is a builtin on the [plan.Provider] reached via the bridge.
Mechanically, the implementation lives in `pkg/op/provider/plan/provider.go`
and `pkg/op/starlarkbridge/*.go`; the bridge holds the invocation set, but
`plan.run` is reified as a provider method so the .star surface stays
consistent.

### D9 — Override routing at `Execute()` (landed signature; Phase 4 wiring)

`GraphExecutor.Run` signature is `Run(graph, variables map[string]Variable)`.
The executor stashes the variable map on its receiver; dispatch reaches a
slot with `VariableValue{Name: X}` and resolves via `variables[X].Value`. No
`Properties` interface lookup remains anywhere in the executor.

Subgraph composition: when a parent graph's slot fill for a child subgraph
is `VariableValue{Name: Y}`, the variable map at the root carries `Y`. The
subgraph's matching slot resolves to the same value. Bubble-up at runtime
mirrors the bubble-up declared at plan time.

Phase 4 wires the preflight pass that populates `variables` from the
executor's own [op.VariableResolver] before dispatch. Today's callers (writ
adopt, tests) pass `nil` or supply a pre-resolved map directly.

### D10 — Preflight integration (Phase 4)

New preflight pass `bindVariables` added to `GraphExecutor.Run`, joining
`ResolvePending` (landed by 13.0(k) k.15) and future orphan / type-check /
platform passes. The executor doesn't construct the resolver itself — the
caller constructs the env from a spec; the executor's preflight builds the
resolver from `env.Application`, calls `Resolve(graph.Parameters())`, and
populates `env.variables`. Preflight at this layer:

1. Compute `graph.Parameters()` — the root's bubble-up surface.
2. For each parameter:
   - Check `variables[name]` exists, OR parameter has a default.
   - If parameter has a default, type-assert `Default` against `Type`.
   - If variable is present, type-assert `variables[name].Value` against
     `Type`.
3. Collect errors; aggregate into the D5 envelope.
4. Fail fast on non-empty: graph transitions to `StateFailed`; executor
   returns `errors.Join(errs...)`.

Skipped in dry-run, matching `ResolvePending`'s convention
([`pkg/op/executor.go:79`](../../../../pkg/op/executor.go)).

## Implementation phases

Each phase is one or more commits on
`refactor/extract-starlark-from-op.phase-8`. Build stays green at every
commit boundary — Phase 1's failing-tests are intentional and the harness
reports them as expected failures via [t.expect_error].

### Phase 1 — Foundations (DONE)

Done across four commits. After Phase 1:

- The sealed-three slot model is in place ([op.VariableValue] replaces
  `EnvironmentValue`); `Properties` deleted; `RuntimeEnvironment.Data` and
  `Property` deleted.
- `pkg/op` binding-layer types are in place ([op.Variable],
  [op.VariableSource], [op.VariableSourceKind], [op.VariableResolver]
  skeleton).
- `pkg/application` is in place with `NewApplication` + `DryRun`.
- `pkg/assert` adds `Nil` / `NotNil` helpers for invariant checks.
- [op.Plan] + [op.GraphExecutor] own session lifetimes; [op.Graph.Rebind] /
  [op.Graph.Unbind] are the binding surface; `RuntimeEnvironment.Close` is
  `sync.Once`-guarded.
- `starlarkbridge.NewRuntime(env)` borrows the env from its caller; no
  internal env construction.
- `cmd/lore`, `cmd/star`, `cmd/devlore-test` migrated to the
  one-env-per-session pattern.
- `RegisterParameter` cascade is in place; the four star providers
  (`config`, `setup`, `goast`, `commands`) consume it.
- `plan.variable(name, default_value=…)` builtin is in place on the
  [plan.Provider].
- Seven `.star` integration tests authored under
  `cmd/devlore-test/devloretest/data/test_writ_adopt*.star` — they parse and
  the harness invokes them; preflight does not yet validate so they execute
  against an empty graph.

### Phase 2 — `VariableResolver.Resolve` real implementation

- Implement source precedence cascade per D5
  ([`pkg/op/variable_resolver.go:108`](../../../../pkg/op/variable_resolver.go)).
- Env-string parsing registry (`string → bool / int / os.FileMode /
  time.Duration / string`); extensibility hook for additional target types.
- `camelToSnake` (or `kebabToSnake` if flag-name → env-var rules want it)
  helper for env-key derivation. Pick rules consistent with Go's idiomatic
  patterns or document a custom rule.
- Origin tagging per source.
- Aggregated errors: missing-required, type-mismatch, default-type-mismatch.
- **Tests** under `pkg/op/variable_resolver_test.go`: source precedence,
  origin recording per kind, missing-required, type-mismatch,
  default-type-mismatch, `camelToSnake` edge cases, `Get` panic on
  pre-`Resolve`.

### Phase 3 — `ExecutableUnit.Parameters()` bubble-up

- Implement `*Subgraph.Parameters()` topological-root union per D3
  ([`pkg/op/subgraph.go:69`](../../../../pkg/op/subgraph.go)).
- Multi-root collision detection (two roots declare the same parameter name
  with incompatible types) as a plan-time error.
- **Tests** under `pkg/op/subgraph_test.go`: single-root passthrough,
  multi-root union, multi-root collision.

### Phase 4 — Preflight validation

- Add `bindVariables` preflight pass to `GraphExecutor.Run` before
  `ResolvePending`.
- Construct the env's [op.VariableResolver] (already wired on
  `env.variableResolver`), call `Resolve(graph.Parameters())`, fold errors
  into the D5 envelope.
- Populate `env.variables` with the resolved map; threading from
  `env.variables` to slot-fill resolution.
- Skip in dry-run.
- **Tests:** Phase 1's `.star` integration tests now start passing for the
  cases that don't require `plan.run` materialization (variable-presence
  assertions, missing-required, type-mismatch — anything that resolves
  pre-dispatch). Full integration green requires Phase 5.

### Phase 5 — `plan.run` invocation materialization

- Implement per D8.
- Reachability walk + node materialization + edge materialization +
  invocation-registry freeze.
- The seven `.star` integration tests start passing end-to-end (the bridge
  now produces real graph nodes; the executor's preflight + dispatch run
  against them).
- **Tests:** unit coverage in `pkg/op/starlarkbridge/` for reachability
  walks, edge emission, registry-freeze invariant, error on post-freeze
  emission.

### Phase 6 — `writ adopt` migration

The three call sites in `runAdopt`'s helper `adoptFile`
([`cmd/writ/writ/commands.go`](../../../../cmd/writ/writ/commands.go)):

| Line | Method | Slots |
|---|---|---|
| 1406 | `fp.Mkdir(nil, destDir, 0o755, "")` | path, mode |
| 1416 | `fp.Move(nil, &file.Resource{SourcePath: …}, destPath)` | source, destination |
| 1428 | `fp.Link(nil, &file.Resource{SourcePath: …}, filePath)` | source, target |

**Each writ adopt CLI argument is realized as an [op.VariableValue] slot.**
No user-controlled input lives in an [op.ImmediateValue]. The cobra-parsed
flags land in `Application.Flags` under their snake-cased names; the
executor's preflight pass resolves them through the cascade and fills
`env.variables`; dispatch populates the slots at run time.

The package layout follows the existing `migrate_cmd.go` + `migrate/`
precedent (six other commands in `cmd/writ/writ/` already separate cobra
glue from logic via the same shape):

```
cmd/writ/writ/
  adopt_cmd.go     ← NEW. newAdoptCmd, runAdopt, AdoptConfig
                     (extracted from commands.go).
  adopt/           ← NEW subpackage.
    adopt.go       ← AdoptFiles, AdoptItem
                     (per-file iteration scaffolding).
    plan.go        ← BuildGraph(env, src, dest, mode) — the op.Plan closure
                     factored out for isolated testability. Each adopt
                     argument enters as op.VariableValue{Name: "..."}.
    execute.go     ← Run(executor, graph) wraps executor.Run with
                     adopt-specific error mapping.
```

`adoptFile` (currently `commands.go:1400+`) becomes:

```go
graph, err := op.Plan(ctx, spec, func(g *op.Graph) error {
    return adopt.BuildGraph(g, …)
})
if err != nil { return err }
return adopt.Run(executor, graph)
```

`adopt.BuildGraph` constructs three nodes (mkdir → move → link) on `g.Root`
with VariableValue slots for `dest_dir`, `source_path`, `dest_path`, and
ImmediateValue only for non-user constants (file modes the tool sets
itself, hard-coded backup suffixes, etc.).

`executor.Run(graph, nil)` — the executor's preflight resolves variables
from `env.Application` (built from the cobra command's flags) and
populates `env.variables` before dispatch.

`commands.go` shrinks by ~500 lines (the full adopt surface moves out).
The full `cmd/writ/writ/command/` subpackage normalization for the
remaining cobra commands (deploy, decommission, upgrade, reconcile) is a
separate layout-only PR after 13.0(n) closes.

### Phase 7 — Deferred (writ migrate + file provider cleanup)

NOT part of 13.0(n). Tracked here as the immediate follow-on so the sequence
is documented.

- Migrate the 5 writ migrate sites (`migrate_cmd.go:292/302/310/320`,
  `migrate/execute.go:81`) to the same pattern as Phase 6.
- Grep `pkg/op/provider/file/provider.go` for nil-activation defensive
  branches. Delete them.

This phase lands in a separate, smaller PR after 13.0(n) closes.

## Test strategy

### Starlark-first integration tests

Phase 1 authored seven `.star` tests before the supporting infrastructure
existed. The tests parse and the harness runs them end-to-end; they execute
against an empty graph today (Phase 1) and against a real materialized graph
once Phase 5 lands. From Phase 6 onward, the tests become regression guards
for the writ adopt migration.

### Go unit coverage

Each new Go type / function in `pkg/op` (binding layer) and the modified
slot machinery gets unit tests. These cover edges the integration test
cannot reach economically — `camelToSnake` edge cases, panic-on-pre-Resolve,
multi-root collision detection, default-type validation against parameter
type, env-string parsing per target type.

### Writ-side coverage

Phase 6 adds a writ-side test for the graph-construction helper. The
behavioral test for `writ adopt` continues to pass — same observable
filesystem behavior, different internal path.

### Coverage matrix

| Scenario | `.star` integration | Go unit | Writ-side |
|---|---|---|---|
| Single-graph mkdir → move → link | ✓ |  | ✓ |
| Missing required parameter | ✓ | ✓ |  |
| Type mismatch | ✓ | ✓ |  |
| Origin namespace assertion | ✓ | ✓ |  |
| Origin full string assertion | ✓ | ✓ |  |
| Source precedence | ✓ | ✓ |  |
| Default-type mismatch at preflight |  | ✓ |  |
| Subgraph bubble-up | ✓ | ✓ |  |
| Subgraph multi-root collision |  | ✓ |  |
| `Get` panic on pre-`Resolve` |  | ✓ |  |
| Dry-run skips parameter preflight | ✓ | ✓ |  |
| `writ adopt` real-path execution |  |  | ✓ |
| `Variable.String()` format |  | ✓ |  |
| `VariableSource.String()` format |  | ✓ |  |
| `plan.run` reachability walk |  | ✓ |  |
| Invocation-registry freeze post-`plan.run` |  | ✓ |  |

## Locked design decisions

Decisions taken during plan finalization (2026-05-15) and locked in the
sections above:

1. **`camelToSnake` rule for env keys.** Greedy acronym (Go-idiomatic):
   consecutive uppercase letters form a single acronym; an acronym ends
   when a lowercase letter follows. `URLPath → URL_PATH`, `targetRoot →
   TARGET_ROOT`. Mixed acronyms (`IPv4Address → I_PV4_ADDRESS`) are
   tolerated. Documented under D5 step 3.
2. **`Application.Flags` key form.** Snake-case, normalized at
   [application.NewApplication] time. `dry-run → dry_run`,
   `target-root → target_root`. The resolver matches the parameter name
   verbatim. Framework code reading specific flags
   ([application.Application.DryRun]) likewise reads the snake-case key.
   Documented under D5 step 2.
3. **Env-string parsing strategy.** Private `envValue` type implements
   [op.SourceConverter]; the resolver wraps raw env strings before calling
   [op.Convert]. The cascade picks up `envValue` at step 5; the wrapper
   handles primitives via the shared `Kind` switch with
   `parseDefaultExpression`, then stdlib special-cases (`time.Duration`,
   `os.FileMode`), then [encoding.TextUnmarshaler], then `json.Unmarshal`
   as the universal fallback. `envValue.CanConvertTo` returns false for
   [op.Resource] targets so step 7 (registered Resource construction)
   wins routing. [op.Convert] itself is not modified — the codebase's
   prior layering decision at [`pkg/op/parameter.go:236`](../../../../pkg/op/parameter.go)
   stands. Documented under D5.
4. **Phase 6 layout.** `cmd/writ/writ/adopt_cmd.go` + `cmd/writ/writ/adopt/`
   subpackage (with `adopt.go`, `plan.go`, `execute.go`) matching the
   existing `migrate_cmd.go` + `migrate/` precedent (and six other
   subpackages: `identity/`, `reconcile/`, `segment/`, `snapshot/`,
   `tree/`). Documented under Phase 6.
5. **Each writ adopt CLI argument is an [op.VariableValue] slot.** No
   user-controlled input lives in an [op.ImmediateValue]. Documented
   under Phase 6.

## Deferred to phase

These decisions remain open and will be locked when the relevant phase
opens, not now:

- **`plan.run` surface.** The starlark builtin shape (argument form,
  implicit-vs-explicit invocation, integration with flow-control
  combinators) — Phase 5 design decision. Writ adopt is Go-side and does
  not exercise `plan.run`; this question is scoped to .star authors and
  to the seven integration tests in `test_writ_adopt*.star`.

## Files affected

### Modified (Phase 1, landed)

- [`pkg/op/slot.go`](../../../../pkg/op/slot.go) — sealed-three update;
  signature change; `EnvironmentValue` and `Properties` deleted.
- [`pkg/op/graph.go`](../../../../pkg/op/graph.go) — `NewGraph` argless;
  `Rebind` / `Unbind`; `RuntimeEnvironment()` accessor.
- [`pkg/op/executor.go`](../../../../pkg/op/executor.go) — `NewGraphExecutor`
  takes `(ctx, spec)`; `Close`; `Environment`; `Run(graph, variables)`.
- [`pkg/op/runtime_environment.go`](../../../../pkg/op/runtime_environment.go)
  — session-scoped owner doc; `Close` sync.Once; `RegisterParameter`;
  `VariableByName`; `declaredParameters` / `variables` / `variableResolver`
  fields; `Data` / `Property` deleted; `ProgramName` deleted (Application.Name
  is now the single source of truth).
- [`pkg/op/runtime_environment_spec.go`](../../../../pkg/op/runtime_environment_spec.go)
  — `assert.NotNil("spec.Application", …)`; `WithApplication`.
- [`pkg/op/activation_record.go`](../../../../pkg/op/activation_record.go) —
  `Graph` field added; executor stamps `node.graph`.
- [`pkg/op/provider/plan/provider.go`](../../../../pkg/op/provider/plan/provider.go)
  — `Variable(name, defaultValue any) *op.Variable`.
- [`pkg/op/provider/flow/provider.go`](../../../../pkg/op/provider/flow/provider.go)
  — `Graph` field removed; reads from `ActivationRecord.Graph`.
- [`pkg/op/starlarkbridge/runtime.go`](../../../../pkg/op/starlarkbridge/runtime.go)
  — `NewRuntime(env)` (single typed arg); modules + registry pulled from
  `env.Registry`.
- [`pkg/op/starlarkbridge/task_builder.go`](../../../../pkg/op/starlarkbridge/task_builder.go)
  — bridge plumbs the borrowed env through invocation registration.
- [`cmd/lore/lore/builder.go`](../../../../cmd/lore/lore/builder.go) —
  prepareScriptEnv builds spec → env explicitly; passes env to bridge.
- [`cmd/star/star/application.go`](../../../../cmd/star/star/application.go)
  — Application gains `env *op.RuntimeEnvironment`; `Close` delegates to
  `env.Close`.
- [`cmd/star/main.go`](../../../../cmd/star/main.go) — binds flags before
  `star.NewApplication`; `PersistentPreRunE` calls `runtime.Refresh(cmd)`.
- [`cmd/star/provider/{config,setup,goast,commands}/provider.go`](../../../../cmd/star/provider)
  — migrated to `RegisterParameter` + `VariableByName`.
- [`cmd/devlore-test/devloretest/runner.go`](../../../../cmd/devlore-test/devloretest/runner.go)
  — `Start` uses `op.Plan(ctx, spec, fn)`; closure pulls env via
  `g.RuntimeEnvironment()` and constructs the bridge from it.
- [`star/extensions/com.noblefactor.devlore.Actions/templates/receiver_type.gen_test.go.template`](../../../../star/extensions/com.noblefactor.devlore.Actions/templates/receiver_type.gen_test.go.template)
  — env shape updated for `Application` + `Flags["dry-run"]`.
- All `*/gen/receiver_type.gen_test.go` files regenerated.

### Modified (Phases 2–6)

- `pkg/op/variable_resolver.go` — real `Resolve` cascade.
- `pkg/op/subgraph.go` — `Parameters()` topological-root union.
- `pkg/op/executor.go` — `bindVariables` preflight pass.
- `pkg/op/starlarkbridge/invocation_registry.go` — reachability walk hook.
- `pkg/op/starlarkbridge/promise.go` — promise → graph-edge materialization.
- `pkg/op/provider/plan/provider.go` — `plan.run` builtin.
- `cmd/writ/writ/commands.go` — `runAdopt`'s helper rewrites (Phase 6).
- `cmd/devlore-test/devloretest/test_context.go` — `t.set_*` /
  `t.expect_variable*` builtins (mostly landed; finish in Phase 2).

### New (Phase 1, landed)

- [`pkg/op/variable.go`](../../../../pkg/op/variable.go) — `Variable`,
  `VariableSource`, `VariableSourceKind`.
- [`pkg/op/variable_resolver.go`](../../../../pkg/op/variable_resolver.go) —
  `VariableResolver` skeleton.
- [`pkg/op/planner.go`](../../../../pkg/op/planner.go) — `op.Plan` free
  function.
- [`pkg/op/starlarkbridge/var.go`](../../../../pkg/op/starlarkbridge/var.go)
  + [`var_test.go`](../../../../pkg/op/starlarkbridge/var_test.go) — bridge
  plumbing for `plan.variable`.
- [`pkg/application/application.go`](../../../../pkg/application/application.go)
  — `Application` + `NewApplication` + `DryRun`.
- [`pkg/assert/assert.go`](../../../../pkg/assert/assert.go) — `Nil` /
  `NotNil` helpers.
- [`cmd/devlore-test/devloretest/data/test_writ_adopt*.star`](../../../../cmd/devlore-test/devloretest/data)
  (seven plans).
- [`cmd/devlore-test/devloretest/helpers.go`](../../../../cmd/devlore-test/devloretest/helpers.go).

### New (Phases 2–6)

- `pkg/op/variable_resolver_test.go` — resolver cascade unit coverage.
- `pkg/op/subgraph_test.go` additions — multi-root collision.
- Writ-side adopt helper file (`cmd/writ/writ/adopt_helper.go` or similar).

### Deleted (Phase 1, landed)

- `op.EnvironmentValue` (slot variant in `pkg/op/slot.go`).
- `op.Properties` (interface).
- `op.RuntimeEnvironment.Data` (field).
- `op.RuntimeEnvironment.Property` (method).
- `pkg/binding/*` — the prior session's skeleton; types moved to `pkg/op`.

### Deferred to follow-on PR (Phase 7 — writ migrate)

- `cmd/writ/writ/migrate_cmd.go` — 4 nil-activation sites.
- `cmd/writ/writ/migrate/execute.go` — 1 nil-activation site.
- `pkg/op/provider/file/provider.go` — defensive nil-activation paths.

## Related plans and docs

- Parent: [phase-8.md](../phase-8.md)
- Slot model history: [phase-7.md](../phase-7.md) §"Fork F — DataRef under
  the sealed SlotValue"
- Architecture (data flow & slot resolution):
  [docs/architecture/2.1-typed-slots.md](../../../architecture/2.1-typed-slots.md)
- Catalog state machine precedent (Pending/Active/Gone, catalog-owned
  transitions): [phase-8.md](../phase-8.md) §13.0(k) k.13
- Preflight architecture (pass aggregation, D5 envelope): parent plan
  [extract-starlark-from-op.md](../../extract-starlark-from-op.md) Step 16 row

## Subsequent work

After 13.0(n) closes:

- **Follow-on PR (immediate next):** Phase 7 above. Migrate the 5 remaining
  writ migrate nil-activation sites; delete the file provider's defensive
  nil-activation paths. Smaller PR, normal cadence.
- **Step 13:** `plan.choose` successor design — branches as subgraphs with
  their own parameter surfaces and conditional edges. Bubble-up
  infrastructure makes the redesign tractable.
- **Step 14:** `plan.gather` successor design — fan-out as subgraph-per-item
  with per-iteration parameter binding.
- **Step 15:** `plan.wait_until` successor design — polling subgraph with
  condition node.
- **Step 16 (residual):** `plan.load` / `plan.save`. `plan.run` itself moved
  into 13.0(n) Phase 5; the load/save surface stays Step 16.

13.0(n)'s monster-PR exception is a one-time concession driven by the
structural coupling of the slot-model change. The follow-on writ-migrate PR
and Steps 13–16 return to the small-PR cadence the `CLAUDE.md` rules call
for.
