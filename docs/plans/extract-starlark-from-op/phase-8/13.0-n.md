---
title: "13.0(n): Variable binding infrastructure"
parent: "docs/plans/extract-starlark-from-op/phase-8.md"
issue: https://github.com/NobleFactor/devlore-cli/issues/283
status: in-progress
created: 2026-05-14
updated: 2026-05-19
---

# Plan: 13.0(n) — Variable binding infrastructure

## Summary

Rebuild the slot value model around a PowerShell-style variable binding system.
Graphs, subgraphs, and flow-control containers expose a bubble-up parameter
surface; the caller supplies values at execute time via a single immutable
variable map; a new [op.VariableResolver] assembles that map from layered
sources (override / flag / env / config / default) with explicit precedence and
provenance tracking via [op.VariableSource]. Retire the ambient
`RuntimeEnvironment.Data` / `Properties` machinery (done). Test-first: the
starlark integration tests that model `writ adopt` are authored before the
infrastructure that satisfies them. `writ adopt` running through the new
binding model is the exit discriminator.

The binding-layer types live in **`pkg/op`** alongside the rest of the
operations layer; there is no separate `pkg/binding`. Tool-side state (flag /
config / override maps + program name) lives in a new **`pkg/application`**
package that every tool (lore, star, writ, devlore-test) projects into.

`plan.assemble` invocation materialization is **in scope** for 13.0(n):
`plan.Provider` collects [op.Invocation] entries in its session
[op.InvocationRegistry] while a `.star` plan executes (each plan-mode
call routes through `plan.Provider.Invocation` per D12); `plan.assemble`
walks the supplied invocation set and materializes [op.Node] children
onto the assembled graph's root. `plan.run(graph)` then executes the
materialized Graph through a borrowed [op.GraphExecutor]. Without these
steps the integration-test contract authored under Phase 1 cannot be
satisfied.

## Exit criterion

13.0(n) is complete when both of the following hold:

1. **All tests pass**, including the seven starlark integration tests under
   `cmd/devlore-test/devloretest/data/test_writ_adopt*.star` that model the
   `writ adopt` workflow (mkdir → move → link) with bubbled parameter binding,
   subgraph composition, precedence and origin assertions, and failure-mode
   coverage (missing-required, type-mismatch).
2. **`writ adopt` runs through the new infrastructure** and produces the same
   behavior the user observes today — the three nil-activation call sites in
   `cmd/writ/writ/commands.go` (`:1406` Mkdir, `:1416` Move, `:1428` Link) are
   gone, replaced by graph construction + [op.VariableResolver] +
   [op.GraphExecutor.Run].

When both criteria hold, `make build` is clean, `make vet` is clean, and
`make test` is green across the full suite.

The 5 remaining nil-activation call sites in `cmd/writ/writ/migrate_cmd.go`
and `cmd/writ/writ/migrate/execute.go` and the file provider's defensive
nil-activation paths are **out of scope** for 13.0(n) — they land in a
follow-on PR at the smaller-PR cadence.

## Scope

### In scope

- **Slot model — sealed three-variant [op.SlotValue].** Variants:
  [op.ImmediateValue], [op.PromiseValue], [op.VariableValue]. `EnvironmentValue`
  and the `Properties` interface are gone (deleted; no migration window).
  `SlotValue.Resolve` signature is `(variables map[string]Variable, results map[string]any) any`.
- **Session ownership model.** [op.RuntimeEnvironment] is the single owner of
  the session's [op.Root] and is the only type that holds Close responsibility
  ([op.RuntimeEnvironment.Close], guarded by `sync.Once`). Co-users
  ([starlarkbridge.Runtime], [op.GraphExecutor], [op.Graph]) borrow the env by
  pointer.
- **[op.Plan] free-function planner.** Drives a planning session bounded by a
  spec and a caller-supplied closure: builds a planning env, constructs a
  fresh graph, binds via [op.Graph.Rebind], invokes the closure, then defers
  [op.Graph.Unbind] and `env.Close`. Returned graph is unbound until the next
  session-owner (the executor) rebinds it.
- **[op.GraphExecutor].** Constructor takes `(ctx, spec)`; the executor owns
  its own env and exposes `Close` + `Environment`. `Run(graph, variables)`
  rebinds the graph to the executor's env, dispatches, and defers
  `graph.Unbind` so the graph is portable to subsequent sessions.
- **Binding-layer types in [pkg/op](../../../../pkg/op).** [op.Variable]
  (Name / Value / Source), [op.VariableSource] (Kind / Name),
  [op.VariableSourceKind] (Unknown < Default < Config < Env < Flag < Override),
  [op.VariableResolver] (constructor takes `*application.Application`),
  [op.Parameter] (already present; `Default any`, `Optional bool`).
- **[pkg/application](../../../../pkg/application).** Tool-side handle with
  `Name`, `Flags`, `Config`, `Overrides` maps plus `DryRun()`.
  [application.NewApplication] projects the user-supplied flags from a cobra
  command's merged pflag set (only flags the user actually passed land in
  `Flags`).
- **[op.RuntimeEnvironment.RegisterParameter] cascade.** Provider-declared
  parameters resolve immediately against the Application's source maps
  (override → flag → env → config → default), populate `env.variables`, and
  return errors on type-mismatch or re-registration with a conflicting type.
  Reads via [op.RuntimeEnvironment.VariableByName].
- **`plan.variable(name, default_value=…)` builtin.** Authored on the
  [plan.Provider]; returns an [op.Variable] reference that the bridge
  translates to [op.VariableValue]{Name: name} at slot-fill time.
- **`plan.run` invocation materialization.** Walks the reachable
  invocation set collected by [op.InvocationRegistry] (owned by
  `plan.Provider`), reifies each [op.Invocation] as an [op.Node] on the
  bound graph's root, emits edges per the PromiseValue NodeRef and
  Resource producerID metadata the planners stashed during dispatch.
- **[op.ExecutableUnit.Parameters] bubble-up.** Node returns the parameters
  whose slots carry [op.VariableValue]; subgraph returns the deduplicated
  union of its topological roots' parameter surfaces.
- **[op.VariableResolver.Resolve] real implementation.** Precedence walk;
  type assertion at resolve time; env-string parsing to the parameter's
  declared Go type; aggregated errors on missing-required / type-mismatch /
  default-type-mismatch.
- **Preflight integration.** `bindVariables` preflight pass on
  [op.GraphExecutor.Run] computes `graph.Parameters()`, calls
  `resolver.Resolve`, populates `env.variables`, joins errors into the D5
  envelope, and fails fast.
- **Starlark integration tests (the contract).** Seven `.star` plans under
  `cmd/devlore-test/devloretest/data/test_writ_adopt*.star` already authored;
  they parse today, and they become green as Phase 2 / 3 / 4 / 5 land.
- **Test-harness extensions.** Runner option surface, [t.set_*] /
  [t.expect_variable*] builtins on the test-context provider.
- **`writ adopt` migration.** The three nil-activation sites in `runAdopt`'s
  helper (`commands.go:1406/1416/1428`) rewire to graph construction +
  `VariableResolver.Resolve` + `executor.Run`.

### Out of scope (deferred to follow-on PR after 13.0(n) lands)

- **Writ migrate.** The 5 nil-activation call sites in `migrate_cmd.go` and
  `migrate/execute.go`. Same migration pattern, smaller PR.
- **File provider defensive-path cleanup.** Deletion of the defensive
  nil-activation paths in `pkg/op/provider/file/provider.go`. Must wait until
  writ migrate's sites land, so the defensive paths aren't load-bearing.

### Out of scope (other phase-8 / phase-9+ work)

- `plan.choose` / `plan.gather` / `plan.wait_until` successor design.
- `plan.load` / `plan.save`.
- Orphan detection at plan-end.
- `CanConvert` and plan-time type-check pass.
- `.star` caller migration to new flow-control surfaces.
- `file.Resource` taxonomic factoring.
- Reconciliation as a side effect on Active catalog entries (#156).
- Platform verification at preflight (#282).

## Background

### Today's slot model (pre-13.0(n))

Sealed three-variant `op.SlotValue` (the original shape, pre-rewrite):

| Variant | Resolved at | Source |
|---|---|---|
| `ImmediateValue{Value any}` | plan time | call-site literal or converted Go value |
| `PromiseValue{NodeRef, Slot}` | execute time | `results[NodeRef]` (sibling output) |
| `EnvironmentValue{Key string}` | execute time | `Properties.Property(Key)` reading `RuntimeEnvironment.Data` |

The `Properties` interface and `RuntimeEnvironment.Data map[string]any`
provided an ambient property bag populated at process startup from CLI flags,
env vars, and user config. `plan.env("target_root")` created an
`EnvironmentValue` at plan time; resolution deferred to execute time via
`Properties.Property(key)`.

### Why the model needed to change

Two structural problems blocked the bubble-up composition model that
flow-control combinators require:

1. **No declared parameter surface.** Today, "overrides" passed to
   `Execute(graph, overrides)` was an opaque `map[string]any`. The executor
   had no declared schema to check against. A missing required parameter
   surfaced as a nil dereference or a type-assertion panic deep inside a
   provider method, mid-execution, after side effects had partially
   committed.
2. **Ambient state was not composable.** `RuntimeEnvironment.Data` was
   process-wide; it could not be re-scoped per subgraph. A subgraph composed
   inside a parent graph could not supply a different value for its child's
   `EnvironmentValue` lookup — the lookup hit the shared `Data` bag.

Bubble-up requires both: a typed parameter surface declared at plan time, and
a variable-resolution layer that scopes per-call rather than per-process and
tracks where each value came from.

### The unified model (what landed)

Replace `EnvironmentValue` with `VariableValue`. The slot's lookup target moves
from "process-wide property bag" to "this session's variable map." The tool
main becomes the assembler of the variable map — it parses CLI flags, env,
and config and stamps them into an [application.Application]; the
[op.VariableResolver] reads from `app.Flags` / `app.Config` / `app.Overrides`
plus the process environment (prefix derived from `app.Name`) and produces a
typed `map[string]op.Variable` with provenance. The executor sees only the
resolved map. The `Properties` abstraction is gone from the slot model
entirely. Subgraph composition becomes recursive bubble-up: a subgraph's
[op.VariableValue] slots are bound by its parent graph's slot fill (which may
be `ImmediateValue`, `PromiseValue`, or another `VariableValue`); the bubble
continues all the way up to the root graph's parameter surface.

## Design

### D1 — Sealed three-variant `SlotValue` (landed)

```go
type SlotValue interface {
    isSlotValue()
    Resolve(variables map[string]Variable, results map[string]any) any
}

type ImmediateValue struct{ Value any }
type PromiseValue   struct{ NodeRef, Slot string }
type VariableValue  struct{ Name string }
```

`ImmediateValue` and `PromiseValue` ignore the variables map;
`VariableValue.Resolve` returns `variables[v.Name].Value` (nil-safe on map
absence).

`VariableValue.Resolve` is reachable only after preflight has confirmed every
required `VariableValue` has a matching entry in the variable map. If
`variables[Name]` is missing at Resolve time, today's behavior returns nil —
Phase 4's preflight ensures the case is unreachable.

### D2 — Parameter (landed)

`op.Parameter` (in [`pkg/op/action.go`](../../../../pkg/op/action.go)):

```go
type Parameter struct {
    Name     string
    Type     reflect.Type
    Optional bool
    Variadic bool
    Kwargs   bool
    Default  any
}
```

`Optional` discriminates required-vs-optional (not a sentinel on `Default`).
`Default` is the parameter's declared default value, already at
`Parameter.Type` (parsed once at announce time by `parseDefaultExpression`).
Variadic and Kwargs cannot carry a default — the grammar rejects
`*parts?` / `**kwargs?=foo`.

Validation:

- **At resolve time.** [op.assignToType] asserts every non-env source value
  against the declared `Type` via `reflect.TypeOf(raw).AssignableTo(declared)`.
  Mismatches return an error before the variable is stored. Nilability is
  handled per `reflect.Kind` (pointer / interface / slice / map / chan / func
  kinds accept nil; everything else does not).
- **At slot-fill time.** Defense-in-depth — preflight catches issues first,
  but a slot-fill assertion mismatch is a `VariableResolver` bug and fails
  immediately rather than panicking inside a provider method.

### D3 — `ExecutableUnit.Parameters()` (asymmetric by type)

```go
type ExecutableUnit interface {
    ID() string
    Parameters() []op.Parameter
}
```

`Parameters()` answers "what's the input surface of this executable?" — but
the answer is computed differently for Node and Subgraph because the
underlying shape differs.

- **Node.** `Parameters()` returns the **full method signature** — every
  parameter the bound Go method declares, populated by `Node.Bind` from
  `method.Parameters()`. This is the Go-level reality of what the node can
  accept. Slot fills (`ImmediateValue` / `PromiseValue` / `VariableValue`)
  do not filter this view; the method signature is the contract regardless
  of how individual slots are wired.

- **Subgraph.** `Parameters()` returns the **bubble-up variable surface** —
  the deduplicated set of `VariableValue` references across the subgraph's
  children's slots (recursing into nested subgraphs). Discovery is a
  graph-walk:

  1. For each child node, iterate its slots; for each slot whose `Value` is
     `VariableValue{Name: N}`, contribute `slot.Parameter` (Name + Type +
     Default) under name N.
  2. For each child subgraph, recurse — its `Parameters()` already yields
     deduplicated, type-checked bubble-ups; merge into the parent's working
     set.
  3. Dedup by name. Same-name + same-`Type` collapses to one entry. Same
     name + different `Type` is a plan-time error (the variable map at
     runtime is keyed by name and carries one value).

  No topological-root algorithm is needed — `ImmediateValue` and
  `PromiseValue` slots are intrinsically resolved (inline / sibling output)
  and do not contribute to the external surface. The walk discovers
  exactly the slot fills that demand external values: the variable map
  keys the subgraph needs the resolver to provide.

**Why the asymmetry is intentional.** For a Node — atomic, Go-method-bound
— the input surface IS the method signature. For a Subgraph — composite
— the input surface IS the set of bubbled-up variables. Both answer
"what's the input surface?" using the most natural representation for
their respective shape. `Parameters()` is a uniform interface accessor;
its semantic varies with the type because the type's nature varies.

Phase 1 lands a stub on `*Subgraph` (parameters left nil); Phase 3 lands
the graph-walk and the name-collision check. Node.Parameters() needs no
Phase 3 work — `Node.Bind` already does the right thing.

**Note on Gather and other flow combinators.** A gather operation reads
its body's bubble-up surface to know which variable name(s) to bind per
iteration. Since gather bodies are typically Subgraphs (or wrap Nodes in
an implicit one), `Subgraph.Parameters()` is the discovery surface for
gather. The full per-combinator design (Gather, Choose, WaitUntil) sits
in Step 13/14/15 and is foreshadowed in D11 (stack-frame execution model
+ subgraph ownership).

### D4 — `Variable`, `VariableSource`, `VariableSourceKind` (landed)

In [`pkg/op/variable.go`](../../../../pkg/op/variable.go):

```go
// Ascending precedence; higher beats lower.
type VariableSourceKind int

const (
    VariableSourceKindUnknown VariableSourceKind = iota
    VariableSourceKindDefault
    VariableSourceKindConfig
    VariableSourceKindEnv
    VariableSourceKindFlag
    VariableSourceKindOverride
)

type VariableSource struct {
    Kind VariableSourceKind
    Name string  // lookup key that matched
}

type Variable struct {
    Name   string
    Value  any
    Source VariableSource
}
```

All three carry `String()` for diagnostics. `VariableSource.String()` formats
as `<kind>:<name>` (or `"unknown"` for the zero value). `Variable.String()`
formats as `<name> = <value> [<source>]`.

`Name` convention per `Kind`:

| Kind | `Name` = | Example `String()` |
|---|---|---|
| `Unknown` | (empty) | `unknown` |
| `Default` | parameter name | `default:target_root` |
| `Config` | config-map key | `config:target_root` |
| `Env` | literal env var with program prefix | `env:WRIT_TARGET_ROOT` |
| `Flag` | normalized snake-case flag name | `flag:target_root` |
| `Override` | override-map key | `override:layer` |

### D5 — `VariableResolver` (landed)

In [`pkg/op/variable_resolver.go`](../../../../pkg/op/variable_resolver.go):

```go
type VariableResolver struct {
    app      *application.Application
    resolved map[string]Variable
}

func NewVariableResolver(app *application.Application) *VariableResolver

func (r *VariableResolver) EnvPrefix() string
func (r *VariableResolver) Get(name string) (Variable, bool)
func (r *VariableResolver) Variables() map[string]Variable
func (r *VariableResolver) Resolve(parameters []Parameter) []error
```

The resolver captures a pointer to the [application.Application] at
construction. At resolve time it reads `app.Flags`, `app.Config`,
`app.Overrides` directly (not snapshots — late binding lets per-command flag
adjustments take effect). The env-var prefix is derived from
`strings.ToUpper(app.Name) + "_"`.

**Resolution order per parameter** (descending precedence; first hit wins):

1. `app.Overrides[name]` → `VariableSourceKindOverride`.
2. `app.Flags[name]` → `VariableSourceKindFlag`. `app.Flags` keys are
   snake-case (normalized at [application.NewApplication] time —
   `dry-run` → `dry_run`, `target-root` → `target_root`); the resolver
   matches the parameter name verbatim. Framework code that reads specific
   flags (e.g., [application.Application.DryRun] for `dry-run`) likewise
   reads the snake-case key (`Flags["dry_run"]`).
3. `os.Getenv(EnvPrefix() + camelToSnake(name))` → `VariableSourceKindEnv`.
   `camelToSnake` uses Go's idiomatic greedy-acronym rule: consecutive
   uppercase letters form a single acronym; an acronym ends when a
   lowercase letter follows. Examples: `targetRoot` → `TARGET_ROOT`,
   `sourceURL` → `SOURCE_URL`, `URLPath` → `URL_PATH`, `UserID` →
   `USER_ID`. Mixed acronyms like `IPv4Address` produce slightly imperfect
   output (`I_PV4_ADDRESS`) and are tolerated; the rule serves the 99%
   case at zero maintenance cost.
4. `app.Config[name]` → `VariableSourceKindConfig`.
5. `Parameter.Default` (when `Optional == true` and `Default != nil`) →
   `VariableSourceKindDefault`.

After picking the value, the resolver:

- For env-sourced values, wraps the raw string in a private `envValue` type
  that implements [op.SourceConverter]. The [op.Convert] cascade picks the
  wrapper up at step 5 (source-side opt-in); `envValue.ConvertTo` handles
  primitives (bool / int* / uint* / float* / string via the existing
  `Kind` switch shared with `parseDefaultExpression`), stdlib special-cases
  (`time.Duration` via `time.ParseDuration`, `os.FileMode` via
  `strconv.ParseUint(base 0)`), then probes the target for
  [encoding.TextUnmarshaler], then falls through to `json.Unmarshal` for
  arbitrary Go types. `envValue.CanConvertTo` returns `false` for targets
  implementing [op.Resource] so [op.Convert] step 7 (registered Resource
  construction) wins the routing — env strings for Resource-typed
  parameters feed the same constructor path that tag URIs use today.
- For non-env sources, type-asserts the picked value against
  `parameter.Type` via [op.assignToType]. Mismatch → error.
- Records `resolved[name] = Variable{Name, Value, Source}`.

[op.Convert] itself is not modified. Adding a string-source step there would
silently parse stray starlark string literals reaching primitive-typed
slots — the codebase already documented this layering decision at
[`pkg/op/parameter.go:236`](../../../../pkg/op/parameter.go). `envValue` is
a distinct type from `string`; starlark slot fills produce plain `string`
and continue to error correctly when a primitive target is required.

For each required parameter (`Optional == false`) that found nothing →
aggregated error naming the parameter, target type, and the literal lookup
keys the resolver tried.

`Get` and `Variables` panic if called before `Resolve` — that's a programmer
error. After `Resolve` the resolver is fully read-only.

Phase 1 skeleton: `Resolve` initializes the resolved map to empty and
returns no errors. Phase 2 lands the cascade walk.

### D6 — `RegisterParameter` cascade on the runtime environment (landed)

Providers that need a binding-layer parameter immediately (at construct time,
not at preflight) call `env.RegisterParameter(p)`. The method:

1. Records the parameter in `env.declaredParameters`. Re-registration with
   the same name + same `Type` is idempotent; same name + different `Type`
   returns a type-mismatch error.
2. Walks the cascade: `Overrides → Flags → Env (deferred) → Config → Default`
   on `env.Application` directly (the resolver is shared, but
   `RegisterParameter` is a synchronous helper that doesn't require a full
   `Resolve` pass to inspect a single name).
3. Type-asserts the picked value via [op.assignToType]. Mismatch → error
   without overwriting state.
4. Stores the result in `env.variables[name]` so subsequent
   [op.RuntimeEnvironment.VariableByName] calls return it.

This shape lets providers participate in variable binding **before** the
graph's preflight pass — the four star providers (`config`, `setup`, `goast`,
`commands`) currently rely on this path for tool-side parameters.

The env source step in `RegisterParameter` is a deliberate skip until the
resolver's full env-string-parsing logic lands in Phase 2.
[op.VariableResolver.Resolve] is the canonical path for graph-time parameter
resolution; `RegisterParameter` is the construct-time fast path.

### D7 — Session ownership, planning, execution (landed; Plan closure signature flipped in-flight 2026-05-19)

```go
func NewRuntimeEnvironment(ctx context.Context, spec *RuntimeEnvironmentSpec) *RuntimeEnvironment
func (re *RuntimeEnvironment) Close() error           // sync.Once-guarded

func NewGraph() *Graph                                // unbound
func (g *Graph) Rebind(env *RuntimeEnvironment)
func (g *Graph) Unbind()
func (g *Graph) RuntimeEnvironment() *RuntimeEnvironment

func Plan(ctx context.Context, spec *RuntimeEnvironmentSpec,
          fn func(*RuntimeEnvironment) (*Graph, error)) (*Graph, error)

func NewGraphExecutor(ctx context.Context, spec *RuntimeEnvironmentSpec) *GraphExecutor
func NewGraphExecutorForEnv(env *RuntimeEnvironment) *GraphExecutor   // borrows env; Close is no-op
func (e *GraphExecutor) Close() error
func (e *GraphExecutor) Environment() *RuntimeEnvironment
func (e *GraphExecutor) Run(graph *Graph, variables map[string]Variable) (any, error)
```

`Plan` and `GraphExecutor` are the two Go-side session-owners for the
two-session path. Each builds an env from a spec, binds the graph during
the active session, and unbinds at the end so the graph is portable to
the next session. The executor's `Run` accepts the resolved variable map
(Phase 4 populates it from the executor's own resolver pass before
dispatch; callers pass `nil`).

**In-script single-session path.** When starlark drives execution
in-script via `plan.run` (per D8 / Phase 5), `plan.Provider.Run`
constructs a `GraphExecutor` via `NewGraphExecutorForEnv(p.RuntimeEnvironment())` — the
borrowed-env constructor — rather than building a fresh env from a
spec. The borrowed executor's `Close` is a no-op (the env is owned by
the surrounding `op.Plan` lifecycle and closed on its defer). Preflight
(D10), dispatch, and the rest of the execution machinery happen inside
the borrowed `GraphExecutor.Run` **exactly** as they do for the Go-side
path. There is no preflight in `plan.Provider.Run` — `plan.Provider.Run`
is a thin delegator that borrows the env, constructs the executor, and
calls `executor.Run(graph, nil)`. Single env, single session, single
preflight code path.

### D8 — Tier-3 plan methods: `plan.assemble`, `plan.run`, `plan.save`, `plan.load`, `plan.clear` (Phase 5)

Five direct methods on `plan.Provider` — a Graph materializer, a Graph
executor, a Graph serializer, a Graph deserializer, and a registry
resetter. None are Invocation-producing; none route through codegen or
through the `Invocation` dispatch. The materializer-plus-executor model
and the multi-graph-from-one-registry pattern were settled during the
2026-05 spec session that closes 13.0(k); this section captures the
settlements load-bearing for Phase 5.

```python
# Creation / composition (returns an immutable Graph)
graph = plan.assemble(body=[...], retry_policy=...)

# I/O (symmetric)
plan.save(graph, "workflow.json")
graph = plan.load("workflow.json")

# Execution — takes a Graph only; no body= shortcut
result = plan.run(graph)

# Reset the bridge's plan state
plan.clear()
```

**`plan.assemble(body=[…], …kwargs)`** — the canonical Graph constructor.

- `body=` is **required** and lists the invocations to root at
  `graph.Root`. For each, `plan.assemble` calls
  `graph.Root.AddChild(inv.Target)`, which stamps `parentID = "root"`
  on the Target. There is no implicit-body fallback: omitting `body=`
  is an error, consistent with the "plan.assemble is explicit"
  decision (no auto-materialization, no auto-rooting).
- `body=` elements with an existing non-empty `parentID` (already a child
  of some other Subgraph) are a nesting error — nested invocations cannot
  be promoted to root.
- Reserved kwargs `retry_policy=` and `error_action=` stamp those fields
  on `graph.Root`. Remaining kwargs populate `graph.Root.FrameBindings`.
- **Orphan check (post-assemble registry scan).** Parent-stamping during
  `.star` evaluation IS the reachability mark: container methods route
  through their specialized planner (per D12) — `flow.SubgraphPlanner`,
  for example — which calls `AddChild` on each `body=` element, stamping
  its `parentID` to the enclosing Subgraph's ID. After `plan.assemble`
  has rooted its own `body=` and applied reserved kwargs, every
  Invocation Target wired into any container's body chain has a
  non-empty `parentID`; anything still empty is an orphan.
  `plan.assemble` does a single linear pass over the
  `op.InvocationRegistry`, collects orphans via `errors.Join`, and
  returns `(nil, err)` when the set is non-empty. No separate
  reachability walk is needed. To assemble divergent graphs from the
  same registry, call `plan.clear()` between assembles.
- Children within each Subgraph are placed in **topological order** via
  `Subgraph.sortChildren()` applied post-rooting to each Subgraph
  reachable from `graph.Root`.

**`plan.run(graph)`** — executes a Graph. The Graph must come from
`plan.assemble` or `plan.load`. There is **no `body=` shortcut**: to
execute a freshly-composed graph, authors write
`plan.run(plan.assemble([...]))` explicitly. Returns the graph's
return value. Preflight, dispatch, and receipt collection happen
inside a `GraphExecutor` constructed via `NewGraphExecutorForEnv`
on `plan.Provider`'s env back-pointer (per D7's in-script
single-session path).

**`plan.save(graph, path)`** — serializes a Graph to `path`. Returns
`None`. Saving an orphan-bearing Graph is unreachable (assemble already
refused), so plan.save trusts the Graph it receives.

**`plan.load(path)`** — deserializes a Graph from `path`. Returns the
Graph. The inverse of `plan.save`.

**`plan.clear()`** — resets the plan to its initial state: purges
`op.InvocationRegistry`, resets parent-stamping state. Does NOT
invalidate previously-assembled Graphs (those are immutable artifacts
the user holds). Does NOT trigger orphan detection (orphans are
detected at `plan.assemble`, not at `plan.clear`).

**Multi-graph from one registry.** The invocation registry is a template
pool. The same Invocation can be referenced from multiple `plan.assemble`
calls' `body=` lists, producing per-Graph `op.Node` instances:

```python
cleanup_foo = plan.file.remove("foo")
cleanup_bar = plan.file.remove("bar")
prod_graph    = plan.assemble([cleanup_foo, cleanup_bar], environment="prod")
staging_graph = plan.assemble([cleanup_foo, cleanup_bar], environment="staging")
```

Both assembles cover the full registry contents (no orphans), and produce
graphs with distinct root frames (`environment` differs per assemble).
Per-Graph `op.Node` instances are settled for now (execution state stays
on the Node struct). plan.clear()'s implementation phase migrates
execution state off the Graph if shared-template Nodes become desirable.

**Parent identity is by ID, not pointer.** Pointers don't serialize.
`graph.Root.ID` is a constant ("root"), so a child invocation parented to
two graphs both stamp `parentID = "root"` — idempotent. Containment
(`Subgraph.Children []NodeID`) is the canonical serialized form; the
runtime parent reference is derivable from containment at load time. The
parent-pointer landing in Phase 3 is refactored to this ID-based model in
**Phase 3.5** (precursor commit before Phase 5 starts).

**Reserved kwargs** (recognized by `plan.Provider.Invocation` on every
plan-mode call, plus `plan.assemble`):

| Kwarg | Role | Destination |
|---|---|---|
| `label=` | Invocation label | `Invocation.Label`. Empty triggers `registry.AutoLabel("<provider>.<method>")`. |
| `retry_policy=` | Retry behavior | `executableUnit.retryPolicy`. |
| `error_action=` | Failure handler — always a `*Subgraph` at the op layer. Authoring grammar: `error_action=[invocation, ...]` (a list of invocations). The plan-side helper `subgraphFromInvocations` materializes the list into a Subgraph (same primitive `plan.subgraph(body=[...])` uses). No bare-Node / bare-Invocation auto-wrap. | `executableUnit.errorAction`. Default: `flow.Provider.Failed` (built-in sentinel, no registry entry). |
| `body=` | Structural children (container methods only) | `Subgraph.Children`. The specialized container planner (`SubgraphPlanner`, etc.) calls `AddChild` per element, stamping each child's `parentID`; `plan.assemble` does not (root-level children remain parentless until consumed). |
| `items=` | Iteration / dispatch data (container methods only) | The Subgraph's `Items` slot, resolved at execute time. |
| (anything else) | Frame bindings (container methods only) | The invoked subgraph's `FrameBindings`. |

**Containers.** The four containers (`subgraph`, `choose`, `gather`,
`wait_until`) all materialize to `op.Subgraph`. Each method declares a
specialized planner in its `MethodMetadata` (per D12): `SubgraphPlanner`,
`ChoosePlanner`, `GatherPlanner`, `WaitUntilPlanner`. The planner owns
the kwarg classification and unit-shape construction for its method;
plan.Provider's `Invocation` method is otherwise uniform across
containers and leaf methods. The receiver-type dispatch at execute time
differs per container (subgraph runs all children sequentially; choose
runs one selected branch; gather runs body once per item with
per-iteration child frames; wait_until polls). Per-iteration frame
minting is execute-time behavior owned by Steps 14/15; Phase 5 produces
only the static graph.

**`op.Node` is a goroutine-safe template.** Per the project memory
[execution units are goroutine-safe templates], a single materialized
`op.Node` can be safely dispatched concurrently. Iteration/parallel
constructs dispatch one body N times with per-dispatch frame minting —
the body is never cloned per iteration.

### D9 — Override routing at `Execute()` (landed)

`GraphExecutor.Run` signature is `Run(graph, variables map[string]Variable)`.
The executor stashes the variable map on its receiver; dispatch reaches a
slot with `VariableValue{Name: X}` and resolves via `variables[X].Value`. No
`Properties` interface lookup remains anywhere in the executor.

Subgraph composition: when a parent graph's slot fill for a child subgraph
is `VariableValue{Name: Y}`, the variable map at the root carries `Y`. The
subgraph's matching slot resolves to the same value. Bubble-up at runtime
mirrors the bubble-up declared at plan time.

Phase 4 wires the preflight pass that populates `variables` from the
executor's own [op.VariableResolver] before dispatch. Today's callers (writ
adopt, tests) pass `nil` or supply a pre-resolved map directly.

### D10 — Preflight integration (landed)

New preflight pass [GraphExecutor.bindVariables] added to
[GraphExecutor.Run], running **before** [ResourceCatalog.ResolvePending]
(landed by 13.0(k) k.15) and ahead of future orphan / type-check /
platform passes. The executor doesn't construct the resolver itself — the
env's `variableResolver` (wired by [NewRuntimeEnvironment] from
`spec.Application`) is the one source. The pass:

1. Compute `graph.Parameters()` — the root subgraph's bubble-up variable
   surface (per D3).
2. Call `env.variableResolver.Resolve(env, params)`. The resolver walks
   each parameter through the cascade (Override → Flag → Env → Config →
   Default), type-asserts source values, and aggregates errors for
   missing-required / type-mismatch / default-type-mismatch.
3. If `len(errs) > 0`: `graph.State = StateFailed`; return
   `errors.Join(errs...)` to the caller. No dispatch runs.
4. On success: merge `resolver.Variables()` into `env.variables`. If
   the caller passed a non-nil `callerVariables` map to `Run`, layer
   those entries on top as the highest-priority override (test injection
   surface; production callers pass nil).
5. Mirror `env.variables` into `e.variables` so [Node.ResolveSlots] sees
   the same map [GraphExecutor.bindVariables] just populated.

**Runs in every mode, including dry-run.** Variable resolution is pure —
it reads in-memory `Application` maps and the process env; no
filesystem or network probes. Dry-run output that renders slot values
needs them resolved, so the pass runs unconditionally. This is the one
divergence from `ResolvePending`'s convention (which skips dry-run
because catalog resolution touches target-machine state).

### D11 — Stack-frame execution model + subgraph ownership (forward-looking)

The execution model 13.0(n) prepares the ground for — and that Step 13/14/15
implement in full — is **stack-frame-based dispatch with per-type subgraph
executors and explicit ownership pointers**. 13.0(n) lands the ownership
pointer in Phase 3 as forward-compat scaffolding; the frame machinery and
the per-type executors are downstream work. This section documents the
target so the Phase 3 scaffolding doesn't need a backwards-incompatible
rename later.

#### Stack frames

Every invocation of any subgraph (root, plain composition, gather body
iteration, choose branch, wait_until polling cycle) creates a **frame**:

```go
type Frame struct {
    Subgraph  *Subgraph          // the static template being invoked
    Parent    *Frame             // the calling frame; nil only at the root invocation
    Variables map[string]Variable // overlay on Parent.Variables; lookups fall through
    Results   map[string]any     // node ID → output, scoped to this invocation
    Attempts  []Attempt          // per-invocation retry history
    Status    SubgraphStatus     // pending → completed | failed | rolled_back | skipped
}
```

Frames are dynamic; the `Subgraph` struct is a **template** (its current
`Status`, `Attempts`, `State` fields are conceptually frame state, not
template state, and will move to the frame in Step 13). The same Subgraph
template can be invoked many times concurrently — gather iterating 16
items pushes 16 concurrent frames over the same body subgraph; each frame
isolates its own state.

**What frames solve:**

- **Per-iteration variable scoping.** Gather binds `iter.Variables[name] =
  item_i` for each iteration's frame. Other iterations see their own
  bindings via their own frames; the parent's `Variables` is shadowed
  read-only.
- **Variable resolution scope chain.** `VariableValue.Resolve` walks the
  frame chain — current frame first, then parent, then grandparent —
  until it finds a binding for the name or runs out. Nested subgraphs can
  declare local variables that shadow outer ones; gather's iteration
  variable is one example of that.
- **Concurrent state isolation.** Each iteration of a gather body has
  its own frame; its Status / Attempts / Results live there. No race on
  shared `Subgraph.Status` because no concurrent writer touches the
  template — each writes its own frame.
- **Compensation and rollback.** Unwinding walks the frame stack; each
  frame knows what it consumed (Variables) and produced (Results), so
  compensation can be sequenced precisely without re-inspecting the
  shared template.

**What stays per-executor (correctly so):**

- **Retry policy.** Gather retries per-iteration. Choose re-chooses
  (doesn't retry the same branch). Wait_until polls (a retry-of-condition
  shape). Root retries the whole subgraph. The MECHANICS (frame.Attempts)
  are uniform; the POLICY is type-specific and rightly so — that's the
  combinator's defining behavior.
- **Status emission.** Each combinator emits its own progress shape
  (gather: per-iteration progress; choose: which branch was taken;
  wait_until: each poll). Frames provide the context label; the
  per-executor decides what to say.
- **Dispatch logic.** The whole point of the per-type split. Frames
  don't subsume it.

#### Subgraph ownership

Every executable unit (Node or Subgraph) carries a **parent ID** to its
containing Subgraph (or to the graph root):

```go
type executableUnit struct {
    id          string
    parameters  []Parameter
    parentID    string          // empty only for the graph root; references enclosing Subgraph's ID
    retryPolicy *RetryPolicy    // every unit; nil = inherit from parent chain
    errorAction *Subgraph       // every unit; nil = default flow.Provider.Failed sentinel; populated from error_action=[...] via subgraphFromInvocations
}
```

By-ID rather than by-pointer because pointers don't serialize — the same
constraint that drives the Edge model. Containment
(`Subgraph.Children []NodeID`) is the canonical serialized form; the
runtime parent reference is derivable from containment at load time.
`graph.Root.ID` is a constant ("root"), so a child invocation parented to
two graphs both stamp `parentID = "root"` — idempotent (cf. D8's
multi-graph reuse pattern).

**Retry policy inheritance via frame chain.** The retryPolicy field
follows a parent-walk resolution rule analogous to variable lookup: when
a unit's retryPolicy is nil, the executor walks the parentID chain
upward and uses the first non-nil retryPolicy it encounters. A nil
throughout the chain means "no retry, maxAttempts = 1." A unit can opt
OUT of an inherited cascade by setting an explicit
`&RetryPolicy{MaxAttempts: 0}` — that's a non-nil policy that terminates
the walk AND specifies zero retries. The effective lookup lives in a
helper `effectiveRetryPolicy(graph *Graph, unit ExecutableUnit)
*RetryPolicy` (Phase 5 implementation alongside the bridge work);
current executor logic (`if sg.RetryPolicy() != nil { ... }`) is
interim and switches to the helper when the bridge target-subgraph
work lands.

**ErrorAction shape and default sentinel.** `errorAction` is always a
[*Subgraph] at the op layer (Phase 5.3b narrowing — was previously
`ExecutableUnit`). Authoring grammar: `error_action=[...]` always takes
a list of invocations, never a bare invocation. The plan-side helper
`subgraphFromInvocations` materializes the list into a Subgraph by
`NewSubgraph` + `AddChild` per element. Same primitive shape as
`body=` for `plan.subgraph` — the list IS the subgraph specification.
No type-switching, no auto-wrap of bare nodes; a missing pair of
square brackets is an authoring error.

Nil errorAction falls back to the `flow.Provider.Failed` sentinel at
dispatch time. ErrorAction does NOT inherit through the parent chain —
failure handling is unit-local, and a parent's error_action handles
parent-level failures, not inherited into children. (Frame-chain
inheritance for errorAction would mean a child's failure runs a handler
the child didn't declare, which surprises authors. Retry is different —
retry semantics like "retry the whole pipeline" are naturally cascading.)

The parent ID is stamped at the moment a child is added to a subgraph —
via the `Subgraph.AddChild` method (Phase 3, refactored from pointer to ID
in Phase 3.5) that centralizes the wiring and replaces today's direct
`Children = append(...)` sites in `graph.go`.

The ownership pointer drives dispatch in the per-type-executor world:

- The top-level executor dispatches **only the root**. It never asks "is
  this branch?"; it just walks `root.Children`.
- Each subgraph's per-type executor dispatches **its own children**, in
  whatever frame-construction pattern that combinator requires.
- Recovery-site walks ownership upward to find the saga-owning frame.
- Variable resolution uses the frame chain (dynamic), not the ownership
  chain (static); the two can diverge (a body subgraph statically owned
  by some parent but dynamically invoked by gather has a frame parented
  to gather's frame).

The `Subgraph.Branch bool` flag is **removed** in 13.0(n). It has no
consumers in the codebase — no reader, no writer outside marshal /
unmarshal round-trip — and the per-type executor model makes its
historical meaning ("choose owns dispatch of me") moot, since every
flow combinator owns dispatch of its body. Removal scope:
`pkg/op/subgraph.go` (field + doc), `pkg/op/marshalers.go` (payload
field, marshal line, unmarshal line). No downstream callers to
migrate.

#### What 13.0(n) implements

Phase 3 landed the **forward-compatibility scaffolding** as parent
pointers; Phase 3.5 refactors it to the by-ID model the serialization
contract requires; Phase 5 consumes the by-ID model for materialization.
After 13.0(n) closes:

- `parentID string` field on `executableUnit` (refactored from
  `parent *Subgraph` in Phase 3.5).
- `retryPolicy` and `errorAction` fields on `executableUnit` (Phase 3.5
  alongside the parent-ID refactor).
- `(s *Subgraph) AddChild(child SubgraphChild)` stamps the parent ID
  idempotently when wiring children.
- Bridge-side parent-stamping: each Invocation listed in a `body=` kwarg
  on a container call gets its `parentID` stamped to the calling
  subgraph's ID at .star dispatch time (Phase 3.5).
- The Tier-3 `plan.assemble` / `plan.run` / `plan.save` /
  `plan.load` / `plan.clear` API (Phase 5, per D8).
- Reachability walk + orphan check + topological-order materialization
  (Phase 5, per D8).

Full frame plumbing and the per-type subgraph executors are **out of
scope for 13.0(n)** and land as Step 13 (choose executor), Step 14
(gather executor), Step 15 (wait_until executor). Phase 3.5's by-ID
scaffolding ensures those phases don't need a serialization rework.

### D12 — Planner-dispatch model (Phase 5 redesign)

Replaces the original NodeBuilder + `options=` dispatch model. Every
plan-mode call routes through a single dispatch method on
`plan.Provider`; per-method dispatch behavior is selected by a `Planner`
declared in the method's announcement metadata.

**Components:**

- **`op.Planner` interface.** One method:
  `Plan(invocator PlanInvocator, receiverType ProviderReceiverType,
   method *Method, args []any, kwargs map[string]any) (ExecutableUnit, error)`.
  The planner is responsible only for unit-shape construction (Node vs
  Subgraph; slot fill; default application); `plan.Provider` stamps
  Label / RetryPolicy / ErrorAction on the returned unit and registers
  the Invocation. Planners are stateless and constructed once per type
  at announcement time.
- **`op.PlanInvocator` interface.** The narrow contract a Planner
  consumes for plan-time session state. Today: `InvocationRegistry()`.
- **`op.ActionPlanner`.** The default vanilla planner — one starlark
  call produces one leaf `*op.Node`. Used by every method whose
  announcement does not declare a specialized planner.
- **`flow.{Choose,Gather,Subgraph,WaitUntil}Planner`.** Specialized
  planners for flow's four container methods. Each materializes a
  differently-shaped `*op.Subgraph`. flow's leaf methods (`Complete`,
  `Degraded`, `Elevate`, `Failed`) use `ActionPlanner` like everything
  else — flow is not a "type" with its own planner; the dispatch
  specialization is at the **method** level, not the provider level.
- **`op.MethodMetadata`.** Codegen-emitted record per method:
  `ParameterNames []string` (ordered to match the Go method's parameter
  slots) + `Planner reflect.Type` (optional; nil → ActionPlanner).
  Carried in the announcement map passed to `AnnounceProvider`:
  `AnnounceProvider(type, roles, ctor, map[string]MethodMetadata)`.
- **`op.plannerForType(reflect.Type) Planner`.** Reflection-based
  resolver that turns the declared planner type into its singleton
  instance. Handles value-receiver and pointer-receiver implementations
  uniformly.
- **`*op.Method.Planner` field.** The resolved Planner instance is
  baked onto each `*op.Method` at announcement (via `SetPlanner`).
  Method-resolution-time, not call-time.
- **The adapter at `plan.<X>`.** A tiny `starlark.HasAttrs` carrying two
  fields: a back-pointer to `plan.Provider` and the
  `op.ProviderReceiverType` it routes for. `Attr(name)` returns a
  builtin that calls `plan.Provider.invocation(receiverType, name, args,
  kwargs, retryPolicy, errorAction, label)` (unexported method on
  plan.Provider). The adapter has no business logic; the dispatch lives
  on plan.Provider. plan.Provider maintains a map of these adapters,
  one per planning sub-namespace, populated lazily by `ResolveAttr`.

**Call flow** for `plan.file.write_text(path="foo", content="bar",
label="my_write")`:

1. starlark resolves `plan` → `*goReceiver` wrapping `plan.Provider`.
2. `.file` → `plan.Provider.ResolveAttr("file")` returns the adapter.
3. `.write_text` → `adapter.Attr("write_text")` returns a `starlark.Builtin`.
4. Builtin invoked with `(args, kwargs)`. Its body extracts reserved
   kwargs (`label`, `retry_policy`, `error_action`) by name, converts
   the rest from starlark → Go via the bridge's standard conversion,
   and calls `plan.Provider.Invocation`.
5. `plan.Provider.Invocation`:
   - Looks up `*op.Method` from `receiverType.MethodByName("WriteText")`.
   - Calls `method.Planner().Plan(p, receiverType, method, args, kwargs)`.
   - Stamps `Label` / `RetryPolicy` / `ErrorAction` on the returned unit.
   - Wraps in `&op.Invocation{Target: unit, Result: NewPromise(unit, ""),
     Label: <resolved>}` and registers via `p.invocations.Register`.
6. The `*op.Invocation` is wrapped by the framework's standard
   goReceiver path and returned to starlark. Its fields (`Target`,
   `Result`, `Label`) and its sole method (`FillSlot`) are accessible
   from the script.

**Tier 2 (root-planned peer methods).** Flow is root-placed; its
methods land flat on `plan.*` (e.g., `plan.choose(...)`, not
`plan.flow.choose(...)`). `plan.Provider.buildPeerBuiltins` at init
iterates root planners and, for each method, registers a builtin in
`peerBuiltins` keyed by the bare snake-case name. The builtin closes
over `(p, receiverType, methodName)` and routes through
`plan.Provider.invocation` exactly like the adapter's builtins do.

**Why method-level, not provider-level.** Container semantics (Choose,
Gather, Subgraph, WaitUntil) are method-level concepts. Mixing flow's
leaf methods (`Complete`, `Failed`, …) into a FlowPlanner forces a
per-method switch inside the planner — the exact runtime kind-discovery
the NodeBuilder retirement was meant to eliminate. With method-level
planner declaration, the answer is at announcement: the method either
names a planner or defaults to ActionPlanner. No per-call branching.

**Subgraph is a unit that holds an action.** Each container kind binds
its OWN action — there is no single "subgraph action":

| Starlark call | Subgraph.Receiver | Go method |
|---|---|---|
| `plan.subgraph(...)` | `flow.Subgraph` | `flow.Provider.Subgraph(items []any, kwargs map[string]any)` |
| `plan.choose(...)` | `flow.Choose` | `flow.Provider.Choose(defaultCase any, cases ...Case)` |
| `plan.gather(...)` | `flow.Gather` | `flow.Provider.Gather(activationRecord, items, do, limit)` |
| `plan.wait_until(...)` | `flow.WaitUntil` | `flow.Provider.WaitUntil(target, predicate, timeout, interval)` |

The four `flow.<Kind>Planner` types each mint a Subgraph and stamp its
`Receiver` to the kind's action name. Subgraph's slot list comes from
the bound method's parameters — same Bind path Node uses. The
executor dispatches Subgraph the same way it dispatches Node:
`unit.Action()` resolves through the registry by Receiver name. No
hardcoded `executeSubgraph` branch; the existing one becomes the body
of `flow.Provider.Subgraph`. **One ExecutableUnit type** is the target
shape; today Node and Subgraph are still distinct types because the
Subgraph fields (`FrameBindings`, `Items`, `Cases`, `Branch`,
`Compensate`) haven't yet collapsed into slots. Closing that gap is
part of Phase 5's pending work.

**plan.assemble materialization.** `plan.assemble(invocations, ...kwargs)`
is the explicit materialization step. Each `.star` script that wants a
Graph (whether to execute it via `plan.run`, save it via `plan.save`, or
inspect it from the harness) must call it. Auto-materialization on
Invocation registration was rejected.

The full materialization pipeline (root the body, frame setup, edge
materialization, topological sort, orphan scan) is documented as the
"Shared assemble pipeline" later in this phase. Briefly:
`plan.assemble` allocates a fresh `*op.Graph`, rebinds it to
`p.RuntimeEnvironment()` (the env back-pointer inherited from `op.ProviderBase`),
roots each `body=` invocation under `graph.Root`, stamps reserved
kwargs on Root, materializes edges, topologically sorts reachable
Subgraphs, runs the orphan scan, and returns the Graph (or `(nil, err)`
on orphan failure).

No state is stashed on `plan.Provider`; the assembled Graph is the
return value. Execution happens when the script subsequently calls
`plan.run(graph)`.

Each phase is one or more commits on
`refactor/extract-starlark-from-op.phase-8`. Build stays green at every
commit boundary — Phase 1's failing-tests are intentional and the harness
reports them as expected failures via [t.expect_error].

### Phase 1 — Foundations (DONE)

Done across four commits. After Phase 1:

- The sealed-three slot model is in place ([op.VariableValue] replaces
  `EnvironmentValue`); `Properties` deleted; `RuntimeEnvironment.Data` and
  `Property` deleted.
- `pkg/op` binding-layer types are in place ([op.Variable],
  [op.VariableSource], [op.VariableSourceKind], [op.VariableResolver]
  skeleton).
- `pkg/application` is in place with `NewApplication` + `DryRun`.
- `pkg/assert` adds `Nil` / `NotNil` helpers for invariant checks.
- [op.Plan] + [op.GraphExecutor] own session lifetimes; [op.Graph.Rebind] /
  [op.Graph.Unbind] are the binding surface; `RuntimeEnvironment.Close` is
  `sync.Once`-guarded.
- `starlarkbridge.NewRuntime(env)` borrows the env from its caller; no
  internal env construction.
- `cmd/lore`, `cmd/star`, `cmd/devlore-test` migrated to the
  one-env-per-session pattern.
- `RegisterParameter` cascade is in place; the four star providers
  (`config`, `setup`, `goast`, `commands`) consume it.
- `plan.variable(name, default_value=…)` builtin is in place on the
  [plan.Provider].
- Seven `.star` integration tests authored under
  `cmd/devlore-test/devloretest/data/test_writ_adopt*.star` — they parse and
  the harness invokes them; preflight does not yet validate so they execute
  against an empty graph.

### Phase 2 — `VariableResolver.Resolve` real implementation (DONE)

Landed in `pkg/op/variable_resolver.go` (`Resolve` at line 115; per-parameter
`resolveOne` cascade at line 159):

- Source precedence cascade per D5: Override → Flag → Env → Config → Default.
- Env-string parsing via the private `envValue` type
  ([`pkg/op/env_value.go`](../../../../pkg/op/env_value.go)) implementing
  [op.SourceConverter]; primitives via the shared `Kind` switch, stdlib
  special cases (`time.Duration`, `os.FileMode`), `encoding.TextUnmarshaler`,
  `json.Unmarshal` fallback. Resource-typed targets opt out via
  `envValue.CanConvertTo` so [op.Convert] step 7 (registered Resource
  construction) wins routing.
- `CamelToSnake` helper for env-key derivation
  ([`pkg/op/helpers.go`](../../../../pkg/op/helpers.go)).
- Origin tagging per source ([op.VariableSource]).
- Aggregated errors: missing-required (with literal lookup keys named in the
  message), type-mismatch, default-type-mismatch.
- Tests in [`pkg/op/variable_resolver_test.go`](../../../../pkg/op/variable_resolver_test.go).

### Phase 3 — `Subgraph.Parameters()` bubble-up + ownership pointer (DONE)

> **Note:** The parent-pointer landing here is refactored to an ID-based
> reference in **Phase 3.5** (precursor commit before Phase 5 starts). The
> ID-based model is the canonical form per D8 / D11; the pointer landing
> was an interim shape that pre-dated the serialization constraint
> surfacing in the spec session.



Two pieces, both small:

**Subgraph.Parameters() bubble-up via graph-walk (per D3):**

- Implement `*Subgraph.Parameters()` as a graph-walk over children's slots,
  collecting `VariableValue` references, deduping by name, erroring on
  type collision ([`pkg/op/subgraph.go:69`](../../../../pkg/op/subgraph.go)).
  Recurse into nested subgraphs by calling their already-deduped
  `Parameters()`.
- `Node.Parameters()` requires no change — `Bind` already populates it
  from `method.Parameters()` (full method signature per D3's asymmetric
  contract).

**Ownership pointer (forward-compat for D11):**

- Add `parent *Subgraph` field to `executableUnit`
  ([`pkg/op/executable_unit.go`](../../../../pkg/op/executable_unit.go)).
- Add `(s *Subgraph) AddChild(child ExecutableUnit)` method to
  `pkg/op/subgraph.go` that appends to the unexported `children` slice AND
  stamps the parent pointer on the child's underlying `executableUnit`.
- Migrate the two direct `g.Root.Children = append(...)` sites in
  [`pkg/op/graph.go:121`](../../../../pkg/op/graph.go) and `:129` to use
  the new `Subgraph.AddChild`. No other call sites today.
- The Branch flag on Subgraph is **removed** in 13.0(n) (see D11);
  zero consumers in the codebase.

**Tests** under `pkg/op/subgraph_test.go`:

- Single Node child with one VariableValue → Parameters yields one entry.
- Multiple children with the same Variable name (same Type) → dedup to
  one.
- Same Variable name across two children with different Types → plan-time
  error.
- Nested subgraph contributes its bubble-up to the parent's surface
  (recursion).
- Slot fills of `ImmediateValue` / `PromiseValue` do NOT contribute to
  Parameters() (they don't demand external values).
- `AddChild` stamps `parent` on the child's underlying executableUnit.
- Parent pointer survives a roundtrip through nested subgraphs.

### Phase 3.5 — Parent-pointer to parent-ID refactor + bridge kwarg classification (DONE)

Landed across commits `92e119b` → `58308a3` ("Phase 4.5" working label).
Mechanical refactor of Phase 3's pointer landing to the by-ID model that
serialization (D8 plan.save / plan.load) requires, bundled with the
bridge-side kwarg classification that produces `parentID` values at .star
dispatch time.

**`executableUnit` refactored** ([`pkg/op/executable_unit.go`](../../../../pkg/op/executable_unit.go)):

- `parent *Subgraph` → `parentID string` (line 47).
- Added `retryPolicy *RetryPolicy` and `errorAction *Subgraph` fields
  (D5 / D11 settlement — every executable unit carries both; errorAction
  field type narrowed from `ExecutableUnit` to `*Subgraph` in Phase 5.3b).
- `ParentID()` returns the bare string; callers needing the Subgraph
  reference do their own lookup via `*Graph.SubgraphByID`.
- `stampParent` is idempotent — matching ID succeeds silently; a
  conflicting non-empty ID panics with a descriptive message.

**Bridge kwarg classification** ([`pkg/op/starlarkbridge/container.go`](../../../../pkg/op/starlarkbridge/container.go)):

For container methods, classifies each starlark kwarg:

- `body=` → `Subgraph.children` via `AddChild`; stamps each child's
  `parentID` to the calling subgraph's ID.
- `items=` → `Subgraph.Items` (as a SlotValue for late dispatch-time resolution).
- `error_action=` → `executableUnit.errorAction`.
- `retry_policy=` → `executableUnit.retryPolicy`.
- (anything else) → `Subgraph.FrameBindings` (frame-binding kwargs).

Extraction helpers (`extractBodyKwarg`, `extractErrorActionKwarg`,
`extractRetryPolicyKwarg`, `extractItemsKwarg`) each filter the matched
kwarg out of the tuple slice before returning.

**Subgraph.Parameters() filters local FrameBindings** (commit `58308a3`):
the bubble-up walk deletes any name appearing in `Subgraph.FrameBindings`
so locally-bound variables don't propagate up to the caller's surface.

**Bypass sites migrated:**

- `pkg/op/marshal.go` — `applySubgraphChildrenPayloads` replays children
  through `AddChild`; `subgraphChildPayload` discriminated union (Node /
  Subgraph) replaces the flat ID lists.
- `pkg/op/graph.go` — `Graph.AddNode` / `Graph.AddSubgraph` both route
  through `Root.AddChild`.
- `cmd/lore/lore/builder.go` — `addNativePMNodes` adds directly to the
  target subgraph via `AddChild` (commit `92e119b`).
- `cmd/devlore-test/devloretest/runner.go` — was not actually building
  graph structure directly (it borrows the env via
  `g.RuntimeEnvironment()` and lets the bridge produce nodes); no
  migration needed.

**Tests:**

- `pkg/op/subgraph_test.go` covers `AddChild` parent stamping, nested
  ownership, type-collision panic, nested bubble-up, and the four
  FrameBindings cases (full-bind, partial-bind, nested-hides-from-outer,
  empty-map-no-op).
- `pkg/op/starlarkbridge/container_test.go` covers each extraction helper:
  absent / list-of-invocations / None / non-list / non-invocation-element
  for body=, absent / target / None for error_action=, absent for
  retry_policy=, plus two `starlarkValueToSlotValue` cases.

**Container roster gap (Phase 5):** `containerMethods` currently registers
only `"flow.Subgraph"`. `flow.Choose`, `flow.Gather`, and `flow.WaitUntil`
need to be added when Phase 5 materializes their per-type dispatch.

### Phase 4 — Preflight validation (DONE)

Landed at [`pkg/op/executor.go:175`](../../../../pkg/op/executor.go)
(`bindVariables`):

- Runs before `ResolvePending` in `GraphExecutor.Run`.
- Reads the env's [op.VariableResolver] (`env.variableResolver`, wired by
  `NewRuntimeEnvironment` from `spec.Application`); calls
  `resolver.Resolve(env, graph.Parameters())`; folds errors via
  `errors.Join` into the D5 envelope and fails the run before any dispatch.
- Merges `resolver.Variables()` into `env.variables`; layers caller-supplied
  `callerVariables` on top as the highest-priority override (test injection
  surface).
- Mirrors `env.variables` into `e.variables` so [Node.ResolveSlots] sees
  the same map.
- **Runs in every mode, including dry-run** (per D10). Variable resolution
  is pure (reads in-memory `Application` maps and the process env; no
  filesystem or network probes); dry-run output that renders slot values
  needs them resolved. This diverges intentionally from `ResolvePending`'s
  convention (which skips dry-run because catalog resolution touches
  target-machine state).
- Full integration test green requires Phase 5 (the `.star` tests need
  materialized graph nodes from `plan.run` to dispatch against).

### Phase 4.6 — Wire-format refactor + free-func-to-method cleanup (DONE)

Single commit between Phase 4 and Phase 5. Two coupled changes: β-strict
containment on the wire (in-memory representation unchanged), and a
pkg/op convention pass that converts single-struct-arg free functions to
methods on that struct.

**β-strict wire form** (in-memory unchanged):

- [Subgraph] marshals `children` as `[]string` of unit IDs.
- [Graph] marshals top-level flat `subgraphs []*Subgraph` and
  `nodes []*Node` lists, both sorted by ID for stable output.
- `subgraphChildPayload` discriminated union and its helpers
  (`toSubgraphChildrenPayloads`, `applyChildPayloads`) DELETED.
- Unmarshal builds [Graph.unitsByID] symbol table from the flat lists;
  each Subgraph's `pendingChildren []string` is resolved through
  [Subgraph.linkChildren] (calling [Subgraph.AddChild], which re-stamps
  parentIDs from the containment relationship); every Edge endpoint is
  validated against [Subgraph.ChildByID] via [Subgraph.validateEdges].
  Dangling references aggregate to `errors.Join`.

**Subgraph by-ID index:**

- New unexported `childrenByID map[string]ExecutableUnit`; maintained
  in lockstep with `children` by [Subgraph.AddChild]. Sole-mutator
  invariant preserved (both fields unexported).
- New public accessors: [Subgraph.Children] and [Subgraph.ChildByID].
- New unexported `pendingChildren []string` stash for unmarshal-time
  child IDs awaiting Graph-level resolution.

**Free-func-to-method conversion in pkg/op:**

- `collectNodes(children, *out)` → [Subgraph.descendantNodes].
- `findSubgraph(children, id)` → [Subgraph.descendantSubgraphByID].
- `sortChildren(children, edges)` — DELETED. Only caller
  (`executor.executeChildren`) was already dead code itself.
- `topologicalSortChildren(children, edges)` →
  [Subgraph.topologicallySortedChildren].
- `childIDs(children)` (introduced during the wire-format work) →
  [Subgraph.childIDs].

Sort moved out of `descendantNodes` / `descendantSubgraphs` — those now
return pure tree-walk order. [Graph.marshalPayload] and
[Graph.CanonicalContent] sort by ID inline before assigning to the wire
payload.

`executor.executeChildren` deleted alongside the free `sortChildren`.
Phase 5 rebuilds dispatch end-to-end alongside the [executeSubgraph]
rewrite.

**Doc accuracy fix:** [Graph.Serialize]'s comment previously claimed
"the checksum is computed before encoding" — false; it's a thin
`enc.Encode(g)` wrapper. Doc rewritten: [Graph.Checksum] is emitted
as-is, and callers refresh via [Graph.CanonicalContent] before calling
if they want a fresh checksum.

**Tests:**

- [`pkg/op/marshalers_test.go`](../../../../pkg/op/marshalers_test.go)
  (new) — round-trip suite covering an adopt-shaped graph (two adopt
  subgraphs with mkdir → move → link each), YAML and JSON variants,
  plus rejection tests for dangling child IDs and dangling Edge
  endpoints.
- `cmd/devlore-test/devloretest/data/test_round_trip_writ_adopt.star`
  (new) — Phase 1 fixture matching the existing `test_writ_adopt*.star`
  pattern. Parses today; live assertions (`plan.assemble` /
  `plan.save` / `plan.load` / `t.expect_graph_equal`) sit in comments
  waiting for Phase 5.

**Checksum migration:** None. Greenfield project; previously-
checksummed receipts become unverifiable.

**Source-file rename:** `pkg/op/marshal.go` → `pkg/op/marshalers.go`
(and `marshal_test.go` → `marshalers_test.go`) for consistency with
`interfaces.go` / `helpers.go` naming.

### Phase 5 — Planner-dispatch model + Tier-3 plan methods

> **Phase 5 redesign (2026-05-19):** The original Phase 5 design rooted
> dispatch in [`starlarkbridge.NodeBuilder`] with an `options=` kwarg
> wrapper and a `containerMethods` runtime table. Both are retired. The
> replacement, captured here, routes every plan-mode call through a
> single dispatch method on [op.plan.Provider] and selects per-method
> dispatch behavior via a [Planner] declared in each method's
> announcement metadata.

**Current state (entry point for the phase):**

- [`pkg/op/executor.go`](../../../../pkg/op/executor.go) —
  `executeSubgraph` is intentionally stripped and returns an error
  pointing at this phase. Per-dispatch [Frame] minting populated from
  `Subgraph.FrameBindings`, `Subgraph.Items` propagation, retry per the
  frame-chain `effectiveRetryPolicy(graph, unit) *RetryPolicy` helper,
  and `errorAction` dispatch on failure all land in the rewrite.
- [`pkg/op/invocation_registry.go`](../../../../pkg/op/invocation_registry.go)
  — the session ledger now lives in `pkg/op` (relocated from
  starlarkbridge). `plan.Provider` owns the instance; planners consume
  it via the [op.PlanInvocator] interface.
- [`pkg/op/planner.go`](../../../../pkg/op/planner.go) — `Planner`
  interface + `ActionPlanner` (default vanilla planner) + `plannerForType`
  resolver landed. `op.Plan` free function (Phase 1) sits in the same
  file.
- [`pkg/op/provider/flow/planners.go`](../../../../pkg/op/provider/flow/planners.go)
  — flow's four specialized planners: `ChoosePlanner`, `GatherPlanner`,
  `SubgraphPlanner` (filled), `WaitUntilPlanner`. The three unfilled
  bodies return "not implemented" sentinels.
- [`pkg/op/method.go`](../../../../pkg/op/method.go) — `MethodMetadata`
  struct (codegen-emitted: `ParameterNames []string`, `Planner reflect.Type`);
  `Method.planner` field + `Planner()` / `SetPlanner()` accessors.
- [`pkg/op/receiver_registry.go`](../../../../pkg/op/receiver_registry.go)
  — `AnnounceProvider` takes `map[string]MethodMetadata`; resolves each
  entry's `Planner reflect.Type` via `plannerForType` and threads through
  `NewProviderReceiverType` / `newReceiverType` to land on `*Method`.
- [`pkg/op/provider/plan/provider.go`](../../../../pkg/op/provider/plan/provider.go)
  — `ResolveAttr` returns nil (no-op) for Tier-1 (`plan.file`, etc.)
  and Tier-3 (`plan.assemble`, etc.) lookups until the adapter type
  and the Tier-3 method builtins land in Layer A. Tier 2 (root
  planners) is unchanged in shape.
- ~~`SetErrorAction` parentID-stamping gap~~ — closed in Phase 5.2.
  `error_action` field type narrowed from `ExecutableUnit` to `*Subgraph`
  in Phase 5.3b.
- `Subgraph.sortChildren()` exists but is not yet called from anywhere.
  Step 4 of the assemble pipeline below is its first caller.

**Dispatch model (the planner-shim approach).**

When starlark evaluates `plan.file.write_text(path="foo", content="bar",
label="my_write")`, the lookup walks:

1. `plan` → `*goReceiver` wrapping `plan.Provider`.
2. `.file` → `plan.Provider.ResolveAttr("file")` returns the adapter
   (one per sub-namespace, cached) — a tiny `starlark.HasAttrs` carrying
   two fields: a back-pointer to `plan.Provider` and the
   `op.ProviderReceiverType` for `file`. No business logic.
3. `.write_text` → `adapter.Attr("write_text")` returns a `starlark.Builtin`
   whose body calls back into `plan.Provider.Invocation(receiverType,
   "WriteText", args, kwargs, retryPolicy, errorAction, label)`.

`plan.Provider.Invocation` is the single dispatch method:

```go
func (p *Provider) Invocation(
    receiverType op.ProviderReceiverType,
    methodName   string,
    args         []any,
    kwargs       map[string]any,
    retryPolicy  *op.RetryPolicy,
    errorAction  *op.Subgraph,
    label        string,
) (*op.Invocation, error)
```

It looks up `*op.Method` from `receiverType.MethodByName(methodName)`,
delegates unit-shape construction to `method.Planner().Plan(...)`,
stamps `Label` / `RetryPolicy` / `ErrorAction` on the returned unit,
wraps it in an `*op.Invocation`, and registers via `p.invocations`.

**Planner contract.** Each `*op.Method` carries a `Planner` (declared in
`MethodMetadata.Planner` reflect.Type at announcement; resolved to its
singleton via `plannerForType`). Five planner types cover the whole
provider catalog:

| Planner | Methods | Unit kind |
|---|---|---|
| `op.ActionPlanner` (default) | every non-flow method + flow's leaf methods (`Complete`, `Degraded`, `Elevate`, `Failed`) | `*op.Node` |
| `flow.ChoosePlanner` | `flow.Choose` | `*op.Subgraph` (case fold) |
| `flow.GatherPlanner` | `flow.Gather` | `*op.Subgraph` (iteration with body resolution) |
| `flow.SubgraphPlanner` | `flow.Subgraph` | `*op.Subgraph` (kwarg classification: body=/items=/frame bindings) |
| `flow.WaitUntilPlanner` | `flow.WaitUntil` | `*op.Subgraph` (polling cadence) |

**Reserved kwargs.** Every plan-mode call accepts three reserved kwargs
flat (no `options=` wrapper):

| Kwarg | Type | Effect |
|---|---|---|
| `label=` | `string` | Becomes `Invocation.Label`; empty triggers `registry.AutoLabel("<provider>.<method>")`. |
| `retry_policy=` | `*op.RetryPolicy` | Stamped on the unit's `executableUnit.retryPolicy`. |
| `error_action=` | starlark list of invocations, materialized to `*op.Subgraph` by `subgraphFromInvocations` (plan-side helper) | Stamped on the unit's `executableUnit.errorAction` per Phase 5.2/5.3b. |

The adapter's per-method builtin extracts these by name before passing
the remaining kwargs to `plan.Provider.invocation`.

**Shared assemble pipeline** (single function consumed by every
Tier-3 method that takes `body=`). Parent-stamping during `.star`
evaluation is the
reachability mark — flow's `SubgraphPlanner.Plan` calls `AddChild` on
each `body=` element, stamping each child's `parentID` to the enclosing
Subgraph's ID. No separate reachability walk is needed; the orphan scan
in step 5 tests the post-assemble invariant that every registered
Invocation's Target has a non-empty `parentID`.

1. **Root the body= children.** For each Invocation in `body=`, call
   `graph.Root.AddChild(inv.Target)`. `AddChild` stamps
   `parentID = "root"` on the Target. Targets already carrying a
   non-empty `parentID` are a nesting error.
2. **Frame setup.** Reserved kwargs (`retry_policy=` / `error_action=`)
   stamp `graph.Root`'s fields via `SetRetryPolicy` / `SetErrorAction`.
   Remaining kwargs populate `graph.Root.FrameBindings`.
3. **Edge materialization.** For each Subgraph reachable from
   `graph.Root` (parentID chain terminates at `"root"`), walk each child
   unit's slots and emit an edge on the enclosing Subgraph's `Edges`
   list for every PromiseValue NodeRef and Resource `producerID`.
4. **Topological sort.** Call `Subgraph.sortChildren()` on each
   reachable Subgraph so its `children` slice is ordered per the Edges
   produced in step 3.
5. **Orphan scan.** Linear pass over the `op.InvocationRegistry`: any
   Invocation whose `Target.ParentID()` is empty is an orphan. Aggregate
   via `errors.Join` and return `(nil, err)` from `plan.assemble`.

**Tier-3 method wrappers:**

- `plan.assemble(body=[…], …kwargs)` → run shared pipeline; on success
  return the constructed Graph, on orphan-error return `(nil, err)`.
- `plan.run(body=[…], …kwargs)` → shared pipeline → executor.Run(graph)
  → return graph's return value.
- `plan.run(graph)` → executor.Run(graph) directly (skip materialization).
- `plan.save(graph, path)` → serialize graph to path; return None.
- `plan.load(path)` → deserialize; return Graph.
- `plan.clear()` → purge `op.InvocationRegistry`, reset
  parent-stamping state. Does NOT touch user-held Graphs. Does NOT
  trigger orphan detection.

**Tests:**

- `pkg/op/` unit coverage for: `ActionPlanner.Plan` (slot fill, default
  application, projection of `*Invocation` / `*Promise` / `*Variable` to
  SlotValue variants); `flow.SubgraphPlanner.Plan` (kwarg classification
  for body= / items= / frame bindings); `plannerForType` resolution.
- `pkg/op/provider/plan/` unit coverage for `plan.Provider.Invocation`
  (Method lookup, planner delegation, label / retry / errorAction
  stamping, invocation registration, label collision).
- The seven `.star` integration tests under
  `cmd/devlore-test/devloretest/data/test_writ_adopt*.star` start
  passing end-to-end (the planner-shim path produces real graph nodes;
  the executor's preflight + dispatch run against them).
- Round-trip test: `plan.save` then `plan.load` produces an equivalent
  Graph (same op.Nodes, same edges, same Children order).

**Pending work — next session entry point.**

The planner-dispatch foundation (D12 components) and the assert
refactor landed in commits through `refactor(13.0(n) Phase 5):
MethodMetadata + AnnounceProvider rewrite; plumb planner onto
*op.Method; assert.{Nil,NotNil} generic`. Remaining work to make
Tier-3 calls + container dispatch actually run:

**In-flight state at session boundary (2026-05-19):**

Across the working tree, ahead of any commits since `5503da9`:

- `pkg/op/planner.go` + `cmd/devlore-test/devloretest/runner.go` —
  `op.Plan`'s closure signature flipped to
  `func(*RuntimeEnvironment) (*Graph, error)`; runner.go updated to
  match.
- `pkg/op/subgraph.go` + `pkg/op/marshalers.go` — `Subgraph.Branch`
  field removed (zero consumers); marshal / unmarshal payload field
  gone.
- `pkg/op/executable_unit.go` — Layer A step 1 done: `ExecutableUnit`
  interface gained `SetRetryPolicy(*RetryPolicy)` and
  `SetErrorAction(*Subgraph)`. Also reflects the parallel panic →
  `assert.*` sweep (stampParent now `assert.Truef`; `fmt` import
  dropped).
- `pkg/op/promise.go` + `pkg/op/provider/flow/planners.go` — Layer A
  step 2 done: `Promise` now holds `ExecutableUnit` (was `*Node`);
  `NewPromise(unit ExecutableUnit, slot string)`; `Node()` accessor
  renamed `Unit()`; slot-shaped reads in `Path` / `Attr` / `AttrNames`
  type-assert to `*Node`. flow.projectKwargValue caller updated.
- `pkg/op/starlarkbridge/go_converters.go` (new) +
  `pkg/op/starlarkbridge/go_receiver.go` (slimmed) +
  `pkg/op/starlarkbridge/go_converters_test.go` (merged) +
  `pkg/op/starlarkbridge/starlark_to_go_typed_test.go` (deleted) —
  Layer A step 3 done: starlark→Go converters consolidated into
  go_converters.go; `starlarkToGoTyped` capitalized to
  `StarlarkToGoTyped`; the standalone test file merged into
  go_converters_test.go.
- `pkg/op/starlarkbridge/go_receiver.go` — one-file style refactor
  ride-along: Parameters/Returns blocks on every method, receiver
  `w` → `g`, `dispatch` signature wrapped, `NoSuchAttrError` and
  friends moved into `region UNEXPORTED FUNCTIONS`,
  `rt` → `receiverType` in `NewProvider`.
- `docs/plans/extract-starlark-from-op/phase-8/13.0-n.md` — design
  edits from this session: Tier-3 terminology throughout, D7
  single-session note, D8 `plan.run` requires `plan.assemble`'s
  Graph, D11 / D12 `error_action=[...]` list grammar (no auto-wrap),
  Layer A renumbered for dependency-respecting order.

Layer A steps 1, 2, 3 are complete in tree. Step 4 (the unexported
`(p *Provider) invocation(...)` dispatch on `plan.Provider`) is the
next code task.

**Settled this session (2026-05-19) — do not relitigate:**

- The five Tier-3 methods — `Assemble`, `Run`, `Save`, `Load`,
  `Clear` — are plain methods on `plan.Provider`, exposed as
  starlark builtins under the `plan` namespace. Same shape as the
  existing `(p *Provider) Variable(name, defaultValue any) *op.Variable`:
  **NOT codegen-routed**, NO `MethodMetadata` entry, NO `Planner`
  indirection, NO `provider.gen.go` hand-edit.
- **No state stash on `plan.Provider`.** The assembled graph is
  `Assemble`'s return value; not a field on the provider. An
  `AssembledGraph()` accessor pulling from provider-side state is
  rejected.
- **`plan.run(...)` inside the `.star` script is the execution
  entry point.** The runner no longer pulls an assembled graph
  and calls `executor.Run` after the script finishes; execution
  happens inside the script via `plan.run`. The runner's job
  reduces to: evaluate the script for its side effects, surface
  errors, route receipts / dry-run / trace from env-side
  collectors.
- **`plan.Provider` carries an `env *op.RuntimeEnvironment`
  back-pointer**, set at provider construction. Every Tier-3
  method needs the env (`Assemble` for `graph.Rebind`; `Run` to
  dispatch; `Save` / `Load` for ctx + receipt routing).
- **`plan.run` reuses the planning env for execution** (single env,
  single session). D7's two-session model (planning env + executor
  env) remains the convention for Go-side callers that use
  `op.Plan` + `op.GraphExecutor` directly (writ adopt et al.).
  When starlark drives execution in-script via `plan.run`, the
  planning env doubles as the execution env.
- **Receipt / dry-run / trace routing.** `plan.run` writes to
  env-side collectors (env owns the receipt collector / output
  sink / trace channel); the runner reads them after the script
  ends.
- **`plan.Provider.Run` and `op.GraphExecutor.Run` coexist.** The
  provider method is the in-script entry; the executor is the
  Go-side entry for callers driving execution from outside a
  starlark session.
- **`Assemble` carries `retry_policy` / `error_action` /
  frame-binding metadata** per D8, not just invocations.
- **Reserved-kwarg auto-wrap** (single-Node / single-`*Invocation`
  `error_action=` shape → one-child `*op.Subgraph`) happens in
  `SplitReservedKwargs` at extraction time so the downstream
  signature is uniformly `*op.Subgraph`.

**Operational notes for the next session:**

- **Codegen cannot be re-run.** `build/star` and `build/star.lkg`
  are both absent; `make generate` would attempt to build star
  from source, and star's own packages currently can't build
  without the generated files. Layer A as revised does **not**
  require any `*.gen.go` hand-edits — the five Tier-3 methods are
  plain provider methods, not codegen-routed. (The codegen-cannot-run
  constraint still applies to any future Tier-1 method addition
  that needs a fresh `MethodMetadata` entry.)

- **Currently-red tests** (knowingly documented in the last commit;
  these are the ones expected to go green after Layer A + Layer B):
  - `cmd/devlore-test`: `TestCLI_ScriptFirst`, `TestCLI_ScriptMiddle`,
    `TestCLI_ScriptLast`, `TestCLI_DefaultAllToStdout`,
    `TestCLI_SummaryOnly`, `TestCLI_GraphOnly`,
    `TestCLI_ReceiptOnlyYAML`, `TestCLI_ReceiptOnlyJSON`,
    `TestCLI_RoutToFiles`, `TestCLI_JSONReceiptToFile`,
    `TestCLI_DryRun`, `TestCLI_Trace`, `TestCLI_Silent`.
  - `cmd/devlore-test/devloretest`: `TestRunCmd_BasicExecution`,
    `TestRunCmd_DryRun`, `TestRunCmd_Trace`, `TestRunCmd_ReceiptJSON`,
    `TestRunCmd_OutputRouting`.
  - `cmd/star/star`: `TestSourceFile_StarlarkIntegration`,
    `TestLintCopyright_*` (all variants).

- **`.star` files needing a Tier-3 wrap.** 63 of 64 files under
  `cmd/devlore-test/devloretest/data/` lack any Tier-3 call
  (`plan.run` / `plan.assemble`). The 64th
  (`test_round_trip_writ_adopt.star`) has `plan.assemble`
  references in comments waiting for Phase 5. Layer A step 11
  audits each file: side-effect / behavior-verification tests
  wrap in `plan.run([...])`; graph-structure / round-trip /
  harness-assertion tests wrap in `plan.assemble([...])`. Use
  `grep -L "plan\.\(run\|assemble\)" cmd/devlore-test/devloretest/data/*.star`
  to list the gap.

**Sequencing note.** The 18-step sequence below is the live work
order for 13.0(n) completion. Step boundaries are commit boundaries —
each step is exactly one commit, no mid-execution subdivision.
Discoveries are inserted as new numbered steps; reordering renumbers
the list. The ordering below is dependency-respecting: step N never
references a symbol that step N+1 introduces.

**Layer A — Tier-3 leaf path (gets `test_hello.star` and other
leaf-only `.star` tests green):**

1. Expand `ExecutableUnit` interface with `SetRetryPolicy(*RetryPolicy)`
   and `SetErrorAction(*Subgraph)`. Both Node and Subgraph already
   carry these methods via embedded `executableUnit`; the interface
   addition promotes them to interface-visible so step 4 can stamp
   the reserved-kwarg payload without a `*Node` / `*Subgraph`
   type-switch.

2. Broaden `Promise` to hold `ExecutableUnit` instead of `*Node`.
   Update `NewPromise(unit ExecutableUnit, slot string) *Promise`;
   internal `p.node` references become `p.unit`. `FillSlot`,
   `Project`, `String` adjust accordingly. Required by step 4
   (which wraps planner-returned units in `*op.Invocation` carrying
   a `*op.Promise`) and by Layer B (flow planners that produce
   `*Subgraph` targets).

3. Restructure `pkg/op/starlarkbridge` converters and export
   `StarlarkToGoTyped`. The starlark→Go converter cluster
   (`toGoInto`, `toGoMap`, `toGoSlice`, `toGoStruct`, `toGo`,
   `toNaturalGo`, `starlarkToGoTyped`) moves out of `go_receiver.go`
   into a new `go_converters.go`; the standalone test file
   `starlark_to_go_typed_test.go` merges into `go_converters_test.go`;
   the only function capitalized for export is
   `StarlarkToGoTyped(env *op.RuntimeEnvironment, sv starlark.Value, target reflect.Type) (any, error)`.
   Reserved-kwarg classification (`splitReservedKwargs`) and the
   `error_action=` materialization (`subgraphFromInvocations`) are
   plan-provider semantics, not bridge primitives — they land in
   step 6 inside `pkg/op/provider/plan/helpers.go`, unexported.
   `ArgsToGo` / `KwargsToGo` are dropped: the single generic-shape
   caller (the adapter) inlines a short loop over
   `StarlarkToGoTyped`; typed callers use `starlark.UnpackArgs` plus
   per-arg `StarlarkToGoTyped`.

4. Add unexported method on plan.Provider:
   `(p *Provider) invocation(receiverType op.ProviderReceiverType,
   methodName string, args []any, kwargs map[string]any,
   retryPolicy *op.RetryPolicy, errorAction *op.Subgraph,
   label string) (*op.Invocation, error)`. Body: look up `*op.Method`
   via `receiverType.MethodByName(methodName)`; call
   `method.Planner().Plan(p, receiverType, method, args, kwargs)`;
   stamp RetryPolicy / ErrorAction on the returned unit via the
   interface methods from step 1; build `*op.Invocation` (Target =
   unit, Result = `NewPromise(unit, "")` using the broadened type
   from step 2, Label = resolved); register via
   `p.invocations.Register`. Also add `(p *Provider) InvocationRegistry()
   *op.InvocationRegistry` accessor so `plan.Provider` satisfies
   `op.PlanInvocator`.

5. Add the five Tier-3 methods on `plan.Provider` plus the
   `NewGraphExecutorForEnv` constructor on `*op.GraphExecutor`.
   Plain Go methods, no codegen routing, no MethodMetadata, no
   Planner:

   ```go
   func op.NewGraphExecutorForEnv(env *RuntimeEnvironment) *GraphExecutor

   func (p *Provider) Assemble(
       invocations   []*op.Invocation,
       retryPolicy   *op.RetryPolicy,
       errorAction   *op.Subgraph,
       frameBindings map[string]op.SlotValue,
   ) (*op.Graph, error)

   func (p *Provider) Run(graph *op.Graph) (any, error)
   func (p *Provider) Save(graph *op.Graph, path string) error
   func (p *Provider) Load(path string) (*op.Graph, error)
   func (p *Provider) Clear() error
   ```

   - `Assemble`: allocate `op.NewGraph()`,
     `graph.Rebind(p.RuntimeEnvironment())`,
     `graph.Root.AddChild(inv.Target)` per invocation, stamp
     retryPolicy / errorAction on root via `SetRetryPolicy` /
     `SetErrorAction`, populate `Root.FrameBindings` from
     `frameBindings`, materialize edges from PromiseValue NodeRef
     and Resource producerID, topologically sort reachable
     Subgraphs, orphan-scan the registry; return graph or
     `(nil, errors.Join(...))`.
   - `Run`: construct a borrowed executor via
     `op.NewGraphExecutorForEnv(p.RuntimeEnvironment())` and delegate
     to `executor.Run(graph, nil)`. Preflight, dispatch, and
     receipt collection all happen inside the borrowed
     `GraphExecutor.Run` — `plan.Provider.Run` adds no logic.
   - `NewGraphExecutorForEnv`: borrows the env; its `Close` is a
     no-op (env ownership stays with the surrounding `op.Plan`
     lifecycle).
   - `Save` / `Load`: thin wrappers around `op.Graph` marshal /
     unmarshal.
   - `Clear`: `p.invocations.Reset()`; clear any parent-stamp
     ledger.

6. Add the adapter type at `pkg/op/provider/plan/adapter.go` for
   Tier-1 routing (`plan.<provider>.<method>`) and wire
   `plan.Provider.ResolveAttr` end-to-end. Adapter: two fields
   (`provider *Provider`, `receiverType op.ProviderReceiverType`);
   implements `starlark.Value` + `starlark.HasAttrs`; `AttrNames()`
   returns sorted snake_case method names from
   `receiverType.Methods()`; `Attr(snake)` matches against
   `op.CamelToSnake(method.Name())` and returns a `starlark.Builtin`
   whose body splits reserved kwargs (step 3 helpers), converts
   args/kwargs, and calls `provider.invocation(...)` (step 4).
   `ResolveAttr` wiring:
   - Tier 1 (`plan.file`, `plan.shell`, etc.): lazy-mint adapters,
     cache in `adapters map[string]*adapter` field on the Provider.
   - Tier 2 (root-planner methods — `plan.choose`, etc.): unchanged.
   - Tier 3 (`plan.variable`, `plan.assemble`, `plan.run`,
     `plan.save`, `plan.load`, `plan.clear`): return the matching
     Tier-3 builtin (the builtins themselves land in step 7).

7. Add bridge-side starlark builtins for the five Tier-3 methods,
   exposed under the `plan` namespace (sibling to the existing
   `plan.variable` builtin). One builtin per method; each unpacks
   args/kwargs via the step 3 helpers, splits reserved kwargs, and
   calls the matching `plan.Provider` method from step 5.

8. Finish `cmd/devlore-test/devloretest/runner.go`. The closure
   signature was flipped to `func(*RuntimeEnvironment) (*Graph, error)`
   pre-step-1; remove the post-Plan `executor.Run` path entirely —
   execution moves into the `.star` script via `plan.run`. The
   runner reduces to: load + evaluate the script, surface errors,
   read env-side receipt / trace / dry-run output. The
   `executor.Run` Go-side path remains for callers that don't go
   through a `.star` script (Phase 6 writ adopt).

9. Audit the 64 `.star` files under
   `cmd/devlore-test/devloretest/data/`. For each, decide:
   side-effect / behavior test → wrap in `plan.run([...])`;
   graph-structure / round-trip / harness-assertion test →
   wrap in `plan.assemble([...])`. `test_hello.star` is the
   canonical first example: it expects shell side effects, so
   `plan.run([plan.shell.exec(command='echo "Hello World!"')])`.
   Use `grep -L "plan\.\(run\|assemble\)" cmd/devlore-test/devloretest/data/*.star`
   to list the gap.

**Layer B — Subgraph unification (gets container `.star` tests green):**

10. Move `Receiver string` and `method *Method` fields from `*Node`
    onto the shared `executableUnit` base so both Node and Subgraph
    carry them.
11. Add `(*Subgraph).Bind(method *Method)` mirroring Node.Bind:
    populates `parameters` from `method.Parameters()` and rebuilds
    slot index.
12. Collapse `Subgraph.Items`, `Subgraph.FrameBindings`, the variadic
    Cases (for choose), and `do`/`limit`/`target`/`predicate`/etc.
    into the Subgraph's slot map. The flow.Provider.<Kind> methods
    read these from slot values at execute time.
13. Replace `executor.executeSubgraph`'s hardcoded dispatch with
    `unit.Action()` lookup through the registry — same path Node uses.
    The existing dispatch logic becomes the body of
    `flow.Provider.Subgraph` (and similarly for Choose/Gather/WaitUntil
    where applicable).
14. Fill in `flow.ChoosePlanner.Plan`, `flow.GatherPlanner.Plan`,
    `flow.WaitUntilPlanner.Plan`. Each constructs a `*op.Subgraph`,
    calls `subgraph.Bind(method)` against the matching flow method,
    fills slots from the call's kwargs, returns. `flow.SubgraphPlanner.Plan`
    (already partially filled) updates to use Bind + slot fill instead
    of the current FrameBindings/Items field writes.
15. Marshaler updates: `Subgraph` now has a `Receiver` field; wire
    format gains it. Removed fields (`Items`, `FrameBindings`, etc.)
    fold into the slot list, which serializes the same way Node's does.
16. Update container `.star` integration tests
    (`TestCLI_*`, `TestRunCmd_*`, `TestSourceFile_StarlarkIntegration`,
    `TestLintCopyright_*`) to call `plan.assemble(...)` around their
    invocation lists where missing.

**Phase 6 — writ adopt migration:**

17. Extract `cmd/writ/writ/adopt_cmd.go` + `cmd/writ/writ/adopt/`
    subpackage (`adopt.go`, `plan.go`, `execute.go`) from the
    monolithic `commands.go`.
18. Rewrite the three nil-activation call sites in `runAdopt`'s
    helper `adoptFile` (`commands.go:1406` Mkdir, `:1416` Move,
    `:1428` Link) using VariableValue-slotted nodes wired into a
    graph; the executor's preflight (D10) resolves variables from
    `env.Application` before dispatch.

**Verify-as-you-go:**

After Layer A: `test_hello.star` and other leaf-only `.star` tests
green. Subgraph-using tests remain red.

After Layer B: all `.star` integration tests green.

**Verbatim design decisions from the Phase 5 redesign session
(2026-05-18)** (do not relitigate):

- The HasAttrs returned by `plan.Provider.ResolveAttr` is called the
  **adapter**, never "shim."
- plan.assemble is **explicit** — `.star` authors must call it; no
  auto-materialization on Invocation registration.
- Method-level planner declaration, not provider-level. flow's leaf
  methods (Complete/Degraded/Elevate/Failed) use ActionPlanner.
- Each container kind binds its own action (`flow.Subgraph`,
  `flow.Choose`, `flow.Gather`, `flow.WaitUntil`) — no single
  "subgraph action" handles all containers.
- ExecutableUnit interface expands to include `SetRetryPolicy` and
  `SetErrorAction`.
- Promise broadens to hold ExecutableUnit, not just *Node.
- Subgraph is a unit that holds an action; the long-term target is
  one ExecutableUnit type (collapsing Node + Subgraph), but that
  collapse is gated on the field-to-slot migration in Layer B.

### Phase 6 — `writ adopt` migration

The three call sites in `runAdopt`'s helper `adoptFile`
([`cmd/writ/writ/commands.go`](../../../../cmd/writ/writ/commands.go)):

| Line | Method | Slots |
|---|---|---|
| 1406 | `fp.Mkdir(nil, destDir, 0o755, "")` | path, mode |
| 1416 | `fp.Move(nil, &file.Resource{SourcePath: …}, destPath)` | source, destination |
| 1428 | `fp.Link(nil, &file.Resource{SourcePath: …}, filePath)` | source, target |

**Each writ adopt CLI argument is realized as an [op.VariableValue] slot.**
No user-controlled input lives in an [op.ImmediateValue]. The cobra-parsed
flags land in `Application.Flags` under their snake-cased names; the
executor's preflight pass resolves them through the cascade and fills
`env.variables`; dispatch populates the slots at run time.

The package layout follows the existing `migrate_cmd.go` + `migrate/`
precedent (six other commands in `cmd/writ/writ/` already separate cobra
glue from logic via the same shape):

```
cmd/writ/writ/
  adopt_cmd.go     ← NEW. newAdoptCmd, runAdopt, AdoptConfig
                     (extracted from commands.go).
  adopt/           ← NEW subpackage.
    adopt.go       ← AdoptFiles, AdoptItem
                     (per-file iteration scaffolding).
    plan.go        ← BuildGraph(env, src, dest, mode) — the op.Plan closure
                     factored out for isolated testability. Each adopt
                     argument enters as op.VariableValue{Name: "..."}.
    execute.go     ← Run(executor, graph) wraps executor.Run with
                     adopt-specific error mapping.
```

`adoptFile` (currently `commands.go:1400+`) becomes:

```go
graph, err := op.Plan(ctx, spec, func(env *op.RuntimeEnvironment) (*op.Graph, error) {
    return adopt.BuildGraph(env, …)
})
if err != nil { return err }
return adopt.Run(executor, graph)
```

`adopt.BuildGraph` constructs three nodes (mkdir → move → link) on the graph's `Root`
with VariableValue slots for `dest_dir`, `source_path`, `dest_path`, and
ImmediateValue only for non-user constants (file modes the tool sets
itself, hard-coded backup suffixes, etc.).

`executor.Run(graph, nil)` — the executor's preflight resolves variables
from `env.Application` (built from the cobra command's flags) and
populates `env.variables` before dispatch.

`commands.go` shrinks by ~500 lines (the full adopt surface moves out).
The full `cmd/writ/writ/command/` subpackage normalization for the
remaining cobra commands (deploy, decommission, upgrade, reconcile) is a
separate layout-only PR after 13.0(n) closes.

### Phase 7 — Deferred (writ migrate + file provider cleanup)

NOT part of 13.0(n). Tracked here as the immediate follow-on so the sequence
is documented.

- Migrate the 5 writ migrate sites (`migrate_cmd.go:292/302/310/320`,
  `migrate/execute.go:81`) to the same pattern as Phase 6.
- Grep `pkg/op/provider/file/provider.go` for nil-activation defensive
  branches. Delete them.

This phase lands in a separate, smaller PR after 13.0(n) closes.

## Test strategy

### Starlark-first integration tests

Phase 1 authored seven `.star` tests before the supporting infrastructure
existed. The tests parse and the harness runs them end-to-end; they execute
against an empty graph today (Phase 1) and against a real materialized graph
once Phase 5 lands. From Phase 6 onward, the tests become regression guards
for the writ adopt migration.

### Go unit coverage

Each new Go type / function in `pkg/op` (binding layer) and the modified
slot machinery gets unit tests. These cover edges the integration test
cannot reach economically — `camelToSnake` edge cases, panic-on-pre-Resolve,
multi-root collision detection, default-type validation against parameter
type, env-string parsing per target type.

### Writ-side coverage

Phase 6 adds a writ-side test for the graph-construction helper. The
behavioral test for `writ adopt` continues to pass — same observable
filesystem behavior, different internal path.

### Coverage matrix

| Scenario | `.star` integration | Go unit | Writ-side |
|---|---|---|---|
| Single-graph mkdir → move → link | ✓ |  | ✓ |
| Missing required parameter | ✓ | ✓ |  |
| Type mismatch | ✓ | ✓ |  |
| Origin namespace assertion | ✓ | ✓ |  |
| Origin full string assertion | ✓ | ✓ |  |
| Source precedence | ✓ | ✓ |  |
| Default-type mismatch at preflight |  | ✓ |  |
| Subgraph bubble-up | ✓ | ✓ |  |
| Subgraph multi-root collision |  | ✓ |  |
| `Get` panic on pre-`Resolve` |  | ✓ |  |
| Dry-run skips parameter preflight | ✓ | ✓ |  |
| `writ adopt` real-path execution |  |  | ✓ |
| `Variable.String()` format |  | ✓ |  |
| `VariableSource.String()` format |  | ✓ |  |
| `plan.run` reachability walk |  | ✓ |  |
| Invocation-registry freeze post-`plan.run` |  | ✓ |  |

## Locked design decisions

Decisions taken during plan finalization (2026-05-15) and locked in the
sections above:

1. **`camelToSnake` rule for env keys.** Greedy acronym (Go-idiomatic):
   consecutive uppercase letters form a single acronym; an acronym ends
   when a lowercase letter follows. `URLPath → URL_PATH`, `targetRoot →
   TARGET_ROOT`. Mixed acronyms (`IPv4Address → I_PV4_ADDRESS`) are
   tolerated. Documented under D5 step 3.
2. **`Application.Flags` key form.** Snake-case, normalized at
   [application.NewApplication] time. `dry-run → dry_run`,
   `target-root → target_root`. The resolver matches the parameter name
   verbatim. Framework code reading specific flags
   ([application.Application.DryRun]) likewise reads the snake-case key.
   Documented under D5 step 2.
3. **Env-string parsing strategy.** Private `envValue` type implements
   [op.SourceConverter]; the resolver wraps raw env strings before calling
   [op.Convert]. The cascade picks up `envValue` at step 5; the wrapper
   handles primitives via the shared `Kind` switch with
   `parseDefaultExpression`, then stdlib special-cases (`time.Duration`,
   `os.FileMode`), then [encoding.TextUnmarshaler], then `json.Unmarshal`
   as the universal fallback. `envValue.CanConvertTo` returns false for
   [op.Resource] targets so step 7 (registered Resource construction)
   wins routing. [op.Convert] itself is not modified — the codebase's
   prior layering decision at [`pkg/op/parameter.go:236`](../../../../pkg/op/parameter.go)
   stands. Documented under D5.
4. **Phase 6 layout.** `cmd/writ/writ/adopt_cmd.go` + `cmd/writ/writ/adopt/`
   subpackage (with `adopt.go`, `plan.go`, `execute.go`) matching the
   existing `migrate_cmd.go` + `migrate/` precedent (and six other
   subpackages: `identity/`, `reconcile/`, `segment/`, `snapshot/`,
   `tree/`). Documented under Phase 6.
5. **Each writ adopt CLI argument is an [op.VariableValue] slot.** No
   user-controlled input lives in an [op.ImmediateValue]. Documented
   under Phase 6.
6. **Execution model — stack frames + subgraph ownership + per-type
   executors.** Every subgraph invocation (root, plain composition,
   gather body iteration, choose branch, wait_until polling cycle)
   creates a frame carrying its own `Variables` overlay, `Results` map,
   and `Attempts` history. Frames isolate state for concurrent
   invocations of the same subgraph template. Subgraphs own their
   children via an explicit `parent *Subgraph` pointer; per-type
   executors (root / gather / choose / wait_until / plain) dispatch
   their own children's frames. The legacy `Subgraph.Branch` flag is
   **removed** in 13.0(n) — it had no consumers — leaving dispatch
   ownership to the parent-pointer model. Documented under D11.
   13.0(n) Phase 3 lands the parent pointer for forward-compat; full
   frame plumbing and the per-type executors are Step 13/14/15 work.

7. **`Node.Parameters()` vs `Subgraph.Parameters()` — asymmetric by type.**
   `Node.Parameters()` returns the full Go-method signature (every
   declared parameter, regardless of slot fill). `Subgraph.Parameters()`
   returns the bubble-up variable surface (the deduped, type-checked set
   of `VariableValue` references walked across children's slots, recursing
   into nested subgraphs). Both answer "what's the input surface?" using
   the most natural representation for the type. `Parameters()` stays on
   the `ExecutableUnit` interface; no separate `UnboundSlots()` /
   `BubbleUp()` accessor — variables ARE the bubble-up. Documented under
   D3.

## Deferred to phase

These decisions remain open and will be locked when the relevant phase
opens, not now:

- **`plan.run` surface.** The starlark builtin shape (argument form,
  implicit-vs-explicit invocation, integration with flow-control
  combinators) — Phase 5 design decision. Writ adopt is Go-side and does
  not exercise `plan.run`; this question is scoped to .star authors and
  to the seven integration tests in `test_writ_adopt*.star`.

## Files affected

### Modified (Phase 1, landed)

- [`pkg/op/slot.go`](../../../../pkg/op/slot.go) — sealed-three update;
  signature change; `EnvironmentValue` and `Properties` deleted.
- [`pkg/op/graph.go`](../../../../pkg/op/graph.go) — `NewGraph` argless;
  `Rebind` / `Unbind`; `RuntimeEnvironment()` accessor.
- [`pkg/op/executor.go`](../../../../pkg/op/executor.go) — `NewGraphExecutor`
  takes `(ctx, spec)`; `Close`; `Environment`; `Run(graph, variables)`.
- [`pkg/op/runtime_environment.go`](../../../../pkg/op/runtime_environment.go)
  — session-scoped owner doc; `Close` sync.Once; `RegisterParameter`;
  `VariableByName`; `declaredParameters` / `variables` / `variableResolver`
  fields; `Data` / `Property` deleted; `ProgramName` deleted (Application.Name
  is now the single source of truth).
- [`pkg/op/runtime_environment_spec.go`](../../../../pkg/op/runtime_environment_spec.go)
  — `assert.NotNil("spec.Application", …)`; `WithApplication`.
- [`pkg/op/activation_record.go`](../../../../pkg/op/activation_record.go) —
  `Graph` field added; executor stamps `node.graph`.
- [`pkg/op/provider/plan/provider.go`](../../../../pkg/op/provider/plan/provider.go)
  — `Variable(name, defaultValue any) *op.Variable`.
- [`pkg/op/provider/flow/provider.go`](../../../../pkg/op/provider/flow/provider.go)
  — `Graph` field removed; reads from `ActivationRecord.Graph`.
- [`pkg/op/starlarkbridge/runtime.go`](../../../../pkg/op/starlarkbridge/runtime.go)
  — `NewRuntime(env)` (single typed arg); modules + registry pulled from
  `env.Registry`.
- [`pkg/op/starlarkbridge/task_builder.go`](../../../../pkg/op/starlarkbridge/task_builder.go)
  — bridge plumbs the borrowed env through invocation registration.
- [`cmd/lore/lore/builder.go`](../../../../cmd/lore/lore/builder.go) —
  prepareScriptEnv builds spec → env explicitly; passes env to bridge.
- [`cmd/star/star/application.go`](../../../../cmd/star/star/application.go)
  — Application gains `env *op.RuntimeEnvironment`; `Close` delegates to
  `env.Close`.
- [`cmd/star/main.go`](../../../../cmd/star/main.go) — binds flags before
  `star.NewApplication`; `PersistentPreRunE` calls `runtime.Refresh(cmd)`.
- [`cmd/star/provider/{config,setup,goast,commands}/provider.go`](../../../../cmd/star/provider)
  — migrated to `RegisterParameter` + `VariableByName`.
- [`cmd/devlore-test/devloretest/runner.go`](../../../../cmd/devlore-test/devloretest/runner.go)
  — `Start` uses `op.Plan(ctx, spec, fn)`; closure pulls env via
  `g.RuntimeEnvironment()` and constructs the bridge from it.
- [`star/extensions/com.noblefactor.devlore.Actions/templates/receiver_type.gen_test.go.template`](../../../../star/extensions/com.noblefactor.devlore.Actions/templates/receiver_type.gen_test.go.template)
  — env shape updated for `Application` + `Flags["dry-run"]`.
- All `*/gen/receiver_type.gen_test.go` files regenerated.

### Modified (Phases 2 / 3 / 3.5 / 4, landed)

- [`pkg/op/variable_resolver.go`](../../../../pkg/op/variable_resolver.go)
  — real `Resolve(env, parameters)` cascade with `envValue` env-string
  parsing and Resource-target opt-out (Phase 2).
- [`pkg/op/env_value.go`](../../../../pkg/op/env_value.go) — `envValue`
  type, `SourceConverter` implementation, primitives / `time.Duration` /
  `os.FileMode` / `TextUnmarshaler` / `json.Unmarshal` strategies
  (Phase 2).
- [`pkg/op/helpers.go`](../../../../pkg/op/helpers.go) — `CamelToSnake`
  with the greedy-acronym rule (Phase 2).
- [`pkg/op/subgraph.go`](../../../../pkg/op/subgraph.go) — bubble-up
  `Parameters()` graph-walk with FrameBindings filter; unexported
  `children` slice; `AddChild` as the sole mutator; `FrameBindings`,
  `Items` fields (Phase 3 + 3.5).
- [`pkg/op/executable_unit.go`](../../../../pkg/op/executable_unit.go) —
  `parentID string`, `retryPolicy *RetryPolicy`, `errorAction
  ExecutableUnit`; `stampParent` idempotency; `ParentID` /
  `RetryPolicy` / `SetRetryPolicy` / `ErrorAction` / `SetErrorAction`
  accessors (Phase 3.5).
- [`pkg/op/graph.go`](../../../../pkg/op/graph.go) — `Graph.AddNode` /
  `Graph.AddSubgraph` route through `Root.AddChild` (Phase 3.5);
  `SubgraphByID`, `ResolveExecutable` (Phase 3.5).
- [`pkg/op/marshalers.go`](../../../../pkg/op/marshalers.go) (renamed from
  `marshal.go` in Phase 4.6) — Phase 3.5 introduced the
  `subgraphChildPayload` discriminated union; Phase 4.6 replaced it with
  the β-strict wire form (top-level flat `subgraphs` / `nodes` lists +
  `Subgraph.Children []string`).
- [`pkg/op/starlarkbridge/container.go`](../../../../pkg/op/starlarkbridge/container.go)
  — `dispatchContainer`, `extractBodyKwarg`, `extractErrorActionKwarg`,
  `extractRetryPolicyKwarg`, `extractItemsKwarg`,
  `starlarkValueToSlotValue`; `containerMethods` map (Phase 3.5; choose /
  gather / wait_until entries still pending in Phase 5).
- [`pkg/op/executor.go`](../../../../pkg/op/executor.go) — `bindVariables`
  preflight pass (Phase 4); `executeSubgraph` stripped to a
  Phase-5-pending error (Phase 3.5 cleanup).
- [`cmd/lore/lore/builder.go`](../../../../cmd/lore/lore/builder.go) —
  `addNativePMNodes` adds directly to the target subgraph via `AddChild`
  (Phase 3.5).

### Modified (Phase 4.6, landed)

- [`pkg/op/subgraph.go`](../../../../pkg/op/subgraph.go) — `childrenByID`
  parallel index + `pendingChildren` unmarshal stash; new accessors
  `Children` / `ChildByID`; new unexported helpers `childIDs`,
  `descendantNodes`, `descendantSubgraphs`, `descendantSubgraphByID`,
  `topologicallySortedChildren`; `sortChildren` rewritten to delegate.
- [`pkg/op/graph.go`](../../../../pkg/op/graph.go) — `unitsByID` symbol
  table field; `Nodes` / `SubgraphByID` collapsed to one-liners on the
  new Subgraph methods; `CanonicalContent` rewritten to the new wire
  shape (inline sort); deleted free functions `collectNodes`,
  `findSubgraph`, `sortChildren`, `topologicalSortChildren`;
  `Graph.Serialize` doc fixed (false checksum claim).
- [`pkg/op/marshalers.go`](../../../../pkg/op/marshalers.go) — full
  rewrite to symbol-table wire form; new `subgraphPayload` named type;
  `applyPayload` returns error and runs `linkChildren` + `validateEdges`;
  `Graph.marshalPayload` sorts inline.
- [`pkg/op/executor.go`](../../../../pkg/op/executor.go) — deleted
  `executeChildren` (its only caller and only callee both vanished
  in this phase).

### Modified (Phases 5–6, pending)

- `pkg/op/executor.go` — full `executeSubgraph` rewrite (frame minting,
  FrameBindings application, Items propagation, errorAction dispatch).
- `pkg/op/provider/plan/provider.go` — `env *RuntimeEnvironment`
  back-pointer field (set at provider construction); `Invocation`
  dispatch method (Method lookup → planner delegation → label /
  retry / errorAction stamping → invocation registration); the five
  Tier-3 methods (`Assemble`, `Run`, `Save`, `Load`, `Clear`); the
  adapter type at `adapter.go` for Tier-1 `plan.<provider>.<method>`
  lookup; Tier-3 starlark builtins under the `plan` namespace.
- `pkg/op/provider/flow/planners.go` — fill `ChoosePlanner`,
  `GatherPlanner`, `WaitUntilPlanner` (currently stubbed); use the
  session invocation registry where body resolution is needed
  (GatherPlanner.`do`).
- `pkg/op/invocation_registry.go` — `Reset()` method for `plan.clear`.
- `cmd/writ/writ/commands.go` — `runAdopt`'s helper rewrites (Phase 6).
- `cmd/devlore-test/devloretest/test_context.go` — `t.set_*` /
  `t.expect_variable*` builtins; `t.expect_graph_equal` (Phase 5).

### New (Phase 5, landed)

- [`pkg/op/method.go`](../../../../pkg/op/method.go) — `MethodMetadata`
  struct (`ParameterNames []string`, `Planner reflect.Type`); `Method.planner`
  field with `Planner()` / `SetPlanner()` accessors.
- [`pkg/op/planner.go`](../../../../pkg/op/planner.go) — `Planner`
  interface, `PlanInvocator` interface, `ActionPlanner` (default vanilla
  planner with filled `Plan`), `plannerForType` reflect-based resolver.
- [`pkg/op/provider/flow/planners.go`](../../../../pkg/op/provider/flow/planners.go)
  — `ChoosePlanner`, `GatherPlanner`, `SubgraphPlanner`,
  `WaitUntilPlanner`. `SubgraphPlanner.Plan` filled (body= / items= /
  frame-binding classification); other three return "not implemented".
- [`pkg/op/invocation.go`](../../../../pkg/op/invocation.go),
  [`pkg/op/invocation_registry.go`](../../../../pkg/op/invocation_registry.go),
  [`pkg/op/promise.go`](../../../../pkg/op/promise.go) — relocated from
  `pkg/op/starlarkbridge/` so `pkg/op` owns the invocation ledger and
  promise type directly. `Invocation` is now a plain Go struct
  (Target, Result, Label, FillSlot) with no hand-rolled starlark.Value
  surface.

### Removed (Phase 5)

- `pkg/op/starlarkbridge/task_builder.go` (NodeBuilder), `options.go`
  (Options), `container.go` (dispatchContainer + containerMethods table
  + extractOptionsKwarg / extractRetryPolicyKwarg / etc.), `var.go`,
  `plan_pipeline_test.go`. Replaced by the planner-shim model on
  `plan.Provider` + method-level `Planner` declaration.
- 15× `pkg/op/provider/*/gen/node_builder.gen_test.go` — generated
  surface-area tests for the deleted NodeBuilder.
- `plan.Provider.Options` method + `plan.options(...)` starlark builtin.
  Reserved kwargs (`label`, `retry_policy`, `error_action`) now flat on
  every plan-mode call.

### New (Phase 1, landed)

- [`pkg/op/variable.go`](../../../../pkg/op/variable.go) — `Variable`,
  `VariableSource`, `VariableSourceKind`.
- [`pkg/op/variable_resolver.go`](../../../../pkg/op/variable_resolver.go) —
  `VariableResolver` skeleton.
- [`pkg/op/planner.go`](../../../../pkg/op/planner.go) — `op.Plan` free
  function.
- [`pkg/op/starlarkbridge/var.go`](../../../../pkg/op/starlarkbridge/var.go)
  + [`var_test.go`](../../../../pkg/op/starlarkbridge/var_test.go) — bridge
  plumbing for `plan.variable`.
- [`pkg/application/application.go`](../../../../pkg/application/application.go)
  — `Application` + `NewApplication` + `DryRun`.
- [`pkg/assert/assert.go`](../../../../pkg/assert/assert.go) — `Nil` /
  `NotNil` helpers.
- [`cmd/devlore-test/devloretest/data/test_writ_adopt*.star`](../../../../cmd/devlore-test/devloretest/data)
  (seven plans).
- [`cmd/devlore-test/devloretest/helpers.go`](../../../../cmd/devlore-test/devloretest/helpers.go).

### New (Phases 2 / 3 / 3.5 / 4, landed)

- [`pkg/op/variable_resolver_test.go`](../../../../pkg/op/variable_resolver_test.go)
  — resolver cascade unit coverage.
- [`pkg/op/env_value_test.go`](../../../../pkg/op/env_value_test.go) —
  env-string parsing per target type.
- [`pkg/op/subgraph_test.go`](../../../../pkg/op/subgraph_test.go) —
  bubble-up dedup, type-collision panic, nested recursion / dedup,
  FrameBindings full-bind / partial / nested-hides / empty-map,
  `AddChild` parent-stamp coverage.
- [`pkg/op/starlarkbridge/container_test.go`](../../../../pkg/op/starlarkbridge/container_test.go)
  — kwarg-classification coverage for body / error_action / retry_policy
  extraction and `starlarkValueToSlotValue`.

### New (Phase 4.6, landed)

- [`pkg/op/marshalers_test.go`](../../../../pkg/op/marshalers_test.go)
  — round-trip suite (YAML + JSON) over an adopt-shaped graph (two
  adopt subgraphs of mkdir → move → link); dangling-child and
  dangling-edge rejection tests.
- [`cmd/devlore-test/devloretest/data/test_round_trip_writ_adopt.star`](../../../../cmd/devlore-test/devloretest/data/test_round_trip_writ_adopt.star)
  — Phase 1 fixture; live assertions wait for Phase 5.

### Renamed (Phase 4.6, landed)

- `pkg/op/marshal.go` → `pkg/op/marshalers.go`
- `pkg/op/marshal_test.go` → `pkg/op/marshalers_test.go`

### New (Phases 5–6, pending)

- Writ-side adopt helper file (`cmd/writ/writ/adopt/plan.go` per Phase 6
  layout).
- Bridge-side reachability walk + materialization pipeline tests under
  `pkg/op/starlarkbridge/`.
- Starlark `Graph` wrapper type in `pkg/op/starlarkbridge` + harness
  `t.expect_graph_equal` builtin — needed for the Phase 4.6
  `test_round_trip_writ_adopt.star` fixture to go live.

### Deleted (Phase 1, landed)

- `op.EnvironmentValue` (slot variant in `pkg/op/slot.go`).
- `op.Properties` (interface).
- `op.RuntimeEnvironment.Data` (field).
- `op.RuntimeEnvironment.Property` (method).
- `pkg/binding/*` — the prior session's skeleton; types moved to `pkg/op`.

### Deferred to follow-on PR (Phase 7 — writ migrate)

- `cmd/writ/writ/migrate_cmd.go` — 4 nil-activation sites.
- `cmd/writ/writ/migrate/execute.go` — 1 nil-activation site.
- `pkg/op/provider/file/provider.go` — defensive nil-activation paths.

## Related plans and docs

- Parent: [phase-8.md](../phase-8.md)
- Slot model history: [phase-7.md](../phase-7.md) §"Fork F — DataRef under
  the sealed SlotValue"
- Architecture (data flow & slot resolution):
  [docs/architecture/2.1-typed-slots.md](../../../architecture/2.1-typed-slots.md)
- Catalog state machine precedent (Pending/Active/Gone, catalog-owned
  transitions): [phase-8.md](../phase-8.md) §13.0(k) k.13
- Preflight architecture (pass aggregation, D5 envelope): parent plan
  [extract-starlark-from-op.md](../../extract-starlark-from-op.md) Step 16 row

## Subsequent work

After 13.0(n) closes:

- **Follow-on PR (immediate next):** Phase 7 above. Migrate the 5 remaining
  writ migrate nil-activation sites; delete the file provider's defensive
  nil-activation paths. Smaller PR, normal cadence.
- **Step 13:** `plan.choose` successor design — branches as subgraphs with
  their own parameter surfaces and conditional edges. Bubble-up
  infrastructure makes the redesign tractable.
- **Step 14:** `plan.gather` successor design — fan-out as subgraph-per-item
  with per-iteration parameter binding.
- **Step 15:** `plan.wait_until` successor design — polling subgraph with
  condition node.
- **Step 16 (residual):** `plan.load` / `plan.save`. `plan.run` itself moved
  into 13.0(n) Phase 5; the load/save surface stays Step 16.

13.0(n)'s monster-PR exception is a one-time concession driven by the
structural coupling of the slot-model change. The follow-on writ-migrate PR
and Steps 13–16 return to the small-PR cadence the `CLAUDE.md` rules call
for.
