# SPDX-License-Identifier: MIT
# Copyright Noble Factor. All rights reserved.

extension: com.noblefactor.devlore.Knowledge
description: Devlore knowledge management - extract, index, validate, and sign knowledge artifacts

commands:
  - name: devlore.knowledge.extract
    help: Extract knowledge artifacts from devlore-cli source
    implementation: commands/extract.star
    flags:
      - name: source
        type: string
        default: ""
        help: "Path to devlore-cli (default: ../devlore-cli)"
      - name: target
        type: string
        default: ""
        help: "Output root for knowledge artifacts (default: ../devlore-registry)"
      - name: domain
        type: string
        default: "all"
        help: "Knowledge domain: all, onboarding, migration, or ops"

  - name: devlore.knowledge.index
    help: Generate index.yaml for knowledge domains
    implementation: commands/index.star
    flags:
      - name: target
        type: string
        default: ""
        help: "Knowledge root to read/write indexes (default: ../devlore-registry)"
      - name: dry_run
        type: string
        default: ""
        help: Show what would be done without writing files

  - name: devlore.knowledge.sign
    help: Generate and sign knowledge index
    implementation: commands/sign.star
    flags:
      - name: target
        type: string
        default: ""
        help: "Knowledge root to read/write (default: ../devlore-registry)"
      - name: env
        type: string
        default: "dev"
        help: "Environment: dev or prod"

  - name: devlore.knowledge.validate
    help: Validate knowledge artifact YAML files against JSON schemas
    implementation: commands/validate.star
    flags:
      - name: target
        type: string
        default: ""
        help: "Knowledge root to validate (default: ../devlore-registry)"
