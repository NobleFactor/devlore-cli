// SPDX-License-Identifier: SSPL-1.0
// Copyright (c) 2025-2026 Noble Factor. All rights reserved.

// Code generated by go.render; DO NOT EDIT.

package {{.package}}_test

import (
	"testing"

	"github.com/NobleFactor/devlore-cli/pkg/op"
	"github.com/NobleFactor/devlore-cli/pkg/op/bind"
{{- if .provider_import}}
	_ "{{.provider_import}}/gen"
{{- end}}
)

func TestProviderNodeBuilder_AttrNames(t *testing.T) {

	graph := &op.Graph{}
	p := bind.NewNodeBuilder(providerReceiverType(t), graph)
	names := p.AttrNames()
	if len(names) == 0 {
		t.Fatal("AttrNames() returned empty list")
	}
	for i := 1; i < len(names); i++ {
		if names[i] <= names[i-1] {
			t.Errorf("AttrNames() not sorted: %q <= %q at index %d", names[i], names[i-1], i)
		}
	}
}

func TestProviderNodeBuilder_Attr(t *testing.T) {

	graph := &op.Graph{}
	p := bind.NewNodeBuilder(providerReceiverType(t), graph)
	for _, name := range p.AttrNames() {
		attr, err := p.Attr(name)
		if err != nil {
			t.Errorf("Attr(%q) error: %v", name, err)
			continue
		}
		if attr == nil {
			t.Errorf("Attr(%q) = nil, want non-nil", name)
		}
	}
}

func TestProviderNodeBuilder_Attr_Unknown(t *testing.T) {

	graph := &op.Graph{}
	p := bind.NewNodeBuilder(providerReceiverType(t), graph)
	_, err := p.Attr("nonexistent_method")
	if err == nil {
		t.Error("Attr(nonexistent_method) error = nil, want error")
	}
}

func TestProviderNodeBuilder_Type(t *testing.T) {

	graph := &op.Graph{}
	p := bind.NewNodeBuilder(providerReceiverType(t), graph)
	if got := p.Type(); got != "plan.{{.provider}}" {
		t.Errorf("Type() = %q, want %q", got, "plan.{{.provider}}")
	}
}

func TestProviderNodeBuilder_String(t *testing.T) {

	graph := &op.Graph{}
	p := bind.NewNodeBuilder(providerReceiverType(t), graph)
	if got := p.String(); got != "plan.{{.provider}}" {
		t.Errorf("String() = %q, want %q", got, "plan.{{.provider}}")
	}
}
