// Code generated by go.generate; DO NOT EDIT.

package file

import (
	"fmt"

	"go.starlark.net/starlark"

	"github.com/NobleFactor/devlore-cli/pkg/op"
)

func init() {
	op.RegisterBinding(&op.ProviderBinding{
		Name:     "file",
		Access:   op.AccessBoth,
		Lifetime: op.LifetimeStateless,
		PlannedFactory: func(graph *op.Graph, project string, reg *op.ActionRegistry) starlark.Value {
			return NewFilePlan(graph, project, reg)
		},
	})
}

type FilePlan struct {
	op.Receiver
	graph   *op.Graph
	project string
	reg     *op.ActionRegistry
}

func NewFilePlan(graph *op.Graph, project string, reg *op.ActionRegistry) *FilePlan {
	return &FilePlan{
		Receiver: op.NewReceiver("plan.file"),
		graph:    graph,
		project:  project,
		reg:      reg,
	}
}

func (p *FilePlan) Attr(name string) (starlark.Value, error) {
	switch name {
	case "backup":
		return op.MakeAttr("plan.file.backup", p.backup), nil
	case "copy":
		return op.MakeAttr("plan.file.copy", p.copy), nil
	case "link":
		return op.MakeAttr("plan.file.link", p.link), nil
	case "move":
		return op.MakeAttr("plan.file.move", p.move), nil
	case "remove":
		return op.MakeAttr("plan.file.remove", p.remove), nil
	case "remove_all":
		return op.MakeAttr("plan.file.remove_all", p.remove_all), nil
	case "unlink":
		return op.MakeAttr("plan.file.unlink", p.unlink), nil
	case "write_bytes":
		return op.MakeAttr("plan.file.write_bytes", p.write_bytes), nil
	case "write_text":
		return op.MakeAttr("plan.file.write_text", p.write_text), nil
	case "glob":
		return op.MakeAttr("plan.file.glob", p.glob), nil
	case "mkdir":
		return op.MakeAttr("plan.file.mkdir", p.mkdir), nil
	case "read":
		return op.MakeAttr("plan.file.read", p.read), nil
	default:
		return nil, op.NoSuchAttrError("plan.file", name)
	}
}

func (p *FilePlan) AttrNames() []string {
	return []string{"backup", "copy", "glob", "link", "mkdir", "move", "read", "remove", "remove_all", "unlink", "write_bytes", "write_text"}
}

// backup Backup moves the file at "path" to a timestamped backup location.
//
// Returns:
//   - The backup path and compensation state.
//
// Slots:
//   - path: Absolute path to the file to back up
//   - backup_suffix: Suffix appended before the timestamp (default: .devlore-backup)
func (p *FilePlan) backup(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var path, backupSuffix starlark.Value
	if err := starlark.UnpackArgs("backup", args, kwargs, "path", &path, "backup_suffix", &backupSuffix); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-backup"),
		Action:  p.reg.MustGet("file.backup"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "path", path); err != nil {
		return nil, fmt.Errorf("path: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "backup_suffix", backupSuffix); err != nil {
		return nil, fmt.Errorf("backup_suffix: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// copy Copy writes content to the file at "path" with the given mode.
//
// Returns:
//   - result: the path to the copied file
//   - undo: a state map with the following keys:
//     > path: the path to the copied file
//     > existed_before: true if the file already existed before
//     > previous_content: the content of the file before it was created if it existed
//     > previous_mode: the mode of the file before it was created if it existed
//     > written_checksum: the checksum of the copied file (if available)
//
// Slots:
//   - path: Absolute path where the file will be written
//   - mode: File permission bits (e.g., 0o644)
func (p *FilePlan) copy(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var path, mode starlark.Value
	if err := starlark.UnpackArgs("copy", args, kwargs, "path", &path, "mode", &mode); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-copy"),
		Action:  p.reg.MustGet("file.copy"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "path", path); err != nil {
		return nil, fmt.Errorf("path: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "mode", mode); err != nil {
		return nil, fmt.Errorf("mode: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// link Link creates a symlink at a path pointing to a source file.
//
// Idempotent: if the symlink already points correctly, it's a no-op.
//
// Returns:
//   - result: the path to the symlink
//   - undo: a state map with the following keys:
//     > path: the path to the symlink
//     > existed_before: true if the symlink already existed before
//     > previous_target: the target of the symlink before it was created if it existed
//
// Slots:
//   - source: Absolute path to the symlink target
//   - path: Absolute path where the symlink will be created
func (p *FilePlan) link(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var source, path starlark.Value
	if err := starlark.UnpackArgs("link", args, kwargs, "source", &source, "path", &path); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-link"),
		Action:  p.reg.MustGet("file.link"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "source", source); err != nil {
		return nil, fmt.Errorf("source: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "path", path); err != nil {
		return nil, fmt.Errorf("path: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// move Move moves a file from source to path using "os.Rename".
//
// Returns:
//   - result: the path to the moved file
//   - undo: a state map with the following keys:
//     > source: the source path of the move
//     > path: the destination path of the move
//     > written_checksum: the checksum of the moved file (if available)
//
// Slots:
//   - source: Absolute path to the source file
//   - destination: Absolute path to the destination file
func (p *FilePlan) move(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var source, destination starlark.Value
	if err := starlark.UnpackArgs("move", args, kwargs, "source", &source, "destination", &destination); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-move"),
		Action:  p.reg.MustGet("file.move"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "source", source); err != nil {
		return nil, fmt.Errorf("source: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "destination", destination); err != nil {
		return nil, fmt.Errorf("destination: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// remove Remove deletes the file at "path".
//
// If prune is true and pruneBoundary is set, empty parent directories are removed up to the boundary.
//
// Returns:
//   - result: the path to the deleted file
//   - compState: a state map with the following keys:
//     > path: the path to the deleted file
//     > content: the content of the deleted file (if available)
//     > mode: the file mode of the deleted file (if available)
//
// Slots:
//   - path: Absolute path to the file to delete
//   - prune: If true, remove empty parent directories after deletion
//   - prune_boundary: Stop pruning at this directory (prevents removing too much)
func (p *FilePlan) remove(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var path, prune, pruneBoundary starlark.Value
	if err := starlark.UnpackArgs("remove", args, kwargs, "path", &path, "prune", &prune, "prune_boundary", &pruneBoundary); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-remove"),
		Action:  p.reg.MustGet("file.remove"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "path", path); err != nil {
		return nil, fmt.Errorf("path: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "prune", prune); err != nil {
		return nil, fmt.Errorf("prune: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "prune_boundary", pruneBoundary); err != nil {
		return nil, fmt.Errorf("prune_boundary: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// remove_all RemoveAll removes the file at "path" and any children it contains.
//
// Returns:
//   - result: the path to the deleted file or directory
//   - undo: a state map with the following keys:
//     > path: the path to the deleted file or directory
//     > files_moved: the number of files moved to the recovery directory
//     > recovery_dir: the path to the recovery directory
//   - err: any error that occurred during the removal
//
// Slots:
//   - path: Absolute path to the file or directory to remove
//   - prune: If true, remove empty parent directories after deletion
//   - prune_boundary: Stop pruning at this directory (prevents removing too much)
func (p *FilePlan) remove_all(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var path, prune, pruneBoundary starlark.Value
	if err := starlark.UnpackArgs("remove_all", args, kwargs, "path", &path, "prune", &prune, "prune_boundary", &pruneBoundary); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-remove_all"),
		Action:  p.reg.MustGet("file.remove_all"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "path", path); err != nil {
		return nil, fmt.Errorf("path: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "prune", prune); err != nil {
		return nil, fmt.Errorf("prune: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "prune_boundary", pruneBoundary); err != nil {
		return nil, fmt.Errorf("prune_boundary: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// unlink Unlink removes the symlink at "path".
//
// If prune is true and pruneBoundary is set, empty parent directories are removed up to the boundary.
//
// Returns:
//   - result: the path to the deleted symlink
//   - compState: a state map with the following keys:
//     > path: the path to the deleted symlink
//     > target: the target of the deleted symlink
//
// Slots:
//   - path: Absolute path to the symlink to remove
//   - prune: If true, remove empty parent directories after unlinking
//   - prune_boundary: Stop pruning at this directory (prevents removing too much)
func (p *FilePlan) unlink(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var path, prune, pruneBoundary starlark.Value
	if err := starlark.UnpackArgs("unlink", args, kwargs, "path", &path, "prune", &prune, "prune_boundary", &pruneBoundary); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-unlink"),
		Action:  p.reg.MustGet("file.unlink"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "path", path); err != nil {
		return nil, fmt.Errorf("path: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "prune", prune); err != nil {
		return nil, fmt.Errorf("prune: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "prune_boundary", pruneBoundary); err != nil {
		return nil, fmt.Errorf("prune_boundary: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// write_bytes WriteBytes writes inline content to the file at "path" with the given mode.
//
// Returns:
//   - result: the path to the written file
//   - undo: a state map with the following keys:
//     > tombstone: a [Tombstone]
//   - err: any error that occurred while writing
//
// Slots:
//   - content: String content to write to the file
//   - mode: File permission bits (e.g., 0o644)
func (p *FilePlan) write_bytes(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var destination, content, mode starlark.Value
	if err := starlark.UnpackArgs("write_bytes", args, kwargs, "destination", &destination, "content", &content, "mode", &mode); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-write_bytes"),
		Action:  p.reg.MustGet("file.write_bytes"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "destination", destination); err != nil {
		return nil, fmt.Errorf("destination: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "content", content); err != nil {
		return nil, fmt.Errorf("content: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "mode", mode); err != nil {
		return nil, fmt.Errorf("mode: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// write_text WriteText writes inline content to the file at "path" with the given mode.
//
// Returns:
//   - result: the path to the written file
//   - undo: a state map with the following keys:
//     > tombstone: a [Tombstone]
//   - err: any error that occurred while writing
//
// Slots:
//   - content: String content to write to the file
//   - mode: File permission bits (e.g., 0o644)
func (p *FilePlan) write_text(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var destination, content, mode starlark.Value
	if err := starlark.UnpackArgs("write_text", args, kwargs, "destination", &destination, "content", &content, "mode", &mode); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-write_text"),
		Action:  p.reg.MustGet("file.write_text"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "destination", destination); err != nil {
		return nil, fmt.Errorf("destination: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "content", content); err != nil {
		return nil, fmt.Errorf("content: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "mode", mode); err != nil {
		return nil, fmt.Errorf("mode: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// glob Glob returns file paths matching a pattern relative to Root.
//
// Returns:
//   - []string: List of matching file paths
//
// Slots:
//   - pattern: Glob pattern (e.g., "*.go", "**/*.yaml")
//   - honor_gitignore: If true, filter results using gitignore rules
func (p *FilePlan) glob(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var pattern, honorGitignore starlark.Value
	if err := starlark.UnpackArgs("glob", args, kwargs, "pattern", &pattern, "honor_gitignore", &honorGitignore); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-glob"),
		Action:  p.reg.MustGet("file.glob"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "pattern", pattern); err != nil {
		return nil, fmt.Errorf("pattern: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "honor_gitignore", honorGitignore); err != nil {
		return nil, fmt.Errorf("honor_gitignore: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// mkdir Mkdir creates a directory (and parents) with the given mode.
//
// Returns:
//   - string: The absolute path of the created directory
//
// Slots:
//   - path: Absolute path of the directory to create
//   - mode: Directory permission bits (e.g., 0o755)
func (p *FilePlan) mkdir(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var path, mode starlark.Value
	if err := starlark.UnpackArgs("mkdir", args, kwargs, "path", &path, "mode", &mode); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-mkdir"),
		Action:  p.reg.MustGet("file.mkdir"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "path", path); err != nil {
		return nil, fmt.Errorf("path: %w", err)
	}
	if err := op.FillSlot(node, p.graph, "mode", mode); err != nil {
		return nil, fmt.Errorf("mode: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}

// read Read creates a Blob from the file at "path" for reading the contents of the file at "path".
//
// Returns:
//   - result: the contents of the file
//
// Slots:
//   - path: Absolute path to the file to read
func (p *FilePlan) read(_ *starlark.Thread, _ *starlark.Builtin, args starlark.Tuple, kwargs []starlark.Tuple) (starlark.Value, error) {
	var path starlark.Value
	if err := starlark.UnpackArgs("read", args, kwargs, "path", &path); err != nil {
		return nil, err
	}

	node := &op.Node{
		ID:      op.GenerateNodeID("file-read"),
		Action:  p.reg.MustGet("file.read"),
		Project: p.project,
	}
	if err := op.FillSlot(node, p.graph, "path", path); err != nil {
		return nil, fmt.Errorf("path: %w", err)
	}

	p.graph.Nodes = append(p.graph.Nodes, node)
	return op.NewOutput(node, p.graph, ""), nil
}
