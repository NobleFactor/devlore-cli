// Code generated by go.render; DO NOT EDIT.

package file_test

import (
	"bytes"
	"context"
	"strings"
	"testing"

	"github.com/NobleFactor/devlore-cli/pkg/op"
	filegen "github.com/NobleFactor/devlore-cli/pkg/op/provider/file/gen"
)

// ── Test Helpers ─────────────────────────────────────────────────────────────

func newCtx(t *testing.T) *op.Context {
	t.Helper()
	return &op.Context{
		ContextBase: op.ContextBase{
			Context: context.Background(),
			Writer:  &bytes.Buffer{},
		},
	}
}

func dryRunCtx(t *testing.T) *op.Context {
	t.Helper()
	return &op.Context{
		ContextBase: op.ContextBase{
			Context: context.Background(),
			DryRun:  true,
			Writer:  &bytes.Buffer{},
		},
	}
}

func makeRegistry(t *testing.T) *op.ActionRegistry {
	t.Helper()
	reg := op.NewActionRegistry()
	op.RegisterActions(reg, filegen.Receiver, filegen.Params)
	return reg
}

func getAction(t *testing.T, reg *op.ActionRegistry, name string) op.Action {
	t.Helper()
	a, ok := reg.Get(name)
	if !ok {
		t.Fatalf("action %q not registered", name)
	}
	return a
}

func getCompensable(t *testing.T, reg *op.ActionRegistry, name string) op.CompensableAction {
	t.Helper()
	a := getAction(t, reg, name)
	ca, ok := a.(op.CompensableAction)
	if !ok {
		t.Fatalf("action %q does not implement CompensableAction", name)
	}
	return ca
}

// ── Registration ─────────────────────────────────────────────────────────────

func TestActionNames(t *testing.T) {
	reg := makeRegistry(t)
	names := []string{
		"file.root",
		"file.backup",
		"file.copy",
		"file.link",
		"file.move",
		"file.remove",
		"file.remove_all",
		"file.unlink",
		"file.walk_tree",
		"file.write_bytes",
		"file.write_text",
		"file.exists",
		"file.glob",
		"file.is_dir",
		"file.is_file",
		"file.mkdir",
		"file.read",
		"file.join",
		"file.name",
		"file.parent",
	}
	for _, name := range names {
		a := getAction(t, reg, name)
		if got := a.Name(); got != name {
			t.Errorf("Name() = %q, want %q", got, name)
		}
	}
}

func TestRegister(t *testing.T) {
	reg := makeRegistry(t)
	expected := []string{
		"file.root",
		"file.backup",
		"file.copy",
		"file.link",
		"file.move",
		"file.remove",
		"file.remove_all",
		"file.unlink",
		"file.walk_tree",
		"file.write_bytes",
		"file.write_text",
		"file.exists",
		"file.glob",
		"file.is_dir",
		"file.is_file",
		"file.mkdir",
		"file.read",
		"file.join",
		"file.name",
		"file.parent",
	}
	for _, name := range expected {
		if _, ok := reg.Get(name); !ok {
			t.Errorf("action %q not registered", name)
		}
	}
}

// ── Dry-Run ──────────────────────────────────────────────────────────────────

func TestRootAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.root")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.root") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.root")
	}
}

func TestBackupAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.backup")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.backup") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.backup")
	}
}

func TestCopyAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.copy")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.copy") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.copy")
	}
}

func TestLinkAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.link")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.link") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.link")
	}
}

func TestMoveAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.move")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.move") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.move")
	}
}

func TestRemoveAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.remove")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.remove") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.remove")
	}
}

func TestRemoveAllAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.remove_all")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.remove_all") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.remove_all")
	}
}

func TestUnlinkAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.unlink")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.unlink") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.unlink")
	}
}

func TestWalkTreeAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.walk_tree")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.walk_tree") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.walk_tree")
	}
}

func TestWriteBytesAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.write_bytes")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.write_bytes") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.write_bytes")
	}
}

func TestWriteTextAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.write_text")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.write_text") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.write_text")
	}
}

func TestExistsAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.exists")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.exists") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.exists")
	}
}

func TestGlobAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.glob")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.glob") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.glob")
	}
}

func TestIsDirAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.is_dir")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.is_dir") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.is_dir")
	}
}

func TestIsFileAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.is_file")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.is_file") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.is_file")
	}
}

func TestMkdirAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.mkdir")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.mkdir") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.mkdir")
	}
}

func TestReadAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.read")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.read") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.read")
	}
}

func TestJoinAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.join")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.join") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.join")
	}
}

func TestNameAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.name")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.name") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.name")
	}
}

func TestParentAction_DryRun(t *testing.T) {
	reg := makeRegistry(t)
	action := getAction(t, reg, "file.parent")
	ctx := dryRunCtx(t)

	result, undo, err := action.Do(ctx, map[string]any{})
	if err != nil {
		t.Fatalf("Do() error = %v", err)
	}
	if result != nil {
		t.Errorf("dry-run result = %v, want nil", result)
	}
	if undo != nil {
		t.Errorf("dry-run undo = %v, want nil", undo)
	}

	output := ctx.Writer.(*bytes.Buffer).String()
	if !strings.Contains(output, "[dry-run] file.parent") {
		t.Errorf("dry-run output = %q, want to contain %q", output, "[dry-run] file.parent")
	}
}

// ── Compensable ──────────────────────────────────────────────────────────────

func TestBackupAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.backup")
}

func TestCopyAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.copy")
}

func TestLinkAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.link")
}

func TestMoveAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.move")
}

func TestRemoveAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.remove")
}

func TestRemoveAllAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.remove_all")
}

func TestUnlinkAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.unlink")
}

func TestWalkTreeAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.walk_tree")
}

func TestWriteBytesAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.write_bytes")
}

func TestWriteTextAction_CompensableInterface(t *testing.T) {
	reg := makeRegistry(t)
	_ = getCompensable(t, reg, "file.write_text")
}

func TestCompensableActions_UndoNil(t *testing.T) {
	reg := makeRegistry(t)
	ctx := newCtx(t)

	names := []string{
		"file.backup",
		"file.copy",
		"file.link",
		"file.move",
		"file.remove",
		"file.remove_all",
		"file.unlink",
		"file.walk_tree",
		"file.write_bytes",
		"file.write_text",
	}

	for _, name := range names {
		t.Run(name, func(t *testing.T) {
			ca := getCompensable(t, reg, name)
			if err := ca.Undo(ctx, nil); err != nil {
				t.Errorf("Undo(nil) = %v, want nil", err)
			}
		})
	}
}
