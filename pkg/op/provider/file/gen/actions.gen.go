// Code generated by go.generate; DO NOT EDIT.

package file

import (
	"fmt"
	"os"

	"github.com/NobleFactor/devlore-cli/pkg/op"
	provider "github.com/NobleFactor/devlore-cli/pkg/op/provider/file"
)

func init() {
	op.RegisterBinding(&op.ProviderBinding{
		Name:            "file",
		ActionRegistrar: Register,
	})
}

// Backup — Backup moves the file at "path" to a timestamped backup location.
type Backup struct{ Impl *provider.Provider }

func (o *Backup) Name() string { return "file.backup" }

func (o *Backup) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	path := slots["path"].(string)
	backupSuffix := slots["backup_suffix"].(string)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.backup %v %v\n", path, backupSuffix)
		return nil, nil, nil
	}

	result, state, err := o.Impl.Backup(path, backupSuffix)
	return result, state, err
}

func (o *Backup) Undo(_ *op.Context, state op.UndoState) error {
	if state == nil {
		return nil
	}
	return o.Impl.CompensateBackup(state.(map[string]any))
}

// Copy — Copy writes content to the file at "path" with the given mode.
type Copy struct{ Impl *provider.Provider }

func (o *Copy) Name() string { return "file.copy" }

func (o *Copy) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	path := slots["path"].(string)
	mode := slots["mode"].(os.FileMode)
	content, _ := slots["content"].([]byte)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.copy %v %v\n", path, mode)
		return nil, nil, nil
	}

	result, state, err := o.Impl.Copy(path, mode, content)
	if err != nil {
		return nil, nil, err
	}
	return result, state, nil
}

func (o *Copy) Undo(_ *op.Context, state op.UndoState) error {
	if state == nil {
		return nil
	}
	return o.Impl.CompensateCopy(state.(map[string]any))
}

// Link — Link creates a symlink at a path pointing to a source file.
type Link struct{ Impl *provider.Provider }

func (o *Link) Name() string { return "file.link" }

func (o *Link) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	source := slots["source"].(string)
	path := slots["path"].(string)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.link %v %v\n", source, path)
		return nil, nil, nil
	}

	result, state, err := o.Impl.Link(source, path)
	return result, state, err
}

func (o *Link) Undo(_ *op.Context, state op.UndoState) error {
	if state == nil {
		return nil
	}
	return o.Impl.CompensateLink(state.(map[string]any))
}

// Move — Move moves a file from source to path using "os.Rename".
type Move struct{ Impl *provider.Provider }

func (o *Move) Name() string { return "file.move" }

func (o *Move) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	source := slots["source"].(string)
	destination := slots["destination"].(string)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.move %v %v\n", source, destination)
		return nil, nil, nil
	}

	result, state, err := o.Impl.Move(source, destination)
	return result, state, err
}

func (o *Move) Undo(_ *op.Context, state op.UndoState) error {
	if state == nil {
		return nil
	}
	return o.Impl.CompensateMove(state.(map[string]any))
}

// Remove — Remove deletes the file at "path".
type Remove struct{ Impl *provider.Provider }

func (o *Remove) Name() string { return "file.remove" }

func (o *Remove) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	path := slots["path"].(string)
	prune := slots["prune"].(bool)
	pruneBoundary := slots["prune_boundary"].(string)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.remove %v %v %v\n", path, prune, pruneBoundary)
		return nil, nil, nil
	}

	result, state, err := o.Impl.Remove(path, prune, pruneBoundary)
	return result, state, err
}

func (o *Remove) Undo(_ *op.Context, state op.UndoState) error {
	if state == nil {
		return nil
	}
	return o.Impl.CompensateRemove(state.(map[string]any))
}

// RemoveAll — RemoveAll removes the file at "path" and any children it contains.
type RemoveAll struct{ Impl *provider.Provider }

func (o *RemoveAll) Name() string { return "file.remove_all" }

func (o *RemoveAll) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	path := slots["path"].(string)
	prune := slots["prune"].(bool)
	pruneBoundary := slots["prune_boundary"].(string)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.remove_all %v %v %v\n", path, prune, pruneBoundary)
		return nil, nil, nil
	}

	result, state, err := o.Impl.RemoveAll(path, prune, pruneBoundary)
	return result, state, err
}

func (o *RemoveAll) Undo(_ *op.Context, state op.UndoState) error {
	if state == nil {
		return nil
	}
	return o.Impl.CompensateRemoveAll(state.(map[string]any))
}

// Unlink — Unlink removes the symlink at "path".
type Unlink struct{ Impl *provider.Provider }

func (o *Unlink) Name() string { return "file.unlink" }

func (o *Unlink) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	path := slots["path"].(string)
	prune := slots["prune"].(bool)
	pruneBoundary := slots["prune_boundary"].(string)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.unlink %v %v %v\n", path, prune, pruneBoundary)
		return nil, nil, nil
	}

	result, state, err := o.Impl.Unlink(path, prune, pruneBoundary)
	return result, state, err
}

func (o *Unlink) Undo(_ *op.Context, state op.UndoState) error {
	if state == nil {
		return nil
	}
	return o.Impl.CompensateUnlink(state.(map[string]any))
}

// WriteBytes — WriteBytes writes inline content to the file at "path" with the given mode.
type WriteBytes struct{ Impl *provider.Provider }

func (o *WriteBytes) Name() string { return "file.write_bytes" }

func (o *WriteBytes) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	destination := slots["destination"].(string)
	content := slots["content"].(string)
	mode := slots["mode"].(os.FileMode)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.write_bytes %v %v %v\n", destination, content, mode)
		return nil, nil, nil
	}

	result, state, err := o.Impl.WriteBytes(destination, content, mode)
	return result, state, err
}

func (o *WriteBytes) Undo(_ *op.Context, state op.UndoState) error {
	if state == nil {
		return nil
	}
	return o.Impl.CompensateWriteBytes(state.(map[string]any))
}

// WriteText — WriteText writes inline content to the file at "path" with the given mode.
type WriteText struct{ Impl *provider.Provider }

func (o *WriteText) Name() string { return "file.write_text" }

func (o *WriteText) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	destination := slots["destination"].(string)
	content := slots["content"].(string)
	mode := slots["mode"].(os.FileMode)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.write_text %v %v %v\n", destination, content, mode)
		return nil, nil, nil
	}

	result, state, err := o.Impl.WriteText(destination, content, mode)
	return result, state, err
}

func (o *WriteText) Undo(_ *op.Context, state op.UndoState) error {
	if state == nil {
		return nil
	}
	return o.Impl.CompensateWriteText(state.(map[string]any))
}

// Glob — Glob returns file paths matching a pattern relative to Root.
type Glob struct{ Impl *provider.Provider }

func (o *Glob) Name() string { return "file.glob" }

func (o *Glob) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	pattern := slots["pattern"].(string)
	honorGitignore := slots["honor_gitignore"].(bool)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.glob %v %v\n", pattern, honorGitignore)
		return nil, nil, nil
	}

	result, err := o.Impl.Glob(pattern, honorGitignore)
	return result, nil, err
}

// Mkdir — Mkdir creates a directory (and parents) with the given mode.
type Mkdir struct{ Impl *provider.Provider }

func (o *Mkdir) Name() string { return "file.mkdir" }

func (o *Mkdir) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	path := slots["path"].(string)
	mode := slots["mode"].(os.FileMode)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.mkdir %v %v\n", path, mode)
		return nil, nil, nil
	}

	result, err := o.Impl.Mkdir(path, mode)
	return result, nil, err
}

// Read — Read creates a Blob from the file at "path" for reading the contents of the file at "path".
type Read struct{ Impl *provider.Provider }

func (o *Read) Name() string { return "file.read" }

func (o *Read) Do(ctx *op.Context, slots map[string]any) (op.Result, op.UndoState, error) {
	path := slots["path"].(string)

	if ctx.DryRun {
		_, _ = fmt.Fprintf(ctx.Writer, "[dry-run] file.read %v\n", path)
		return nil, nil, nil
	}

	result, err := o.Impl.Read(path)
	return result, nil, err
}

// Register registers all file actions with the given registry.
func Register(reg *op.ActionRegistry) {
	p := &provider.Provider{}
	reg.Register(&Backup{Impl: p})
	reg.Register(&Copy{Impl: p})
	reg.Register(&Link{Impl: p})
	reg.Register(&Move{Impl: p})
	reg.Register(&Remove{Impl: p})
	reg.Register(&RemoveAll{Impl: p})
	reg.Register(&Unlink{Impl: p})
	reg.Register(&WriteBytes{Impl: p})
	reg.Register(&WriteText{Impl: p})
	reg.Register(&Glob{Impl: p})
	reg.Register(&Mkdir{Impl: p})
	reg.Register(&Read{Impl: p})
}
